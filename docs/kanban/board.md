# MyVisa — Kanban board

> **Generated from [board.json](board.json) by `npm run board` — do not hand-edit.**
> Edit board.json (state) + [cards/](cards/) (detail), then regenerate. The visual
> dashboard is [board.html](board.html) (open in a browser). See [README.md](README.md)
> for how the board works.
>
> _Last updated: 2026-08-07 · stale threshold: 7d_

### In flight — 0 open PRs

> Read from `gh` at 2026-08-08 07:08 UTC. **Not board state** — never written to board.json.

_No open PRs._


## Backlog — 20

- **MV-27** · P4 · [Strip vs keep the mirrored visa-prep rows (checklist ↔ plan)](cards/MV-27-checklist-plan-dedup-rows.md) — _DECISION CONTEXT UPDATE 2026-07-02: since this card was cut, MV-69 (readiness rollup) and MV-71 (reverse stage tags) shipped ON TOP of the mirrored rows — STRIP is no longer the cheap option; KEEP-with-explanation is now the low-risk default. Original: BACKLOG — founder product call, spun from MV-23 part (2). The per-program checklist (lib/checklist/generator.ts) and the plan generator both emit the same AU visa-prep steps (NOC, biometrics, police certificate, GS statement, translations, agent MARN). MV-23 part (1) shipped mental-model copy framing the two surfaces (checklist = per-program requirement reference; plan = single action queue) and the completion ACTION is already deduped by the plan-links design, so the worst failure mode (double-completion) is already mitigated. The remaining call: STRIP the mirrored visa-prep rows from lib/checklist/generator.ts so they live only in the plan, OR KEEP the plan-links completion-mirror. A product/UX decision, not an agent call — needs founder direction before any build. Full spec in the MV-23 dossier. Dossier: cards/MV-27-checklist-plan-dedup-rows.md._
- **MV-38** · P2 · [Proof-of-funds as "next step" + what /documents is FOR](cards/MV-38-documents-purpose-and-next-step.md) — _PROSE REFRESHED 2026-07-02 (ground-truth audit): the “context-free next step” half is RESOLVED by MV-37 (2980e98 — guided A–E timeline replaces impact-ranked queue in lib/plan/select.ts); SourceLine criterion also shipped. Residual ask for the founder decision is ONLY the proof-of-funds-as-next-step framing + what /documents is FOR copy. Original: BACKLOG — founder product call. Founder #4: proof-of-funds 'looks out of place' on the home page and is 'placed as the next step', and 'uploading documents doesn't do anything for the student's actual process.' WHY IT'S THERE (verified): the plan generator emits upload-proof-of-funds whenever finance.proofUploaded is falsy (lib/plan/generator.ts:122-131; AUD 29,710 is real DHA-sourced, not a stub), and the dashboard surfaces the SINGLE top-ranked open plan item as the 'Next step' card (app/(app)/dashboard/page.tsx:30-36, selectNextStep) — so it appears whenever it's the highest-impact incomplete item, out of journey context. REAL ISSUE: /documents is STORE-ONLY (a personal locker — documents/page.tsx:27-30 'upload photos so you can pull them up'; completion = mere presence of an upload), it doesn't submit to DHA/universities or validate the amount — so 'Upload in documents' as THE next step can imply the real process advanced when it didn't. DECIDE: (a) reframe /documents as a readiness checklist (prep, not submission); (b) demote store-only uploads from the single dashboard 'next step'; (c) keep but make the value explicit + add a 'source: DHA' line (audit P3). Best resolved WITH MV-37 (in a journey, proof-of-funds lands in the financial-evidence phase). Dossier: cards/MV-38-documents-purpose-and-next-step.md._
- **MV-48** · P2 · Marketing imagery: two treated photos (landing-hero 4:5 + destination-header 16:9) per docs/imagery-policy.md — _RESCOPED 2026-07-02 (ground-truth audit): docs/imagery-policy.md ALREADY SHIPPED in a49d8a3 (Phase A) — struck from deliverables (stale-prose trap). Remaining slice = source + treat the two marketing photos only (landing hero 4:5, Australia destination header 16:9), founder-gated at ship time. Original: Phase C (audit 'AI-imagery strategy' + Phase C, P2; FOUNDER-GATED — outward-facing brand). WRITE docs/imagery-policy.md (positive/negative prompt guardrails, anchored on authentic 20-24yo Nepali students) BEFORE any photo ships. Then exactly two heavily-treated documentary photos under ONE warm-paper recipe: landing-hero 4:5 (banded beside headline, never behind text) + Australia destination-header 16:9. Product body stays imageless forever (the anti-AI-look defense). Founder approves the actual images before they go live. Spec: docs/audits/2026-06-25-design-division-polish-audit.md. [2026-07-03 overhaul disposition: hero photo slot superseded by the neutral global mascot mark (MV-87 umbrella / spec docs/design/2026-07-03-elevated-calm-overhaul-spec.md); documentary photos remain a policy-permitted future option.]_
- **MV-49** · P3 · Flag pills + quiet NP home nod — _Phase C (audit #19, P2-P3). Replace marketing emoji flags (Windows tofu-box risk) with a bordered IBM Plex Mono ISO-code pill or flat single-colour SVG; add a quiet NP home nod for the Nepali user. Make Nepal->Australia specificity visible in copy/markup, not pixels. Spec: docs/audits/2026-06-25-design-division-polish-audit.md #19. [2026-07-03 overhaul disposition: flag-pill half folds into the landing overhaul slice; the "quiet NP home nod" half is KILLED by the neutral-global-brand decision — no home-country iconography on marketing. Spec docs/design/2026-07-03-elevated-calm-overhaul-spec.md.]_
- **MV-50** · P3 · Hand-SVG marks on success empty states — _Phase C (audit #27, P3). One restrained teal hand-stroke mark on GENUINE success empty states only (never error/incomplete), extending the existing IconShield/IconGuide/IconDoc thin-stroke family muted to text-ink-faint. No AI spot-art. Spec: docs/audits/2026-06-25-design-division-polish-audit.md #27. [2026-07-03 overhaul disposition: reshaped — genuine-empty states get the corridor-mascot searching pose in the shell overhaul; founder decides in mascot brief whether it replaces or joins the stroke-icon family. Spec docs/design/2026-07-03-elevated-calm-overhaul-spec.md.]_
- **MV-85** · P1 · Overhaul Phase 0d: global mascot brief + Higgsfield prompt pack — _docs/design/2026-07-XX-mascot-brief.md: GLOBAL placeless character — abstract geometric migratory bird ("the Wayfinder", species-neutral, Phantom-ghost-level abstraction) + corridor-variant system = plumage skins on the SAME silhouette (Nepal = danphe-crested variant, post-onboarding only). 6 poses, 4 expressions, character sheet; Higgsfield prompts (founder-operated, CONCEPT ONLY) -> approved concepts hand-traced as stroke SVGs (IconShield family style, <20KB, CSS-var fills). Sizes: 24/32 guide avatar, 96-160 empty/error, 160 auth, 320-480 landing. FOUNDER GATES: character pick, concept approval, final SVGs. Spec: docs/design/2026-07-03-elevated-calm-overhaul-spec.md._
- **MV-86** · P1 · Overhaul Phase 0e: imagery-policy amendment (brand character mark + corridor rule) — _Append to docs/imagery-policy.md (never rewrite): new asset class "brand character mark" (hand-traced flat stroke-SVG, palette tokens only; NOT photography, NOT shipped AI pixels); CLOSED slot list (landing hero, auth, error pages, genuine-empty states, guide avatar, milestone beats); corridor rule: pre-onboarding surfaces = neutral global character ONLY, corridor variants only where corridor known, no home-country iconography on marketing; unchanged bans (never data surfaces / beside a verdict / on /how //trust); repoint #f6f5f1 recipe refs to token name; record MV-48 disposition. FOUNDER GATE: policy merge (batch review with MV-85). Spec: docs/design/2026-07-03-elevated-calm-overhaul-spec.md._
- **MV-87** · P1 · Overhaul Phases 1-2 UMBRELLA: funnel + shell (carve per-slice cards after Phase 0 gates) — _Umbrella for the execution phases of the Elevated-calm overhaul — DO NOT build from this card; carve slices with next-free IDs when Phase 0 (MV-82..86) gates clear. Phase 1 funnel: rebrand token swap (+token-contrast.test.ts permanent guard, themeColor meta, favicon), type scale, Card/VerdictPill primitives + Button loading (finishes MV-42 slices 2-3), Motion v2 + honest recap (kills 3s fake analyzing), corridor theme provider (data-corridor attr + lib/theme/corridor.ts registry, np-au only), landing (neutral mascot hero; folds MV-49 flag-pill half — NP-home-nod half KILLED by neutral-brand decision), wizard, results (first corridor-accented surface), auth, mascot slots. Phase 2 shell: dashboard, matches/plan/checklist/documents (reshapes MV-50), profile, guide avatar, chrome+OG. Full detail incl. sequencing, verification, risks: docs/design/2026-07-03-elevated-calm-overhaul-spec.md._
- **MV-124** · P1 · [2026-07-10 audit: slices 2-9 (tracking)](cards/MV-124-audit-remainder-slices-2-9.md) — _undefined_
- **MV-131** · P2 · [Guide: citation contract unenforced, remembers-you false, chats vanish (audit C-12)](cards/MV-131-guide-grounding-memory-honesty.md) — _Audit C-12, uncarded. Grounding is a prompt promise not an enforced contract; the guide implies a memory it lacks; guide-chat.tsx holds messages in component state so a refresh wipes them. Split: copy honesty (small, first) + chat persistence (medium) + citation contract (large, product call, likely a follow-up). Distinct from MV-78/79 (timeout/anti-injection)._
- **MV-136** · P1 · [The product ships under three identities; the name is unsettled (audit F-4)](cards/MV-136-product-name-decision.md) — _Audit F-4 plus the Today founder P0, uncarded (MV-05 holds legal facts, not the name). Multiple identities ship at once (the MeroVisa placeholder plus others), eroding trust and blocking the rebrand+mascot work (name was to be decided FIRST). This is a FOUNDER DECISION card; the mechanical rename sweep (wordmark/metadata/copy/CLAUDE.md) is a downstream agent-buildable follow-up._
- **MV-137** · P1 · [Umbrella: security + reliability hardening (audit F-15/F-16/O-3/O-4/O-5/O-6)](cards/MV-137-security-reliability-umbrella.md) — _UMBRELLA, split a scoped card off when a slice starts; do not build from this directly. Tracks the audit before-launch engineering risks so they stop living only in the report: F-15 no prod monitoring (Sentry unwired; MV-62 = in-app boundaries only), F-16 security cluster (no CSP, fail-OPEN rate limit, sign-claim HMAC oracle, over-broad selects; split further), O-3 non-transactional claim/bootstrap/lead/apply flows, O-4 profile-JSON lost updates, O-5 CI is advisory + no E2E gate, O-6 static-bailout catch + Google-Fonts build dep. Sequence after the student-facing gaps + P0s._
- **MV-138** · P2 · [Umbrella: journey depth, growth loops, orphaned data (audit F-5/F-7-F-14/F-18/F-21/F-22/F-24)](cards/MV-138-journey-growth-umbrella.md) — _UMBRELLA, split a scoped card off when a slice starts; do not build from this directly. Mostly the audit next-month phase. Highest ROI: F-11 (5 sourced modules render to zero surfaces, shipping paid-for value). Trust-sensitive: F-21 fabricated placeholder destination policy, F-5 undisclosed heuristic core. Journey: F-9 human fallback + /about, F-10 post-grant stages, F-13 Reach off-ramp (MV-121 deferred this), F-14 real tracker, F-12 GS/GTE workspace. Growth (needs a founder email call): F-6 email auth, F-7 deliver-a-copy + reminder, F-8 re-engagement/share, F-24 distribution + assessment_claimed event. Debt: F-18 systemic freshness, F-22 de-hardcode + CLAUDE.md fix._
- **MV-163** · P2 · [Enumerate the client-writable INSERT columns on the four Stage 1 tenancy tables, and extend the column-axis guard to cover them](cards/MV-163-tenancy-insert-column-enumeration.md) — _Carved from MV-161's enumeration pass, which incidentally surfaced that the four Stage 1 tenancy tables (cases, case_assignments, invitations, organization_memberships) show the same class of unbounded client-writable INSERT columns MV-161 just fixed for the five case INSERT surfaces — and are NOT covered by MV-161's new column-axis guard, which is deliberately scoped to those five. Do for the four what MV-161 did for the five: enumerate every client-writable INSERT column from the catalog (information_schema.column_privileges x pg_policy, both sides derived at run time, never a hand-written list), bound each hostile axis or record it exempt-with-reason, and EXTEND the guard so an unbounded/unexempted column on a tenancy table fails CI. The guard must be SHARED rather than copy-pasted — a second hand-maintained completeness guard is complete for one family and silently stale for the other. Triaged P2 EXPLICITLY AND PROVISIONALLY: MV-161 earned P0 by measuring a live account-delete lock (P0001) off a hostile supersedes_prediction_id plant; here the finding is structural symmetry with no reproduced breach, so the enumeration is the card's FIRST action and the card re-triages upward the moment it surfaces a genuinely hostile column — an unbounded organization_id, a cross-tenant pointer, or a role/status column that escalates a membership. invitations is flagged for first inspection: its columns map directly onto privilege escalation, and an unbounded role column on an invitation is an admin-grant primitive. Risk cuts both ways here — these tables ARE the tenancy boundary, so every new bound needs an ADMITTED control beside the REFUSED probe or the card ships a staff-onboarding lockout. A table with no authenticated INSERT grant is recorded as such explicitly, not silently absent, so a Stage 3 grant cannot land uncovered. Blocked by MV-161 merging (PR #123) since it extracts that file's guard; coordinates with MV-152/MV-153, whose matrix expectations may not be edited to accommodate it._
- **MV-167** · P1 · [Umbrella: Stage 3 consultancy workspace (MV-168 grants · MV-169 org+team · MV-170 student list · MV-171 case creation+assignment · MV-172 case route · MV-173 indicators+field allowlist · MV-174 service-role retreat + stage exit)](cards/MV-167-stage3-umbrella.md) — _TRACKING ONLY — do NOT build from this card. Drives Stage 3 to its exit gate (an authorized counsellor can create, find, assign, and manage a case WITHOUT a student account) across seven slices. The authoritative document is docs/superpowers/specs/2026-08-07-stage3-workspace-and-access-matrix.md, written by MV-166 and grounded in a live schema read rather than the plan's four bullets; every slice reads it instead of the prose, and a slice that contradicts it amends it IN ITS OWN PR. Holds the slice map, the DAG (a stated reason per edge; NO release train, and why), the six exit criteria with their vacuity guards, and F-1 — the one open founder decision: the plan's exit gate says a COUNSELLOR can create and assign, but both enforcement layers deny a counsellor exactly those two verbs, so the spec proceeds provisionally on reading (a) rather than move a canonical cell. NOT blocked by the Stage 0 D-B legal gate: D-B gates real-data onboarding (Stage 7's pilot), not construction, and all seven slices are reachable on seeded test data. Dossier: cards/MV-167-stage3-umbrella.md._
- **MV-170** · P1 · Stage 3 slice 3 — student list, search, filters, and statuses — _Plan bullet 2a. The org-scoped case list with search, filter and operational_status display. READ-ONLY — no creation, no assignment, no writes at all (MV-171 owns those). Assigned-only for counsellors, all-org for owner/admin, nothing for an unassigned counsellor or an inactive membership (cases_select_accessor → admin-org ∪ assigned ∪ own; access matrix cell 7). Depends on MV-169: the list is scoped by the selected organization, so with no org context there is no scope to list within. TEST TRAP recorded in the spec's exit-gate vacuity table: a fixture with only ONE case, or an 'unassigned' counsellor who holds no membership at all, turns this into a tenancy test that Stage 1 already passes — the fixture must hold at least two cases in the SAME org with the unassigned counsellor holding an ACTIVE membership. This slice is also what first exposes F-3 (a linked student can rewrite the display_name and email this list shows a counsellor); the fix lands in MV-173, in the same stage that creates the exposure._
- **MV-171** · P1 · Stage 3 slice 4 — case creation and assignment (carries the F-1 decision) — _Plan bullet 2b. Create a case with organization_id set and student_user_id NULL through the AUTHENTICATED client; assign/reassign the primary counsellor; write operational_status (gated by the cases_write_surface_guard trigger → can_staff_case). Also adds the case-scoped scoring route, because a case needs an assessment and spec §6.1 refuses to let the client write one — a NEW service-role registry entry, deliberately registered, which is why Stage 3 ends at 8 entries rather than 7. BLOCKED ON FOUNDER DECISION F-1 (MV-168/169/170 are not): the plan's exit gate says an authorized COUNSELLOR can create and assign, but BOTH enforcement layers deny a counsellor exactly those two verbs — cases_insert_admin and case_assignments_insert_admin require is_org_admin, and CASE_PERMISSION_MATRIX.counsellor sets case.create and case.assign to 'deny' with the comment 'Widening this is a deliberate later decision, not a convenience'. The layers AGREE; it is the plan's prose that disagrees. Reading (a) = 'counsellor' is loose prose for consultancy staff, build for owner/admin, nothing moves (spec's recommendation). Reading (b) widens the role and MOVES A CANONICAL CELL, needing its own slice plus edits to the Stage 1 suites. NOT a multi-counsellor model: case_assignments_primary_idx allows at most one primary counsellor per case and assignment_role is restricted to that single literal. No archive (Stage 6), no student invitation (Stage 5)._
- **MV-172** · P1 · Stage 3 slice 5 — render the existing experience inside an explicit case route — _Plan bullet 3, and the largest surface in the stage. Renders the existing MeroVisa experience (profile, matches, plan, documents, checklist) under an explicit case route for a case that is NOT the actor's own — the first time a non-student actor reaches student data through the product. Flips app/api/plan/action/route.ts off service-role onto the authenticated client (retiring registry entry 8) and SPLITS app/api/profile/section/route.ts — its profile write moves, but reScoreAssessment (assessments UPDATE result), invalidatePlan's generator-column copy refresh (impact/title/body/estimates) and adoptOwnerKeyedResidue (UPDATE case_id) are all refused by spec §6.1 and stay service-role, so entry 9 NARROWS rather than retires; all three would fail SILENTLY if flipped (re-score.ts:33 never destructures error, and throwOnError appears nowhere in lib/ or app/). Those moves ARE grants 1 and 5, which is why MV-168 is a hard predecessor (without it this slice ships the case route still on service-role, the exact inverse of the enforcement boundary). Depends on MV-171 for a real student-less case to exercise: before it, the route can only be tested against a personal case, which is the Stage 2 shape and proves nothing new. NOT IN SCOPE: any change to the documents model, Storage paths, or signed downloads (Stage 4 — object paths stay owner-keyed through Stage 3 by the Stage 2 spec §8 decision); the case-context indicators (MV-173). PLUS F-8: five case-scoped write routes (shortlist:46, documents/status:33, outcomes/prediction:28, outcomes/attempt:31, outcomes/event:32) resolve the ACTOR'S OWN personal case via resolvePersonalCaseId and accept no case id — left as-is, every write control in the case route writes to the COUNSELLOR'S own case, and nothing fails loudly because the grants and policies already exist. They take an explicit, authorized case id here. Access matrix cells 13-17 and 21-23 in spec §4; exit criterion E7._
- **MV-173** · P1 · Stage 3 slice 6 — case-context indicators, and closing the student field-allowlist gap (F-3) — _Plan bullet 4, plus the trust bug the list surface exposes. Persistent, unmissable indication of WHOSE case the actor is in and whether it is their own — a counsellor editing a student's profile must never be able to mistake it for their own data. PLUS the TYPESCRIPT field allowlist that lib/cases/permissions.ts and lib/cases/README.md both record ('a Stage 3 mutation that accepts an arbitrary case patch from a student is a defect even though this cell allows the claim'); README.md:152-157 locates the remedy — 'the allowlist belongs with that mutation'. NOT IN SCOPE: F-3's column guard. cases grants authenticated UPDATE on (archived_at, display_name, email, operational_status) and enforce_case_write_surface guards only archived_at and operational_status, so display_name and email ARE student-writable and MV-170's list renders exactly those two to a counsellor — but that omission is the CANONICAL decision, not a gap (stage1-canonical-access-matrix.md:68-70 'the student's write surface is profile fields only'; 20260730180000_case_aware_rls_policies.sql:459 'display_name, email — profile fields. The student's surface'), and it is pinned POSITIVELY by tests/integration/case-rls.itest.ts:833. Narrowing it turns that Stage 1 test red, which spec §5 defines as proof a slice moved a canonical cell and is wrong. F-3 is escalated to the FOUNDER alongside F-1; the impersonation risk is real, and under reading (a) it is mitigated at MV-170's display layer, not the write layer. NOT IN SCOPE: consultancy-internal notes (F-4 — the case.notes.internal permission exists but no column or table does; Stage 4 owns case activity, and no slice invents a notes model to satisfy a permission constant)._
- **MV-174** · P1 · Stage 3 slice 7 — service-role retreat and the stage exit gate — _CARRIES THE STAGE EXIT. Completes the service-role disposition (spec §6.2): reclassify entries 1 and 2 to 'sanctioned' (F-6 — app/(focused)/assessment/[id]/page.tsx serves an UNAUTHENTICATED visitor so no grant to authenticated can ever retire it, and app/api/account/delete/route.ts says 'DELIBERATELY still owner-keyed' in its own justification; both are permanent designs mislabelled as grandfathered, so the list currently reports two entries as awaiting a migration that will never come), narrow entries 3, 4 AND 9 (app/api/profile/section/route.ts does NOT retire — reScoreAssessment, the plan copy refresh and adoptOwnerKeyedResidue are all refused by spec §6.1, and service-role-exceptions.test.ts:280/:293 pin both horns, so a builder cannot keep the admin client and delete the entry), confirm 5-7 wait for Stage 4's Storage policies, and register the new case-scoped scoring route. Net 9 → 9 — the same count it started with, one retired and one added — and the growth is admitted rather than hidden: MV-154's framing of the list as monotonically shrinking does not survive Stage 3, and the stage is scored on scope, not on list size. THEN proves the exit gate — seven criteria E1-E7 at named layers (spec §9.1), each shipping the vacuity guard from §9.2. E4 is the direct analogue of the Stage 2 §A2 vacuity MV-165 recorded honestly: it passes trivially if the case under test has a student_user_id, or on an empty write set, so it must assert cases.student_user_id IS NULL, owner IS NULL on every row written, AND a created-row count > 0. E1 must assert the actor's JWT role is 'authenticated' (a test run as service_role bypasses every policy and proves nothing). E6 must assert per-entry disposition, never a count (the tracked set is 9 at exit). E7 pins F-8: each case-scoped write route must write the STUDENT'S case id, read back from the row — and the counsellor fixture must itself hold a personal case with a row on the same table, or a wrong-case write has nowhere to land and the test proves nothing. AND THE EXIT MUST NOT OVERCLAIM (spec §9.3): with D-B shut, production holds no consultancy org and no student-less case, so a green Stage 3 licenses 'the mechanism is built and proven under RLS as the authenticated user' and NEVER 'it works for real students' — that is earned in Stage 7's pilot._

## Ready (WIP 5) — 0

_empty_

## In progress (WIP 1) — 1

- **MV-169** · P1 · [Stage 3 slice 2 — org context, org selection, and team management](cards/MV-169-org-context-team-management.md) — _Plan bullet 1. Org selection for a multi-org actor (wires getOrgContext/requireOrgPermission to a real surface) plus the team list, role change, and deactivate. Owner-only org settings (canonical divergence #1, verified live: organizations_update_owner reads actor_owner_org_ids). Role change must refuse role='owner' for a non-owner (divergence #5's carve-out). NOT IN SCOPE, both recorded as findings on the spec: org CREATION (F-2 — authenticated holds no INSERT grant on organizations and there is no INSERT policy, so provisioning stays a founder/ops action; no slice grants it), and INVITATIONS (F-5 — staff invitations are Stage 5, so Stage 3 team management can change a role and deactivate a member but CANNOT add one; plan bullet 1 reads as a more complete surface than it is). No case surfaces. Access matrix cells 1, 2, 4, 5 in spec §4._

## In review (WIP 3) — 0

_empty_

## Blocked — 3

- **MV-05** · P1 · [Legal / disclaimer / data boundary](cards/MV-05-legal-disclaimer-boundary.md) · ⏳ 51d in column — _Engineering slice DONE + merged to master (46752f3; gate green): not-immigration-advice disclaimer on verdict/results/dashboard/matches/plan (VerdictDisclaimer), + working right-to-delete path (POST /api/account/delete removes storage+all owned rows+auth user, partial-failure→500 not ok:true) with a type-to-confirm UI on profile. BLOCKED on founder/lawyer for the rest. UPDATE 2026-06-20: full copy packet DRAFTED + Codex-reviewed (5 blockers + 6 should-fixes folded) at docs/legal/2026-06-20-mv-05-legal-copy-packet.md — tightened disclaimer + APP 5 at-collection notice/consent + Privacy policy (/privacy) + ToS (/terms, provider-neutral/no-commissions) + under-18 stance. D1+D2 decided by founder; copy approval delegated to Codex → APPROVED-WITH-NITS (nits applied). WIRED 2026-06-20: the tightened Codex-approved not-advice disclaimer (names DHA/institution as deciders + routes to an OMARA agent/lawyer) shipped on results/dashboard/matches/plan (TDD +1; gate green, 1171 tests). STILL BLOCKED on the remainder: D3 (legal entity name + contact email + governing-law jurisdiction + confirm Vercel region) before /privacy + /terms static pages can publish, plus the consent/DOB/guardian sub-slice (own card; needs founder DB approval). Card can't reach Done until those land._
- **MV-08** · P2 · [Outcome-validation loop (the moat)](cards/MV-08-outcome-validation-loop.md) · ⏳ 49d in column — _DESIGN DELIVERED + CODEX REVIEW FOLDED (no code shipped): build spec for the verdict-validation loop — funnel applied/offer/refused/visa linked to the frozen prediction (verdict + RULE_VERSION + scoreSnapshot). Codex (GPT-5) refute-each-decision pass found 3 blockers + 8 should-fixes, all folded into the doc. Schema is now 3 tables: program_predictions (immutable, prediction-run model, UPDATE-guard trigger) -> application_attempts (B1: institution/program/intake attribution layer) -> outcome_events (append-only; explicit gate + reason_code + verification metadata; self_reported excluded from training). Two-gate model (admission vs visa calibrate separately); verdict recomputed server-side (F16); calibration = CIs + Bayesian pooling on verified outcomes (not n>=30), compatibility-group windowing; cold-start ladder. Doc: docs/superpowers/specs/2026-06-19-outcome-validation-loop-design.md. Gate to build: founder approves 3-table migration + answers 4 open Qs. BUILD STARTED 2026-06-20 (founder steered 'do MV-08'): migration supabase/migrations/20260620000000_add_outcome_validation.sql authored + Codex-vetted (2 defects folded: outcome_events.verified_by FK index + pp_insert_own assessment-ownership check). Founder answers: apply-now (inert) + calibration=CIs+Bayesian + capture=passive/light-nudges; Q3 (verification near vs later) explained in plain terms, awaiting pick (non-blocking for migration/capture). DKIM CAPTURE MECHANISM DECIDED + Codex-vetted GO-WITH-CHANGES 2026-06-20 (Gmail-OAuth rejected; primary = student forwards raw .eml to a per-user verify address, Cloudflare Email Worker verifies DKIM at receipt + binds via >=2 identifiers; human upload fallback) -> folded into doc 6/7/8/9/13. SLICE 1 SHIPPED (TDD, green): DB-independent capture contracts lib/outcomes/{types,events,state-machine,verification}.ts + lib/validation/outcomes.ts, 41 tests, full suite 1212/1212, typecheck+lint clean. SLICE 2 SHIPPED (TDD): lib/outcomes/predict.ts buildPrediction (F16: recomputes per-program verdict + {gradeGap,englishGap,bandGap,tuitionGap} snapshot + ruleVersion via new computeMatch single-program export; snapshot-shape question RESOLVED - doc was right, freeze uses per-program match not corridor runAssessment; exported RULE_VERSION). suite 1217. SLICE 4 SHIPPED (6e5296c): 3 POST routes + GET + applied-transition hook. SLICE 5 SHIPPED (4072730, TDD): UI capture trigger + funnel-root fix — shortlist-button is now a 3-state status control (Not saved->Shortlisted->Applied; founder-chosen via AskUserQuestion), choosing Applied POSTs status:'applied'; persisted status threaded page->verdict-group->card (Set->Map) so Applied survives reload; captureApplication now writes the idempotent root 'applied' outcome_event when it opens the attempt (server-derived gate/authority/source, anchored to attempt.createdAt), closing the confirmed 409 dead-end and self-healing event-less attempts (e.g. smoke-test 073d60dc on next apply). Gate green: typecheck/lint, suite 1249/1249 (+7). OPEN: inbound email handler + verification admin path (gated on founder legal: PIA/minor-consent, VEVO ToS). Founder still: Q3 verification legal gates; a live UI smoke (sign in -> mark Applied -> root applied event lands -> follow-on offer_received returns 201 not 409); clean smoke-test prod rows (pred 4bf88e7d + attempt 073d60dc, owner ece83f09)._
- **MV-55** · P3 · Scholarships how-to: eligibility + application-process dataset (RESEARCH-BLOCKED) · ⏳ 43d in column — _RE-SHAPE NOTE 2026-07-02 (ground-truth audit): not truly externally blocked — pickup = ONE research+build slice: agent re-verifies the 4 flagged gaps against primary sources (DFAT Policy Handbook PDF, UniMelb GRS rates, Sydney RTP stipend), ledgers verified facts as J findings, then builds eligibility/how-to panel rows. Original: Founder ask (2026-06-26) — the RESEARCH-BLOCKED half of scholarship guidance. Full ask = per-award eligibility criteria + application steps + required docs. The honestly-shippable, LEDGER-BACKED subset was extracted & shipped 2026-06-26 as MV-58 (research-degree-only signal on UniMelb GRS + Sydney $135M pool relabel). What REMAINS here is still blocked: the brief (docs/research-briefs/2026-06-26-mv55-scholarship-eligibility.md) sourced eligibility/figure/process facts but flagged them NEEDS-REVERIFY (snippet-sourced; provider pages 403-block automated fetch) — NOT ledgered as findings, so surfacing them breaks trust-first. UNBLOCKS WHEN: a per-scholarship eligibility+application-process dataset is sourced & VERIFIED & ledgered, then expand ScholarshipsPanel with a 'Who's eligible / How to apply' block. Effort M, DATA-DEP._

## Done — 151

- **MV-61** · P1 · [Accept PDF document uploads (transcripts, offers, loan letters, CoEs)](cards/MV-61-pdf-upload.md) — _IN REVIEW — SHIPPED 2026-06-26 on branch mv-61-pdf-upload (founder merges to master to close → Done). First Tier-1 fix from the 2026-06-26 strategic reassessment: the document store accepted images only, so every real checklist document (transcripts, offer letters, loan sanction letters, CoEs — all PDFs) hit a 422. Under the north star (app must be complete enough that a student never needs a consultancy) an un-fileable document store is a bounce point. Added PDF end-to-end: ALLOWED_TYPES + rejection copy 'File must be JPG, PNG, WebP, or PDF' (app/api/documents/upload/route.ts); verifyImageMagic renamed verifyFileMagic with a %PDF (25 50 44 46) branch that rejects MIME spoofs both ways + extensionFor pdf->'pdf' (lib/documents/upload-validation.ts); file-picker accept adds application/pdf (components/documents/document-card.tsx); DocumentViewerModal gains isPdf and renders an iframe instead of a broken <img>, view-error copy 'image'->'document'. TDD RED->GREEN; gate green: typecheck clean, lint 0 errors (1 pre-existing build.mjs warning), full suite 235 files / 1402 tests (was 1396). No scorer/goldens/migration. Dossier: cards/MV-61-pdf-upload.md._
- **MV-01** · P1 · [Consolidate the two match engines](cards/MV-01-match-engine-consolidation.md) — _DONE pending review: anon results now read the same DB catalogue + shared computeMatches as signed-in; GPA normalized at the boundary (root cause in from-assessment); program-level anon results; backcompat shape-guard. Gate green (typecheck/lint/test 1086 pass). Pushed to origin/feat/context-budget (8f06f00); PR not yet opened (gh unauthed — founder opens via compare link)._
- **MV-02** · P2 · [Surface swallowed errors](cards/MV-02-surface-swallowed-errors.md) — _DONE pending review (pushed origin 26216c8; PR not opened): a 13-agent audit found 4 real swallows (profile/section, documents DELETE, documents upload, assess signed-in insert) + 5 already-correct routes. All 4 now return a real error status + structured log on a failed write; document-card delete now guards on res.ok; leads catch now logs. Gate green: typecheck/lint/1106 tests (+8). TDD failing-path test per fix._
- **MV-04** · P2 · [Data-freshness UX + stale-fact CI](cards/MV-04-data-freshness-ux.md) — _DONE pending review (pushed origin 26216c8; PR not opened): scoring-critical facts (CONFIG_PROVENANCE) now have a runtime freshness check — past reverifyBy degrades the verdict card (amber warn, treat-as-indicative) instead of showing a stale verdict as current, plus a CI guard (tests/data/scoring-freshness.test.ts) that goes red when a verdict input ages out. Dormant today (armed until 2027-06-07). Gate green: typecheck/lint/1114 tests (+8). No scorer value changed; goldens byte-identical._
- **MV-03** · P3 · [Wire or relabel the dead work input](cards/MV-03-work-input-field.md) — _DONE pending review (pushed origin 26216c8; PR not opened): recon found work.title actually feeds the document checklist, but Relevance/Years imply a verdict effect no scorer reads. Chose context-only relabel — WorkGapEditor now states 'Optional. Your role helps tailor your document checklist — it doesn't change your verdict.' Copy-only, no scoring change, goldens byte-identical. Gate green: typecheck/lint/1115 tests (+1)._
- **MV-A2** · [Apply Supabase advisor migration](cards/MV-A2-supabase-migration.md) — _DONE 2026-06-20. Migration applied to prod + 3 advisor findings cleared (2026-06-18); leaked-password protection toggle enabled by founder (2026-06-20). All advisor items resolved._
- **MV-10** · P2 · [Cost-estimate tab](cards/MV-10-cost-estimate-tab.md) — _DONE pending review (committed to master). Cost-estimate tab is LIVE: new sourced OSHC fact module (lib/data/source/au-oshc-premiums.ts — 5 gov-approved providers: nib 679.56/yr, Bupa 760, Medibank 949.2; ahm+Allianz quoteOnly, null amounts never fabricated) with AuOshcPremium type + dedicated Zod schema (cost-to-apply DISPLAY pattern: per-record source+lastVerified, NOT the findings ledger). selectCostEstimate() composes the AUD first-year band tuition 44,500 + DHA living 29,710 + OSHC 680–949 + visa 2,000 = 76,890–77,159; CostEstimatePanel renders it (each figure one click from its source; tuition = labeled heuristic, no fake cite) + an OSHC-by-provider sub-list (3 priced + 2 'Quote only'). Wired into /matches replacing 'coming soon'. nib + Medibank re-verified live; Bupa rides on the fan-out's price-list reading (flagged). Gate green: typecheck/lint/1170 tests (+25). TDD failing-test-first per unit. Founder review notes in dossier (Bupa partner-hosted source; tuition heuristic). Dossier: cards/MV-10-cost-estimate-tab.md._
- **MV-06** · P2 · [Integrate ledger slice E/I](cards/MV-06-integrate-ledger-slice.md) — _COMPLETE 2026-06-19 (founder steered 'do what's recommended'). All 4 ready Category-I findings SURFACED as copy-only gov citations — golden-assessments.json byte-identical, TDD per finding: I.010 PIC 4020 (clause 500.217) on the document-integrity row + I.058 MI-request-is-not-permission-to-stay refusal-recovery row (84d518c); I.026 OSHC ≥1-week-before timing on the checklist OSHC requirement (e0cf362); I.025 English approved-test-OR-exemption — new registered english record in AU_STUDENT_VISA_REQUIREMENTS surfaced on the inline checklist English item (no duplicate IELTS row), copy triangulated via Codex + founder-approved general phrasing (6219bac). Gate green: typecheck/lint clean, 1145 tests (+5 TDD across the slice), reconcile + findings-integrity + flip-status guard green. Deferred 45 Category-E findings spun into MV-13 (fact-layer→DB bridge, founder DB-gated). In Review (human gate). Dossier: cards/MV-06-integrate-ledger-slice.md._
- **MV-07** · P2 · [Surface CRICOS provider codes (trust signal)](cards/MV-07-cricos-trust-signal.md) — _DONE + pushed to origin/master. Scope pivoted at kickoff: the CRICOS 'scrape' was already done (70+ providers sourced in TS, gate-checked) and the GS panel already rich, but the sourced CRICOS data was dormant (no consumer). This slice surfaces it — program cards now show the provider's `CRICOS <code> ↗` linking to the official register, via a pure EXPLICIT id→code lookup (not name-derived: Adelaide=merged 04249J; Melbourne/ANU unsourced→null, render nothing). No scorer/DB; goldens untouched. Gate green: typecheck/lint/1128 tests (+7). DB tables + per-provider evidence levels deferred (speculative / sourcing-blocked)._
- **MV-09** · P3 · [Replace hardcoded FX rates](cards/MV-09-fx-single-source.md) — _DONE pending review (local commit, not pushed; awaiting founder GO): budgetToAud now derives budget→AUD from the canonical FX_RATES table via a shared toAud helper — divergent inline rates + the 'Replace with FX lookup later' TODO removed (one source of truth). NPR corrected ÷100→÷90 (MVP corridor; INR/BDT/PKR now exactly canonical; USD/AUD/NGN unchanged) — an intended match-path diff locked by tests; scoring golden byte-identical (match path, not engine). Codex adversarial pass: no defects. Gate green: typecheck/lint/1121 tests (+6). Two client-side 135 literals (callouts, budget-step) noted as deferred hygiene follow-up — already agree with canonical, not divergent._
- **MV-11** · P3 · AI guide — _CLOSED STALE 2026-07-02 (ground-truth audit): fully superseded by MV-63 (DeepSeek client + grounded context + auth-gated route + chat UI, PR #10) hardened by MV-78 (timeout, PR #32) + MV-79 (forged-history defense, PR #33); DEEPSEEK_API_KEY set, live in prod at master 9c9fd0b. The card’s literal spec (Claude Haiku + cached-corpus RAG) would be a NEW architecture card if ever wanted — the shipped deterministic-grounding design already delivers the student outcome. Original: Claude Haiku 4.5 + cached-corpus RAG over the TS fact layer. Deferred until the deterministic core is reliable._
- **MV-13** · P2 · [Bridge TS fact layer into DB catalogue](cards/MV-13-fact-layer-db-bridge.md) — _DONE pending review (DB bridge LIVE in prod). Bridged the reviewed TS fact layer into the live programs catalogue: migration 20260619000000 adds duration_years/finding_refs/generated, enriches 3 true-twin RMIT rows in place + upserts 19 verified programs (RMIT bachelors/masters + UTS Pharmacy + Melbourne Education + Deakin Data Science); catalogue 64->83, all with provenance. Decision MERGE + DEFER (Codex-triangulated): defer 2 RMIT diplomas + 5 Torrens (no ranking-safe entry data). Applied to prod (da28e36; apply_migration {success:true}; get_advisors zero new issues; new cols covered by existing programs_read RLS). Findings-flip step found ALREADY COMPLETE on resume — all bridged Category-E findings are status:used + value_status:structured + reconcile-green (the flip derives from the registered TS fact modules, not the DB, so it landed in the earlier Category-E sourcing slice; the dossier's stale 'remaining flip' premise corrected). Gate green: typecheck/lint, programs parity (24) + ledger guards reconcile-modules/flip-status/findings-integrity/registry-integrity/conflict-gate (16), goldens byte-identical. Anti-drift guard tests/programs/bridge-fact-parity.test.ts. In Review (human gate -> founder closes to Done). Dossier: cards/MV-13-fact-layer-db-bridge.md._
- **MV-15** · P2 · [Read-side outcome surface (make the moat loop visible)](cards/MV-15-outcome-funnel-surface.md) — _DONE pending review (committed to master). The user-facing read half of MV-08's moat: GET /api/outcomes / getOutcomesForUser returned raw rows but nothing showed the user their own loop. New pure lib/outcomes/funnel.ts: deriveFunnelStage(events) folds a recorded event set into the current display stage (applied|offer|accepted|rejected|visa_lodged|visa_granted|visa_refused|enrolled|withdrawn) with terminal/negative precedence (a refused visa reads 'Visa refused', not 'CoE') — complements state-machine.ts (which only says what's legal next); buildOutcomeFunnel joins attempt→prediction(verdict)+program name, sorts by recency, drops prediction-less attempts. New presentational components/outcomes/outcome-funnel.tsx (verdict tint chips + stage pill, honest 'self-reported until verified' copy, renders null when empty). Dashboard renders 'Your applications' after StatsRow, fetching programs/universities ONLY when attempts>0 (no empty shell, no extra queries for the current no-attempt users — matches the 'no fake trackers' stance). No DB schema/scoring/golden change; reads via RLS-scoped server client. Gate green: typecheck/lint, full suite 1268/1268 (+15 TDD). FOUNDER-OWED: live smoke (OAuth-gated + needs seeded attempts — not headless-provable). Dossier: cards/MV-15-outcome-funnel-surface.md._
- **MV-14** · P1 · [Wire lead-insert into the OAuth claim (leads=0 fix)](cards/MV-14-leads-insert-conversion.md) — _DONE pending review (committed to master). Root-caused leads=0 (46 assessments, 5 profiles, 0 leads): conversion is OAuth-only and createLead's only caller (POST /api/leads) is called only by tests — the lead-insert step was orphaned, a wiring gap not a conversion/tracking failure. Fix: claimAndBootstrapProfile now records a lead (authed Google email + assessmentId) on successful claim via the existing idempotent createLead (upsert on (assessment_id,email), ignoreDuplicates); best-effort (try/catch + structured console.error) so a lead-write failure never blocks login; auth/callback passes data.user.email through. No DB schema change, no new PII category (leads.email is purpose-built, service-role-only RLS, never in URL/log), no scoring/golden change, F16 untouched. Deliberately skipped oauth_started client event (lossy across the OAuth redirect; gate_cta_clicked already covers it) — funnel observable via existing client events + non-zero leads/profiles counts + server error log. POST /api/leads left in place (already-orphaned, possible future email-gate). Gate green: typecheck/lint, full suite 1253/1253 (+4 TDD). FOUNDER-OWED: live smoke (real Google OAuth + Supabase — not headless-provable): assess -> Continue with Google -> confirm a leads row lands; confirm the design call (leads as funnel-bottom record vs retire it). Dossier: cards/MV-14-leads-insert-conversion.md._
- **MV-16** · P1 · [Re-assessing never updates the dashboard (primary newest-wins)](cards/MV-16-primary-assessment-newest-wins.md) — _DONE pending review (committed to master). Root-caused the dashboard-pinning bug found while live-smoking MV-14: partial-unique index assessments_primary_idx = UNIQUE(owner) WHERE is_primary allows one primary per owner; the dashboard reads the primary one; but claimAndBootstrapProfile ran a single unconditional promote (update is_primary=true .eq(id).is(is_primary,false)) and DISCARDED the returned {error}. On the 2nd+ claim the owner already has a primary, so the promote violated the index, the error was swallowed, the new row stayed is_primary=false, and the dashboard stayed pinned to the first assessment ever claimed (confirmed live: owner ece83f09 has 16 assessments, only the oldest is primary). Fix A (newest-wins): demote-then-promote as TWO sequential app-layer updates (business logic in Next.js, NOT a Postgres RPC — Codex suggested an RPC, overruled): (1) is_primary=false where owner=userId and is_primary=true, (2) is_primary=true where id=assessmentId; after the demote the owner has no primary so the promote can't trip the index. Both {error} results are now read + structured-logged ([claim] demote/promote ... failed) — no longer swallowed; misleading comment fixed; MV-14 lead-insert unchanged. Design call: LOG not throw (auth/callback has no try/catch — throwing would 500 an already-claimed user); residual demote-ok/promote-fail leaves a momentary no-primary state (logged), which MV-17 removes for the common path. No DB schema/scoring/golden change; F16 untouched. Gate green: typecheck/lint, full suite 1270/1270 (+2 TDD: newest-wins order+filters; failed-promote-is-logged). FOUNDER-OWED: live smoke (2nd assessment on an existing account → dashboard shows the new result; new row is_primary=true, old flipped false); existing pinned rows self-heal on next claim only (retro fix = prod write, founder-gated). Dossier: cards/MV-16-primary-assessment-newest-wins.md._
- **MV-17** · P2 · [Route logged-in re-assessment to profile-edit/reScore](cards/MV-17-logged-in-reassess-routing.md) — _DONE pending founder review (Option A, founder-pre-authorized). Signed-in re-assess now re-scores the EXISTING primary in place instead of re-running the wizard (which minted new rows). Shipped: new POST /api/assess/refresh (server-side: 401 signed-out · 409→/assess?new=1 if no primary, no 500 · else reScoreAssessment(admin,user.id) in place + returns {id}); new client refresh-button.tsx that POSTs then routes to /assessment/{id}; interstitial 'Refresh assessment' is now that button, wizard link relabeled 'Start a new assessment' (→/assess?new=1, unchanged). Signed-out wizard + OAuth claim funnel untouched; MV-16 newest-wins remains the fallback. Codex recon was partly hallucinated (no refresh route existed; reScoreAssessment is (db,userId) not (userId); page is app/(focused)/assess) — verified + corrected against real code. No goldens touched. Gate green: typecheck clean · lint clean · 1274 tests pass (was 1270). Commit d4fee1f. Founder-owed: accept→Done + deferred prod-row cleanup (~16 dup rows, prod write). Dossier: cards/MV-17-logged-in-reassess-routing.md._
- **MV-18** · P2 · [Real-DB claim-path integration smoke](cards/MV-18-claim-path-real-db-smoke.md) — _BUILT + all 3 GREEN, RED→GREEN proven against a real local Postgres 2026-06-20 (gate: npm test 1270 + test:integration 3 + skip-clean + typecheck/lint). tests/integration/claim-path.itest.ts (env-gated, out of default npm test via new vitest.integration.config.ts). claim.ts byte-identical (test-only). Local-stack finding: had to set auto_expose_new_tables=true in config.toml — prod's pre-2026-05-30 tables keep implicit service_role grants, fresh local db reset revokes them (latent prod risk; explicit-grant follow-up flagged, founder-gated). Closes the test gap that let MV-16 ship: tests/assessments/claim.test.ts mocks supabase-js, so it CANNOT enforce the partial-unique index assessments_primary_idx — the exact constraint whose swallowed {error} pinned the dashboard. Build a Vitest INTEGRATION smoke against a real local Postgres (all 14 migrations incl. 20260603170655 which defines the index): mint a non-Google user via auth.admin.createUser (answers the founder's 'smoke-test with a dev account' — claimAndBootstrapProfile is auth-agnostic, takes a userId; only exchangeCodeForSession is Google-specific + un-automatable here), seed two anonymous assessments, run claimAndBootstrapProfile twice, assert newest-wins single primary + a leads row (idempotent re-claim) + profile bootstrap. RED on the pre-MV-16 single-promote code, GREEN with demote-then-promote. Skips cleanly (env-gated) when the local stack is down; out of default npm test. Infra: local Supabase preferred (all 14 migrations present; Docker 29.4.0 installed but daemon DOWN + supabase CLI not installed = two one-time founder setup steps) >> paid dev branch >> prod writes (prohibited w/o founder approval). Test-only, no prod write, business logic untouched. Not started. Dossier: cards/MV-18-claim-path-real-db-smoke.md._
- **MV-19** · P2 · [Wire test:integration into CI](cards/MV-19-ci-integration-job.md) — _DONE pending review (committed to master). Answers MV-18's open Q2: the real-DB claim-path smoke (tests/integration/claim-path.itest.ts) now runs in CI. New second job `integration` in .github/workflows/ci.yml (parallel to validate, ubuntu-latest, timeout 20m, continue-on-error=advisory): npm ci -> npx supabase start (full stack + 14 migrations) -> export SUPABASE_TEST_URL + SUPABASE_TEST_SERVICE_ROLE_KEY to $GITHUB_ENV via a sourced `supabase status -o env` with defensive dual-key fallback -> run step ASSERTS the suite executed (env non-empty + REST health probe + grep verbose summary for >0 passed / 0 skipped, closing the skipIf false-green hole) -> supabase stop if:always. validate job unchanged. Codex GPT-5 adversarial review: ship-with-changes, 3 fixes folded (dual-key extraction, advisory continue-on-error, skip-assertion guard); Codex-confirmed $GITHUB_ENV reaches later steps + stock ubuntu-latest has Docker for supabase start. YAML parse-validated (2 jobs, integration=7 steps). No TS touched (typecheck/lint/test unaffected; the smoke itself was proven RED->GREEN in MV-18). FOUNDER-OWED: observe the first GitHub Actions run go green (agent can't — gh unauthed, no Actions visibility here), then decide required-vs-advisory via branch protection. Dossier: cards/MV-19-ci-integration-job.md._
- **MV-20** · P3 · [Category-E pending-findings disposition pass](cards/MV-20-category-e-disposition.md) — _CONCLUDED — disposition of record, ZERO ledger/data edits (the correct outcome). The '~93 pending need disposition' premise was inflated: E.jsonl is byte-identical to MV-06's 2026-06-19 snapshot, so the homeable E value was already harvested by the earlier sourcing slice + MV-13 (the 80 used). Per-finding recon (1 read-only subagent, file:line evidence) + I self-verified the two load-bearing claims. RESULT over the 45 ready (+ scanned 25 use-later): A) REJECT=0 — the 13 employer-anchor ready findings are durable institutional facts (NRB bank count [gov], 200-bed hospital, IOM degree levels, MoHP platform [gov], hotel descriptions), NOT ephemeral salary data; none match the E.158-160 rejected:ephemeral-jobad precedent (conservative default honoured). B) HOMEABLE-NOW=9 (E.044/086/094/112/113/119/120 RMIT IELTS + E.169 RMIT intake + E.050 Deakin IELTS) but ALL founder-DB-gated: verified the RMIT TS module has NO English field (grep false) + the Deakin uni entry sets no English value, so homing any of them adds a NEW IELTS value onto a live prod-bridged row -> cascades module->bridge-parity->seed->a NEW prod migration (the MV-13 gate). Spun into MV-21 (Backlog). C) KEEP-PENDING=36 correctly parked (unbuilt 485/course-career/checklist surfaces, uncatalogued Torrens/ECU/diplomas, no clean twin). needs-human-call=23 founder-owned untouched; +5 use-later vacancy listings (E.127/128/129/131/143) flagged into the founder-owned Nepal job-ad-evidence policy, not unilaterally rejected. No goldens/scorer/data file touched. Moved to In Review (zero-code disposition like MV-06's scoping). Dossier: cards/MV-20-category-e-disposition.md._
- **MV-21** · P3 · [Surface verified program English requirements (RMIT + Deakin)](cards/MV-21-program-english-enrichment.md) — _PRE-BUILT, PR open (founder-DB-gated at merge + prod-apply only). The MV-13 follow-on scoped out by MV-20: 9 Category-E findings (E.044/E.050/E.086/E.094/E.112/E.113/E.119/E.120/E.169) hold verified program-level IELTS for programs ALREADY in the live bridged catalogue. RMIT TS module gained overallMin/perBandMin (type+schema), 5 RMIT rows + the Deakin row populated with sourced IELTS, seed.ts updated (Deakin 6.5/6.0, RMIT B-Computer-Science 6.5/6.0, B-Nursing 7.0/7.0 + AHPRA+July-intake note, B-Education 7.5, B-Pharmacy-Hons 7.0/6.5, M-Social-Work 7.0/7.0), a new idempotent upsert migration (20260702000000) touches only min_english/min_english_band/notes/finding_refs on the 6 already-bridged ids, bridge-fact-parity + seed-migration-parity extended, 9 findings flipped used (FLIP_STATUS). Gate green: 1588 passing / 1 pre-existing freshness failure (PR #36 fixes, untouched). Goldens byte-identical (no scoring-goldens path touched). Founder-gated: review PR + apply_migration/get_advisors to prod + merge. Dossier: cards/MV-21-program-english-enrichment.md._
- **MV-22** · P2 · [Surface program notes (AHPRA) on the anonymous results MatchCard](cards/MV-22-results-matchcard-notes-parity.md) — _READY (agent-ownable, no gate). A 2026-06-22 evidence-based reconciliation (10-agent verification of the 2026-06-18 18-concern audit vs. current code, workflow wf_4b1a3438-b21) confirmed the audit is essentially shipped — Q5 English test-type, Q7 goal copy, Q8/Q9 conversion, Q10 dashboard, Q11 eligibility filter, Q12 scholarships, Q15 document no-op all SHIPPED in code with file:line proof. This is the smallest residual: a PARITY gap. Program notes (AHPRA registration for nursing, MBA work-experience prereqs) render on the signed-in dashboard ProgramCard's 'Good to know' block but NOT on the anonymous results MatchCard (components/results/university-matches.tsx ~28-62) — so trust-critical caveats are missing on the highest-traffic pre-signup /results surface, the conversion moment. Fix = clone the already-tested dashboard notes block onto the results MatchCard (after the reasons <ul>, before SourceLine) + a test asserting present-when-set/absent-when-null. Verify line numbers against real code first (MV-17 hallucination lesson); if notes isn't threaded to the anon match object, threading is in scope, but if absent at the data layer STOP+report. Presentational → goldens byte-identical. Dossier: cards/MV-22-results-matchcard-notes-parity.md._
- **MV-23** · P3 · [Plan vs Checklist mental-model copy](cards/MV-23-plan-checklist-mental-model-copy.md) — _DONE — founder-accepted 2026-06-23 (part 1 copy); part (2) spun to MV-27. part (1) SHIPPED 2026-06-22 (committed to master). 2026-06-18 audit Q14: the per-program checklist and the plan generator both carry the same AU visa-prep steps (NOC, biometrics, police cert, GS, translations, agent MARN) with no stated mental model, so users see identical actions on two screens (the completion ACTION is already deduped by the existing plan-links design — worst failure mode mitigated). Part (1) AGENT-OWNABLE copy DONE: a mental-model framing line added to the per-program checklist view (ChecklistView header — 'this checklist is your reference for everything this program requires … work through and tick off these steps in your plan, your single action queue') and to the plan (PlanList, shared intro rendering in BOTH empty and populated states — 'this is your action queue … each program’s checklist is the full requirement reference behind it'); the two cross-reference each other. Placement: the per-program ChecklistView + PlanList (the two tested presentational surfaces where the duplicated steps render); the checklist landing + plan page header left untouched (surgical). TDD +3 (RED→GREEN, incl. an empty-plan framing case), gate green (typecheck/lint, full suite 1279, was 1276), goldens byte-identical, lib/checklist/generator.ts NOT modified. Part (2) FOUNDER CALL remains (spun to MV-27): strip the mirrored visa-prep rows from lib/checklist/generator.ts so they live only in the plan, OR keep the plan-links completion-mirror — not silently decided. Evidence: reconciliation wf_4b1a3438-b21. Dossier: cards/MV-23-plan-checklist-mental-model-copy.md._
- **MV-24** · P3 · [DHA evidentiary-tool harvest (CRICOS directory + Nepal evidence map)](cards/MV-24-dha-evidentiary-harvest.md) — _DONE — founder-accepted 2026-06-23 (agent-ownable half). Harvested the DHA Document Checklist Tool's anonymous SharePoint endpoints (POST Termstore.aspx/GetTermsByProperty + ESB.aspx/GetStudentDocumentChecklistType; no cookie/CSRF/digest) politely (conc 2 + 403 backoff, resumable). Shipped 2 NEW sourced DISPLAY-pattern datasets (NOT the finding-traced hand au-cricos-codes.ts, which is untouched): au-cricos-directory.ts (complete 1,669-provider CRICOS directory; Babel 3522E->03522E normalised, verified bic.wa.edu.au) + au-nepal-evidence-levels.ts (1,626 unique codes -> Regular/Streamlined/Undetermined for Nepal passport, study-type 01; 1,572 Regular / 54 Streamlined = real per-provider variation). Wired: Zod schemas; nepal-evidence-lookup; cricos-lookup now returns CricosResolution + resolves Melbourne (00116K) + ANU (00120C) from the directory (were null); ProgramCard shows '{level} evidence . Nepal' beside the CRICOS code; re-runnable scripts/harvest-dha-evidentiary.mjs (.harvest-cache git-ignored). TDD RED->GREEN; gate green (typecheck/lint, suite 1290 was 1279, goldens byte-identical). Independent live re-probe of committed data: 21/21 codes 0 mismatches, 0 codes missing an evidence entry. FOUNDER-OWNED (not blockers): accept->Done; DB-catalogue surfacing (founder-DB-gated like MV-13/21); optional anon-results parity (evidence on MatchCard/GS panel, own slice like MV-22); periodic re-harvest cadence; other study-types. Dossier: cards/MV-24-dha-evidentiary-harvest.md._
- **MV-25** · P3 · [Nepal evidence level on the anonymous results MatchCard](cards/MV-25-results-evidence-parity.md) — _DONE — founder-accepted 2026-06-23 (agent-ownable). The MV-24 anon-results parity follow-on, taken as its own slice (the MV-22 pattern). MV-24 surfaced '{level} evidence . Nepal' on the signed-in dashboard ProgramCard but the anonymous results MatchCard (components/results/university-matches.tsx — the pre-signup conversion surface) omitted it. Design constraint solved: the anon MatchCard + results.tsx are CLIENT components, so importing the resolvers (cricos-lookup -> au-cricos-directory 1,669 rows + nepal-evidence-lookup -> au-nepal-evidence-levels 1,626 rows) would bundle ~3,300 records (~100KB) into the client bundle on the highest-traffic page. Resolution: resolved server-side in assemble.ts (both prod callers — /api/assess route + owned [id] page — are server contexts; anon renders from the server-built payload JSON), level attached as a precomputed string on MatchResult (evidence?: {level, source}; type-only NepalEvidenceLevel import = zero client cost). New server-only helper lib/matches/evidence.ts attachNepalEvidence(matches), present-when-known. Client MatchCard renders the mono/faint line linking to the DHA tool. TDD RED->GREEN +7 (helper 3 / component 2 / assemble seam 2); gate green (typecheck/lint, full suite 1297 was 1290), goldens byte-identical (snapshot only runAssessment output, not matches), au-cricos-codes.ts untouched. Grep-verified: evidence.ts is server-only (no use-client importer) so the datasets never reach the client bundle. FOUNDER-OWNED (not blockers): accept->Done; owned [id] page shows evidence on fresh/re-scored payloads (pre-change stored payloads after a re-score — graceful degradation, house pattern); GS-panel corridor-level treatment is its own surface. Dossier: cards/MV-25-results-evidence-parity.md._
- **MV-26** · P3 · [Freshness guard for the harvested DHA DISPLAY datasets](cards/MV-26-harvest-freshness-guard.md) — _DONE — founder-accepted 2026-06-23 (shipped with the MV-23/24/25 batch). The MV-24 harvest (au-cricos-directory.ts 1,669 providers + au-nepal-evidence-levels.ts 1,626 codes) is DISPLAY-pattern data: one module-level lastVerified stamp, deliberately outside the findings ledger / DATA_MODULES registry, so the existing freshness guard (which walks DATA_MODULES for per-record provenance.reverifyBy) never saw it — shipped with lastVerified 2026-06-22 and NO reverifyBy, so nothing tripped when it aged (silent-rot gap). Fix mirrors the MV-04 stale-fact pattern: add a module-level reverifyBy to each dataset (AU_CRICOS_DIRECTORY_REVERIFY_BY, AU_NEPAL_EVIDENCE_REVERIFY_BY = 2026-12-22), documented as a periodic re-harvest cadence (deliberate TTL, not a fact-expiry date — bulk authority data drifts continuously; tunable founder call), and extend tests/data/freshness.test.ts with a 'harvested DISPLAY datasets' block reusing the existing dueForReverify logic (red on the cadence date = the re-harvest reminder) plus asserting each stamp is a present ISO date later than its own lastVerified (a deleted stamp fails loudly instead of undefined slipping past the <= filter). TDD RED->GREEN +2; gate green (typecheck/lint, full suite 1299 was 1297), goldens untouched (no scorer path), au-cricos-codes.ts untouched. Commit 19ef5f1. Founder-owned: cadence length (6mo default); optional scheduled auto-re-harvest (separate slice). Dossier: cards/MV-26-harvest-freshness-guard.md._
- **MV-28** · P0 · [Recoverable anonymous results (P0)](cards/MV-28-anon-results-recovery.md) — _STATUS 2026-06-23 — BOTH HALVES BUILT. Half (a) sessionStorage recovery DONE on master (51fd70e): anon wizard answers (myvisa.wizard.v1) + results (myvisa.results.v1) survive refresh/back/tab; anon-only; cleared on /assess?new=1. Half (b) anon-recoverable /assessment/[id] DONE on branch mv-28b-anon-read (39c716c), gate green (suite 1321 +6), Codex-reviewed — kept OFF master pending the founder POLICY nod. DESIGN PIVOT — NO migration (Option C). The original 'grant anon SELECT + RLS policy' plan was REJECTED as insecure: the public anon key (shipped in client JS) + a row-content RLS predicate (owner is null and expires_at>now) lets anyone GET /rest/v1/assessments?select=* and enumerate every unclaimed assessment's PII — RLS filters by row content, not by whether the caller supplied the id (Codex confirmed). Instead the anon read uses the server-only ADMIN (service-role) client with the predicate IN THE QUERY (.eq(id).is(owner,null).gt(expires_at,now)) + an app-side guard/canary log as defense in depth; anon keeps ZERO table grant → no PostgREST enumeration surface; matches CLAUDE.md 'business logic in Next.js, Supabase is dumb storage'. Files (branch): lib/assessments/repo.ts getRecoverableAssessment(db,id,nowIso); app/(focused)/assessment/[id]/page.tsx drops the signed-out redirect → admin read → Results mode='anonymous' assessmentId={id} so recovery still offers the claim/sign-in path; tests/assessments/repo-recoverable.test.ts (+5, claimed/expired security boundary) + tests/app/assessment-page.test.tsx (anon-recovery/404). REMAINING GATE (policy + merge ONLY, not a migration): founder confirms it is acceptable that a UUID-bearer can read an UNCLAIMED + NON-EXPIRED assessment's profile (fulfils the existing 'expires in 3 days' promise; the id is only ever returned to the creator) → then merge branch→master to fully close the audit P0. ORIGINAL P0 (docs/audits/2026-06-23-real-user-audit.md): anon results unreachable after refresh/back/tab + the [id] redirect blocked the DB-persisted assessment despite the 3-day promise. COUPLED with MV-31 (#4, DONE 966a978). Only the founder closes to Done._
- **MV-29** · P1 · Strip fabricated conversion-gate teasers — _DONE pending founder review — integrated to master b400a14 (gate green: typecheck/lint clean, full suite 1308). Audit fix #2: the anon results conversion gate (components/results/gated-teasers.tsx) teased a fabricated '23-step guide' + '14 documents' resolving to an unwired 'we'll email you' — the consultancy bait-and-switch the product opposes; the test LOCKED the fabrications in. Now teases only the two REAL built deliverables (the personalised document checklist + the action plan), qualitatively, no invented counts/email promise; test rewritten to assert the fabricated strings are ABSENT (locked+unlocked). No snapshot regen (assertion-based). TDD RED→GREEN. Built in worktree (agent), cherry-picked. Founder accept→Done._
- **MV-30** · P1 · Gate /matches for empty profiles — _DONE pending founder review — integrated to master cdc6ce2 (gate green: typecheck/lint clean, full suite 1308). Audit fix #3: /matches always ran sectionsToMatchInputs({})→computeMatches with empty inputs (userGrade defaults 0 → fabricated 'Reach · Grade short by 60%'); the dashboard never hits this because pickPrompt short-circuits to profile-incomplete. Fix: app/(app)/matches/page.tsx now gates an empty profile (Object.keys(sections).length===0) with the dashboard's PromptCard prompt={kind:'profile-incomplete'} and skips the matcher entirely. TDD RED→GREEN (tests/app/matches-page.test.tsx; two pre-existing null-profile tests switched to FILLED_PROFILE to keep exercising the tab path). Built in worktree (agent), cherry-picked. Founder accept→Done._
- **MV-31** · P1 · Fix persist-failure path + refresh-destroys-input — _DONE pending founder review — integrated to master 966a978 (gate green: typecheck/lint clean, full suite 1309, +1). Audit fix #4: when the anon assessment failed to persist, the screen was a dead end reading 'Something went wrong… please refresh' — and refreshing wipes all 9 useState-only wizard answers + the result. Fix: extracted save(profile) out of handleComplete; the error screen now shows honest copy ('We couldn't save your assessment just now. Your answers are still here — try again.') + a working 'Try again' button that re-POSTs IN PLACE, preserving all wizard/results state (no reload, no re-answering). SCOPE CORRECTION (agent caught, I verified): refresh-button.tsx is MV-17's SIGNED-IN re-score button (used only by assess-interstitial.tsx), NOT the anon path — left untouched; the destructive 'refresh' instruction was copy in assess-flow.tsx. Files changed = components/assess/assess-flow.tsx + tests/assess/assess-flow-id.test.tsx ONLY (use-wizard-state.ts not needed). TDD RED→GREEN (fail-then-succeed /api/assess mock proves Try again re-saves without re-answering + asserts no 'refresh' text). save()/preserved-state error screen left clean for MV-28 sessionStorage to layer on. Built in worktree (agent), cherry-picked. Founder accept→Done._
- **MV-32** · P1 · Honest verdict band + accuracy meter (no raw %) — _DONE pending founder review — integrated to master f744eb2 (gate green: typecheck/lint clean, full suite 1308). Audit fix #5 (UI-only): (a) AccuracyMeter (components/results/accuracy-meter.tsx) showed raw '25% · Basic' → now a qualitative '{level} confidence' label, no user-facing number (progress-bar width CSS retained). (b) verdict-card.tsx lowest reach band takes an optional weighted prop and swaps to blunt severity-scaled copy when weighted<30 ('well out of reach right now — the gaps are substantial') instead of the soft 'a few key areas'; number used only to pick copy, never rendered; legacy payloads (no prop) keep the standard line. results.tsx passes payload.result.weighted through. NO lib/results/* touched (raw 25/'Basic' originates in lib/results/accuracy.ts + goldens lock it; weighted already on payload, no escalation). TDD RED→GREEN (+7). Built in worktree (agent), cherry-picked. Founder accept→Done._
- **MV-33** · P1 · [Honest "Your applications" funnel (subtitle + self-report control)](cards/MV-33-outcome-funnel-honesty.md) — _IN REVIEW — Part A SHIPPED 2026-06-25 (founder steered 'ship MV-33 part A + MV-34'); Part B carved out to MV-39. Audit #6 (wf_5fb5dfa7-009): the outcome funnel is real (banded verdicts, no raw %) but the loop was half-wired and the subtitle over-promised. Part A reconciled the subtitle in components/outcomes/outcome-funnel.tsx — 'What you told us happened … Self-reported until verified' → 'The programs you've applied to, shown against the verdict we gave you. We'll add your offer and visa results as you report them' — so it no longer claims a comparison (rows are stuck at Applied) or a verification ladder that doesn't exist; 'until verified' removed. TDD RED→GREEN, new tests/components/outcomes/outcome-funnel.test.tsx (+2). Gate green: typecheck/lint, full suite 1326 (was 1323), goldens N/A (no scorer path), banded verdicts only/no raw %. Part B (self-report control POSTing /api/outcomes/event so stages advance past Applied — backend already built+tested) → MV-39. Founder closes to Done. Dossier: cards/MV-33-outcome-funnel-honesty.md._
- **MV-34** · P1 · [Disclose the "Applied" prediction-freeze + explain the triage states](cards/MV-34-applied-freeze-disclosure.md) — _IN REVIEW — SHIPPED 2026-06-25. Audit #2 (wf_5fb5dfa7-009): choosing 'Applied' on the not-saved/shortlisted/applied control is the MV-08 capture trigger — it freezes an immutable prediction-of-record (verdict+ruleVersion+scoreSnapshot via /api/shortlist→captureApplication→freezePredictionForProgram) — but the freeze was SILENT (the 3 pills carried no helper text; hidden data capture on a trust-first product, P1). Fix: a one-line disclosure under the pills (forewarning, before the click) — 'Marking Applied locks in this verdict so we can compare it against your real outcome' (banded verdict only, no raw %). Contained in components/matches/shortlist-button.tsx (flex-col items-end wrapper; program-card.tsx untouched; inner group keeps aria-label so button queries unaffected). TDD RED→GREEN, shortlist-button.test.tsx (+1). Gate green: typecheck/lint, full suite 1326 (was 1323), goldens N/A. Deferred (noted): fuller Shortlisted-vs-Applied state explainer; revert-orphans-prediction P2 (intended immutability vs implied clean undo — MV-08 follow-up); 'withdrawn' dead enum branch P3. Founder closes to Done. Dossier: cards/MV-34-applied-freeze-disclosure.md._
- **MV-35** · P1 · [Fix the dashboard "Universities" tile (counts programs, mislabeled)](cards/MV-35-saved-programs-tile.md) — _SHIPPED 2026-06-26 → PR. Audit #5 (wf_5fb5dfa7-009): the dashboard 'Universities' tile rendered shortlist.length (dashboard/page.tsx:88) = count of shortlisted PROGRAMS (one row per program_id), so 3 degrees at one university read 'Universities = 3' and a new user's bare '0' could read as '0 universities exist'. No fabrication/raw % — a faithful count, only mislabeled. Fix (chosen = honest relabel, value already matches): tile → 'Saved programs', prop universities→savedPrograms (the internal name carried the same mislabel); the student shortlists programs not universities, so 'saved programs' is the truthful AND more useful number, and it keeps the change surgical (no university-id plumbing). 'Saved programs: 0' now reads 'none saved yet'. The 'withdrawn still counts' P2 is NOT actioned — the audit itself notes 'withdrawn' is unreachable in the live UI. TDD: stats-row.test.tsx updated (asserts 'Saved programs', NOT 'Universities'). Gate green: typecheck clean, lint 0 errors, suite 1395. Goldens N/A._
- **MV-36** · P2 · [Honest CRICOS link (provider-code framing; subject deep-link infeasible)](cards/MV-36-cricos-link-honesty.md) — _SHIPPED 2026-06-26 → PR. Founder #1 ('link the actual CRICOS page of their subject?') VERIFIED INFEASIBLE (recorded so nobody re-investigates): we hold only PROVIDER/institution codes (Sydney 00026A = whole uni; per-course codes never sourced) AND cricos.education.gov.au is WebForms with no ?code= deep-link (homepage is the only shareable URL — why MV-07 hard-coded it). So this slice makes the EXISTING link honest, not a fake deep-link: program-card.tsx CRICOS line relabelled 'CRICOS {code} ↗' → 'Provider CRICOS {code} · search the register ↗' (names it the provider code, not the course code a student needs on visa form 157A — false-confidence fix; and frames the link as a register SEARCH, not a direct entry). Tooltip now spells out the provider-vs-course-code distinction + 'register has no direct link'. cricos.source/lookup unchanged (no data work). Pinned program-card.test.tsx updated to assert the new label + course-code tooltip. Extends MV-07. OUT OF SCOPE (infeasible/own slice): per-course harvest, deep-link, anon-MatchCard parity. TDD; gate green: typecheck clean, lint 0 errors, suite 1395, goldens byte-identical (presentational)._
- **MV-37** · P2 · [Rework "My plan" into a guided sequential timeline](cards/MV-37-plan-guided-timeline.md) — _IN REVIEW — slice 1 SHIPPED 2026-06-25 (founder sign-off 'do the recommended'; TDD RED→GREEN, suite 1334, goldens byte-identical): phase-grouped A–E journey via lib/plan/phases.ts (PLAN_PHASES + founder-reviewable phaseOf map), ordering now phase→impact→id in lib/plan/select.ts (groupByPhase; newest-created-first artifact removed), guided-timeline copy replacing action-queue in components/plan/plan-list.tsx. selectNextStep shape unchanged so dashboard holds; VISA_PREP_KINDS kept (checklist-link contract). Slice 2 deferred (Codex BLOCKERs needing new state): per-application multi-track + nominated primary, richer blocked/waiting/premature exclusion, offer/CoE I-already-have entry states, full locked-phase scaffold, dynamic capacity label (folds into MV-38). ORIGINAL: BACKLOG — founder DECISION 2026-06-24: 'we need this to be a timeline for students on what to do next, as we are guiding them.' Deliberate reversal of MV-23 part-1 framing (which shipped 'this is your action queue — not a strict timeline'). MV-23 stays Done; MV-37 is the forward vehicle. Current state: 'Your next steps' is impact-ranked (newest-created-first within band — an implementation artifact, lib/plan/select.ts:17-20); only 'Visa preparation' is genuinely sequential (visaPrepOrder over the hand-curated lib/plan/phases.ts). DESIGN-FIRST (brainstorm before build): settle the phase/journey model (e.g. profile/grades → shortlist → English test → documents → proof of funds → apply → CoE → visa lodge → decision), strict-linear vs phase-grouped-with-priority, where no-sequence items sit, how completion advances the timeline, and replace 'action queue' copy with timeline framing. Likely also resolves MV-27 (mirrored rows) and reframes MV-38 (proof-of-funds as 'the next step'). Touch points: components/plan/plan-list.tsx, lib/plan/select.ts (ordering), lib/plan/phases.ts (extend to all phases), lib/plan/generator.ts (kinds→phases). Goldens likely unaffected (ordering, not scorer) — confirm. Dossier: cards/MV-37-plan-guided-timeline.md._
- **MV-39** · P2 · [Outcome self-report control (advance the funnel past Applied)](cards/MV-39-outcome-self-report.md) — _IN REVIEW — SHIPPED 2026-06-25 on branch mv-39-outcome-self-report (first slice under the new branch+PR operating model; founder merges to master to close → Done). Wires the missing UI over the already-built/tested backend so the funnel advances past 'Applied'. selfReportNextEvents(prior) (lib/outcomes/state-machine.ts) derives the legal next milestones from the same canRecordEvent machine the API enforces (a shown button never 409s; excludes silent applied/withdrawn); buildOutcomeFunnel attaches nextEvents to each row (no new fetch); OutcomeSelfReport client control (components/outcomes/outcome-self-report.tsx) renders one student-voice button per legal step, POSTs self_reported event to /api/outcomes/event, router.refresh() on success / inline error on fail / null at terminal; wired into OutcomeRow. Banded verdicts only, no raw %; verification ladder + reason codes OUT (legal-gated MV-08). TDD RED→GREEN +16 (state-machine 8, funnel 1, component 5, funnel-render 2); gate green: typecheck clean, lint 0 errors, full suite 1361, goldens N/A (no scorer path). Pushed branch = master + this slice only (8 files; design WIP not included → clean Vercel preview). Founder-owned: merge PR→Done; copy sign-off on 8 EVENT_LABEL strings; optional program-card mirror. Dossier: cards/MV-39-outcome-self-report.md. ORIGINAL: READY (agent-ownable) — MV-33 Part B, carved out 2026-06-25 (the MV-23→MV-27 split pattern). The outcome funnel can only ever show 'Applied' because the sole event the live app writes is the root 'applied' (lib/outcomes/on-apply.ts); app/api/outcomes/event/route.ts exists + is tested but has ZERO client callers. Add a self-report control (on each funnel row or the program card) that POSTs /api/outcomes/event with offer_received / offer_unsuccessful / visa_lodged / visa_granted / visa_refused so stages advance past Applied. The entire backend is already built + tested — route, state machine (lib/outcomes/state-machine.ts), repo, funnel fold (lib/outcomes/funnel.ts deriveFunnelStage); only the UI affordance is missing. Banded verdicts only, no raw %. OUT OF SCOPE: the verification ladder (classifyEvidence + forward-to-address/DKIM + admin VEVO) = separate legal-gated MV-08 slice. TDD RED→GREEN. Spec in cards/MV-33-outcome-funnel-honesty.md (Part B); evidence audit wf_5fb5dfa7-009._
- **MV-12** · P2 · [Fix CGPA entry in profile academic editor](cards/MV-12-cgpa-editor-normalize.md) — _DONE pending review (committed to master). The signed-in academic editor stored a raw CGPA (e.g. 3.5 / cgpa-4) as gradePercent, so the matches adapter read it as 3.5% and collapsed every program to 'reach' while the verdict path normalized to 87.5% — an internal contradiction. Fix (option a): normalize at the server-side save boundary via a Zod transform on AcademicPatch (new lib/profiles/normalize-academic.ts), so gradePercent is always a true percentage and gradeSystem is never persisted (mirrors profileSectionsFromAssessment; explicit gradeSystem=undefined clears stale values through the merge — self-heals pre-fix rows). F16-safe (no client imports the validation/grade-normalize). No scorer/golden change. Gate green: typecheck/lint/1140 tests (+12). Editor relabel 'Grade percent'→'Grade' deferred as founder-reviewed copy._
- **MV-A1** · [Push feat/context-budget](cards/MV-A1-push-branch.md) — _Pushed to origin/feat/context-budget (upstream set). PR link ready; open via GitHub URL (gh not authed)._
- **MV-D0** · [Phase 0 + Phase 1 (9 slices)](../audits/2026-06-18-EXECUTION-CHECKPOINT.md) — _GPA normalize · conversion/auth · doc re-score removed · matches filter · dashboard cleanup · English test-type · results IA · scholarships · program notes. 1075 tests green._
- **MV-40** · P1 · Design Phase A — a11y + honesty polish — _Per founder: COMPLETE on branch design/phase-a-polish (a49d8a3); NOT merged to production, so set In Review not Done (same state MV-39 was pre-merge; its gate not re-verified by me). The cheap a11y + honesty wins from the 2026-06-25 design-division audit: base :focus-visible ring (#1), prefers-reduced-motion block (#5), pruned dead footer links (#3), 'Soon' badge on AI-guide + softened 'feed' (#7), AccuracyMeter→teal banded (#8), honest /auth email button (#6), removed 3s fake-loading recap (#18), fixed step-counter disagreement (#22), Genuine-Student questions open+numbered (#17), possible-amber contrast token (#10), '~2 min' honesty + above-fold authority line (#20,#29), imageless-body rule + imagery-policy (#4). Phases B+C = MV-41..50. Founder: merge like MV-39 when ready. Spec: docs/audits/2026-06-25-design-division-polish-audit.md._
- **MV-41** · P0 · [Conversion seam: kill silent id:null dead-CTA](cards/MV-41-dead-cta.md) — _SHIPPED 2026-06-26 → PR. VERIFY-FIRST done: the SERVER residual the audit flagged is already closed — app/api/assess/route.ts logs the anon persist miss and still returns the payload (results render), and surfaces 500 only for signed-in users (route.ts:119-146, shipped via MV-28/31/32). The live residual was purely CLIENT: ConversionPaths + ConversionPrompt rendered a SILENTLY disabled 'Continue with Google' button under 'Keep your assessment / expires in 3 days' copy when assessmentId is null — a dead CTA plus a FALSE expiry promise (nothing was saved). Fix: on id:null both components now drop the dead button + false expiry and show honest 'We couldn't save this assessment …' copy with a real recovery — a 'Run it again' link to /assess?new=1. NO email-to-self/copy-link path (the old card summary's idea): that retrieval system does not exist, so implying it would fabricate a feature (trust-first) — re-run is the one truthful recovery. TDD RED→GREEN: conversion-paths.test.tsx (+2, replaced the obsolete 'disables button' test), new-state in conversion-prompt.test.tsx (+2), results.test.tsx core-path render now passes a real id. Gate green: typecheck clean, lint 0 errors, suite 1397 (was 1396). Founder copy review: the 'couldn't save / Run it again' wording. Spec: docs/audits/2026-06-25-design-division-polish-audit.md #2._
- **MV-63** · P1 · [AI guide (Phase 6): grounded source-citing chat that explains, never decides](cards/MV-63-ai-guide.md) — _DONE 2026-06-26 (PR #10 MERGED → master e6d7ab1, live on Vercel) — founder-directed ('build the AI guide', the real consultancy-substitute). Vision: 'explains reasoning, NOT decides — rule-based first, AI explains'; always cites sources; never writes the application. Phase 6 was never spec'd → cards/MV-63-ai-guide.md IS the design/build plan. Founder decisions: live grounded CHAT; provider = DeepSeek (cheap, OpenAI-compatible), key in .env.local (gitignored) as DEEPSEEK_API_KEY+DEEPSEEK_BASE_URL. FOUNDER TODO: (1) rotate key (shared in plaintext) + (2) add DEEPSEEK_API_KEY to Vercel env for prod — until (2) the route returns its calm 503 in prod. Architecture (server-side only, key never client-exposed): app/(app)/guide/page.tsx → components/guide/guide-chat.tsx → /api/guide/chat (auth+Zod+Upstash) → lib/guide/{context,system-prompt,deepseek}. ALL 3 SLICES SHIPPED on branch mv-63-ai-guide: (1) deepseek.ts client (throws on missing-key/non-ok/empty — never silent/fabricated, temp 0.2) + system-prompt.ts GUIDE_SYSTEM_PROMPT guardrails (explain-not-decide, ground-only, cite-sources, never-write-application/SOP, Nepal→AU only, banded-not-%); (2) context.ts buildGuideContext (banded verdict + factor reasons/sources + top-5 matches/evidence + plan + sourced cost; NO raw scores leaked, pinned by test; null→no-fabrication) + /api/guide/chat route (401/422/429, 503-not-fabricated on provider fail); (3) guide-chat.tsx client chat (visible role=alert error per MV-62) replacing the coming-soon stub + page empty-state nudge. TDD 27 guide tests; obsolete app-stubs.test deleted; gate green typecheck/lint, suite 1437. MERGED to master via PR #10 (e6d7ab1). KEY STATUS: founder confirmed the original DeepSeek key is dead (exposure moot) — the graceful 503 keeps prod safe; the guide will only return real answers once a VALID DEEPSEEK_API_KEY is set in the Vercel env (founder action, no code change needed)._
- **MV-62** · P1 · [Resilience: error + loading boundaries + no silently-swallowed taps](cards/MV-62-resilience-boundaries.md) — _SHIPPED 2026-06-26 → PR. Tier-1 journey-reliability. The real user is on a flaky connection: a thrown live Supabase read (dashboard six-way Promise.all at dashboard/page.tsx:47, matches four-way) crashed signed-in pages to a blank frame, and a dropped plan-action tap did nothing with no feedback — both read as 'broken app' = bounce to a consultancy. Scoped DOWN from the ~30-file resilience proposal to the highest-leverage set: ONE app/(app)/error.tsx (covers ALL signed-in routes — they share the (app) group: calm branded 'couldn't load this page, your data is safe' + working reset), ONE app/(app)/loading.tsx (flat-paper skeleton, aria-busy, no spinner), app/global-error.tsx (root-layout fallback, self-contained <html>/<body>, inline brand hex since CSS/fonts may be what failed), and components/plan/plan-item-card.tsx (the silent .catch(()=>null) now surfaces a role=alert 'We couldn't save that just now — try again' and keeps prior state; mirrors outcome-self-report.tsx). Out of scope: bespoke per-route skeletons, anon/marketing trees, toast infra, Sentry wiring (follow-up). TDD RED→GREEN: plan-item-card.test.tsx (+3: network-fail/non-ok/retry-clears), new tests/app/error-boundaries.test.tsx (3). Gate green: typecheck clean, lint 0 errors, suite 1401. Founder copy review on the two messages._
- **MV-42** · P1 · Shared design primitives (VerdictPill + Card + VERDICT_LABELS) — _Phase B (audit #9/#12/#13/#26, P1). Extract one VerdictPill (hand-rolled 4x) + one Card/Panel shell (copy-pasted ~10x); bake role->radius (8 input / 12 card / 16 panel); centralize VERDICT_LABELS (labels worded 5 ways across surfaces, group headers append '(N)' at call site); give bespoke pill-buttons a disabled:opacity-50/pointer-events-none + standard loading-copy contract; one neutral teal meter colour. Stops the radius/label/state drift. Note: #8 AccuracyMeter recolor likely already shipped via Phase A + MV-32 — verify before touching. Spec: docs/audits/2026-06-25-design-division-polish-audit.md #9,#12,#13,#26. SLICE 1 SHIPPED 2026-06-27 (PR pending): centralised VERDICT_LABELS — one lib/scoring/verdict-labels.ts (label singular + groupLabel plural) now feeds both the verdict-card pill and the verdict-group header; the divergent VERDICT_META.label / HEADLINE dicts are gone; output byte-identical; TDD +4 (suite 1441), typecheck clean, lint 0 errors. Inventory corrected the audit's premises: VerdictPill is hand-rolled in 1 place not 4 (the others were worktree copies) so extraction is skipped as premature abstraction; AccuracyMeter is already bg-primary teal (Phase A) so it's a no-op. SLICE 2 (deferred, independent of master): Card/Panel shell extraction (~12 copies, biggest/riskiest) + bespoke pill-button disabled/loading contract (refresh-button, document-viewer-modal, guide-chat). DEFERRED behind PR #11: the eyebrow<->counter consolidation edits the same 9 wizard step files MV-47 renumbers, so it must land after MV-47 merges to master to avoid a head-on conflict._
- **MV-43** · P1 · Wire dark mode (activate the dead token set) — _Phase B (audit #14, P1). DONE 2026-06-28 (PR #27, merged to master). Activated the dead dark-mode token set: pre-hydration theme-init (lib/theme/theme-init.ts) auto-follows OS prefers-color-scheme; kept the background-color (not shorthand) rule per the documented custom-property re-resolution bug. TDD (theme-init tests). Spec: docs/audits/2026-06-25-design-division-polish-audit.md #14._
- **MV-44** · P1 · Accessible matches tablist (WCAG 1.4.1) — _Phase B (audit #11, P1). DONE 2026-06-28 (PR #26, merged to master). Fake colour-only tabs -> real WAI-ARIA tablist (role=tab/tablist/tabpanel; name/role/value), fixing WCAG 1.4.1 (use of colour) + 4.1.2; components/matches/matches-tabs.tsx. TDD: 8 component tests + matches-page integration moved to role=tab. Audit #21 (accordion fold) carved out -> MV-75. Spec: docs/audits/2026-06-25-design-division-polish-audit.md #11._
- **MV-45** · P1 · Progression visuals UMBRELLA (funnel rail + intake timeline + plan<->checklist chips) — _DECOMPOSED 2026-06-28 (was too big for one slice -- 4-5 distinct visuals on different surfaces). Tracks the REMAINING work; pick one sub-slice at a time. Phase B (audit #15/#16/#28, P1). DONE/carved out: #28 chip-on-ChecklistItem already shipped (MV-23 thread); #28 stage-tag-on-PlanItemCard -> MV-71 (in progress). REMAINING here: (#15) thin stepped rail per outcome-funnel row (Applied->Offer->Visa lodged->Granted) -- COORDINATE w/ the merged MV-39 self-report control on the same row; (#16) tick-timeline w/ 'now' marker on results/intake-timing.tsx; PLUS the MV-68-audit reframing: a global cross-stage 'where am I' journey rail in app/(app)/layout.tsx (wizard->results->account->documents->apply) -- biggest, warrants its own brainstorm. Flat dots, mono labels -- no new vocabulary. Spec: docs/audits/2026-06-25-design-division-polish-audit.md #15,#16,#28 + 2026-06-27-mv68-ground-truth-audit.md #3._
- **MV-46** · P2 · Ease the three inert completion beats (audit #24) — _Phase B (audit #24, P1-P2). DONE 2026-06-28 (PR #28, merged to master). Eased the three inert completion beats inside calm-authority -- all ride the ease-calm token + the global prefers-reduced-motion block (collapses to a near-instant step): completeness-ring arc now grows (transition-[stroke-dashoffset]); plan card open->closed settles (transition duration-300 ease-calm); checklist tick replaced the literal '✓ ' string with the styled aria-hidden option-card checkmark. TDD +3 (suite 247 files / 1547). Audit #25 (verdict two-beat reveal + Applied-commit confirm, trust-sensitive) carved out -> MV-76. Spec: docs/audits/2026-06-25-design-division-polish-audit.md #24._
- **MV-47** · P1 · Move the corridor question to Step 1 — _Phase B (audit #23, P1). The destination/corridor question is buried at Step 7, so a Canada-bound student answers six screens before learning the corridor is unsupported (reads as bait-and-switch). Move destination to/near Step 1 (use-wizard-state.ts order) so coverage is known before effort. Spec: docs/audits/2026-06-25-design-division-polish-audit.md #23. SHIPPED 2026-06-27 (branch mv-47-corridor-step1, TDD): destination reordered to position 2 in WIZARD_STEPS (right after homeCountry — corridor known before the six effort screens); 6 static step-eyebrows renumbered (dest 7→2, edu→3, field→4, gradYear→5, gap→6, english→7); 5 order-encoding tests updated (use-wizard-state, use-wizard-state-persist, wizard, wizard-to-results integration) + 1 new explicit corridor-position test. Side benefit: nursing-ahpra/cs-competitive field-of-study callouts now fire (they need destination=australia, previously unset until step 7). Gate green: typecheck/lint clean, suite 1438. RESIDUAL (own card, not bundled — belongs in MV-42 design primitives): per-step 'Step N' eyebrows are static, so for the no-gap student english/budget/goal still mismatch the live header counter by 1 (pre-existing, not worsened)._
- **MV-65** · P1 · Anonymous persist-miss: recover in place, not a dead 'Run it again' — _Consultancy-replacement / journey-reliability (primary C; Codex gpt-5.5 + Claude agreed). On an anonymous persist miss (POST /api/assess returns 200 with id:null — the DB insert failed), BOTH results CTAs (ConversionPrompt near the verdict + the ConversionPaths card at the bottom) linked to /assess?new=1, which wiped the wizard + results sessionStorage and dumped the student back to a BLANK 9-step wizard — and if the DB was still down it looped forever (a guaranteed bounce to a consultancy). But the full profile + computed payload are already in memory; only the server save failed. FIX (TDD): thread an in-place onRetrySave (+ retryingSave) from AssessFlow.save() through Results into both CTAs; the dead /assess?new=1 link becomes a 'Try saving again' button that re-POSTs the same answers (no wizard reset, no data loss), and on success the prompt flips to the real 'Continue with Google' conversion. Honest copy, no false 3-day-expiry promise. Other /assess?new=1 links (interstitial, unsupported-corridor notice, marketing) left untouched — those are legit fresh starts. Files: components/results/{conversion-prompt,conversion-paths,results}.tsx + components/assess/assess-flow.tsx. TDD: rewrote the 2 persist-miss component tests (link -> button + onRetrySave + disabled-while-saving) + a new AssessFlow integration test (persist miss recovers to a real id, fetch called twice, wizard never re-run). Gate green: typecheck clean, lint 0 errors, suite 1441 / 241 files. Branch mv-65-persist-miss-retry -> PR #15, founder-gated merge._
- **MV-64** · P1 · Eyebrow↔counter consolidation: derive each step's 'Step N' from the visible-step index — _MV-47's flagged residual + the MV-42 drift-class kill. Each wizard step hardcodes eyebrow="Step N" in its StepShell, but the gap step is conditional, so for the common no-gap student english/budget/goal render Step 7/8/9 while the live header counter (stepLabel in use-wizard-state.ts) correctly reads Step 6/7/8 of 8 — the eyebrow contradicts the counter. Fix: derive each step's eyebrow from the same visible-step index that feeds stepLabel (one source of truth), thread it via StepProps→StepShell, and delete all 9 hardcoded eyebrow="Step N". TDD: failing test = a no-gap student's english step renders eyebrow 'Step 6' (matching the counter), not 'Step 7'. Branch mv-64-eyebrow-counter off master (post-#11)._
- **MV-66** · P1 · Resilience: error + loading boundaries for the (focused) results routes — _Journey-reliability follow-on to MV-62 (which covered the signed-in (app) group: dashboard/matches/plan/profile/etc.). The (focused) route group had NO error/loading boundary, yet it hosts the single most conversion-critical surface: the anonymous wizard+results (app/(focused)/assess/page.tsx — live reads getUser + getPrimaryAssessmentForUser) and the recovered/saved assessment view (app/(focused)/assessment/[id]/page.tsx — live reads getRecoverableAssessment/getOwnedAssessment + a legacy-shape catalogue recompute via listAllPrograms/listAllUniversities). A thrown read there bubbled past the layout to app/global-error.tsx, which REPLACES the whole document (no FocusBar, bare inline styling) — a jarring dead-end on the results page; a slow read showed a blank frame. FIX (TDD, mirrors MV-62's (app) pattern): app/(focused)/error.tsx (calm branded retry, renders inside the layout so FocusBar stays; HONEST copy — names 'your results', claims only 'usually a connection hiccup', does NOT assert saved-data-is-safe since an anonymous persist-miss may have nothing saved) + app/(focused)/loading.tsx (flat-paper skeleton shaped like the results column — verdict panel over match cards — aria-busy, sr-only Loading, no spinner). matches/plan deliberately NOT touched (already covered by MV-62) — scoped to the one genuinely-missing group. Next.js semantics (next@16): a group-level error.tsx/loading.tsx catches the page reads under assess/ and assessment/[id]/; the layout's own getUser() throw still bubbles to global-error (expected, same as (app)). TDD RED→GREEN: +2 tests in tests/app/error-boundaries.test.tsx (focused error names results + retry calls reset; focused loading announces aria-busy). Gate green: typecheck clean, lint 0 errors, suite 1449/242 (was 1447). Adversarial Codex gpt-5.5/xhigh review confirmed correct semantics, complete (focused)-tree scope, all design tokens resolve, a11y ok. FOLLOW-UP (out of scope, pre-existing from MV-62): app/global-error.tsx:36 'Your saved data is safe' can be false for an anonymous user on the rare layout-bubble path — spun as a separate fix. Branch mv-66-focused-boundaries -> PR, founder-gated merge._
- **MV-68** · P1 · Ground-truth status audit + reconcile stale Phase 5/6 status, then scope next slice — _WHY: twice this session a 'next slice' pick turned out already-built — MV-66 (matches/plan boundaries already shipped in MV-62) then 'Phase 5 documents/Storage' which is FULLY BUILT (Storage bucket 'documents' + per-user-folder RLS in supabase/migrations/20260604060000_add_documents.sql; routes /api/documents/{upload,[id],[id]/view,status}; document-card.tsx POSTs files to Storage + signed-url view + delete; lib/documents/{types,repo,status-repo,upload-validation,flags}; document_status migration already applied to prod 20260626055206). Phase 6 (AI-guide) is also built (MV-63, merged PR #10, parked only on the founder's Vercel DEEPSEEK_API_KEY). So CLAUDE.md 'Not built yet: Phase 5 + Phase 6' and docs/PROJECT_STATUS.md are STALE and mis-steered both me and Codex. TASK (fresh session, post-/compact): (1) ground-truth audit of actual app state — what is genuinely incomplete vs shipped — a small read-only Workflow/Explore fan-out over features (documents, checklist, plan, matches, results, dashboard, profile, guide) checking per-surface: does it have the MV-62/66 error+loading resilience? is the document-readiness 'X of Y -> ready to apply' loop actually wired (checklist<->documents via lib/checklist/plan-links.ts + lib/documents/status-repo.ts) or only per-doc status toggles? any dead stubs / fabricated copy / trust leaks? (2) reconcile CLAUDE.md 'Project State' + docs/PROJECT_STATUS.md to reality (Phase 5/6 built). (3) from the audit, scope the TRUE next slice by student outcome (consultancy-replacement) and propose it. Candidates already surfaced: document-readiness loop (Codex runner-up), MV-45 progression visuals (confirmed not-built), route-resilience gaps. NOTE: MV-67 is reserved for the spawned global-error.tsx copy fix (background chip task_1c626e6b). Operate per autonomous-operating-model._
- **MV-69** · P1 · [Document-readiness rollup: 'X of Y required → ready to apply' per program](cards/MV-69-document-readiness-rollup.md) — _MV-68 audit finding #1 (docs/audits/2026-06-27-mv68-ground-truth-audit.md). Today three per-item 'done' signals exist with NO rollup and the student gets no system signal they're ready to apply: (a) an uploaded file flips that program's checklist row to 'Have' (app/(app)/checklist/[programId]/page.tsx:39-41 -> lib/checklist/generator.ts:140-142 statusFor); (b) plan state mirrors onto checklist rows (lib/checklist/plan-links.ts:14-22, 7 keys, medical unmapped); (c) the global document_status.obtained toggle on /checklist/all (lib/documents/status-repo.ts) is a DEAD-END BRANCH — listObtainedKinds has exactly one caller and feeds only that page's own checkboxes, never the per-program checklist, the plan, or any count. SCOPE: derive a real, data-backed readiness count per program (how many required 'now'-stage items are satisfied vs total, from the SAME generator + status sources, banded honestly — e.g. '4 of 7 required documents ready') and surface it on the per-program checklist header + the dashboard Documents stat + optionally an honest 'ready to apply' state when all now-stage required items are met. As part of it, RECONNECT or RETIRE the isolated /checklist/all document_status branch so its ticks count toward the rollup (Codex-decide which). TDD; trust-first (never claim ready when it isn't; never fabricate counts); goldens — generator output may change, update deliberately. Effort M. Branch mv-69-readiness-rollup off master -> PR, founder-gated merge._
- **MV-70** · P2 · [Resilience: error + loading boundaries for the (marketing) group](cards/MV-70-marketing-boundary.md) — _MV-68 audit finding #4. The (marketing) route group has NO error.tsx or loading.tsx, yet app/(marketing)/page.tsx, app/(marketing)/auth/page.tsx, and app/(marketing)/layout.tsx all do live supabase.auth.getUser() reads. A thrown/slow auth read on the first-impression landing surface -> blank frame or a bubble to the document-replacing global-error.tsx. FIX (TDD, mirrors MV-62/MV-66): app/(marketing)/error.tsx (calm branded retry inside the marketing chrome) + app/(marketing)/loading.tsx (flat-paper skeleton). KEY (Codex gpt-5.5/xhigh confirmed against installed next@16 docs): a same-segment layout.tsx throw bubbles PAST the group error boundary to global-error, AND loading.tsx does not cover the layout await -- so the boundary files only catch the PAGE-level reads. The audited failure path (layout's auth.getUser() probe on the anonymous homepage) is fixed by a narrowly-scoped try/catch in layout.tsx that degrades to the signed-out 'marketing' variant + logs loudly (page.tsx/auth.tsx reads are followed by redirect() so they must NOT be try/caught -- the error boundary covers their throws). Scope (d) = both boundary files + layout try/catch. +2 tests in tests/app/error-boundaries.test.tsx + new tests/app/marketing-layout.test.tsx (degrade-on-failure is the key honesty property). Lower student-outcome impact than MV-69 (landing, not the deep journey) but completes resilience parity. Effort S. Branch mv-70-marketing-boundary off master -> PR, founder-gated merge._
- **MV-71** · P1 · [Progression visual #28b: checklist-stage tag on PlanItemCard (reverse Plan->Checklist link)](cards/MV-71-plan-checklist-stage-tag.md) — _Carved out of the over-scoped MV-45. Design-division audit #28 ('Checklist<->Plan relationship asserted in copy, never shown') has TWO halves: (a) mono planState chip on ChecklistItem -- ALREADY SHIPS (checklist-item.tsx PLAN_CHIP + 'Track in your plan ->', from the MV-23 thread); (b) stage tag on PlanItemCard -- NOT built. This slice builds (b): a mono 'Checklist - Now' / 'Checklist - After offer' tag on each plan card whose kind mirrors a checklist requirement, closing the loop (checklist rows already point INTO the plan; plan cards now point BACK to the checklist stage). Reverse lookup planKind->ChecklistStage lives in lib/checklist/plan-links.ts (owns the mapping), backed by a static map GUARDED by a drift test that generates a checklist and asserts each linked key's stage matches (the generator stays the single source). Stages are mixed (3 now: doc-preparation/agent-marn/sponsor-income-cert; 4 after-offer: noc-application/biometrics/police-certificate/gs-responses) so the tag is genuinely informative. Vocabulary matches the checklist sections ('What you need now' / 'After your offer') -- no new terms (audit constraint). TDD: extend tests/checklist/plan-links.test.ts + new tests/plan/plan-item-card.test.tsx. Flat mono pill, calm-authority. Effort S. Branch mv-71-plan-checklist-stage-tag off master -> PR, founder-gated merge._
- **MV-72** · P1 · [Progression visual #16: intake tick-timeline on the results surface](cards/MV-72-intake-tick-timeline.md) — _Audit #16 ('intake timing has no timeline'). IntakeTimingCard rendered only a text list; add a calm flat tick-timeline above it: a 'Now' anchor + status-coloured intake ticks (open/tight/closed = verdict palette) at real calendar distance. NO new/invented data — positions derive only from each intake's existing month/year (computeIntakeTiming -> AUSTRALIA.intakes, DHA source + lastVerified 2026-06-02). Geometry is a pure unit-tested buildIntakeTimeline(timing, now) in lib/timing/intake.ts (mirrors computeIntakeTiming's now-param); component stays presentational. Timeline is aria-hidden — the nearest sentence + deadline notes + alternatives list below remain the accessible source of truth (no info lost, no duplicate SR output). Existing copy unchanged. TDD +9 (5 helper + 4 component); typecheck/lint clean; full suite 244/1472; goldens byte-identical (no scoring touched). Visual spacing is a blind judgment call (results surface is auth-gated, not browser-verifiable) — easy to nudge. Branch off master -> PR, founder-gated merge._
- **MV-73** · P1 · [Progression visual #15: outcome-funnel journey rail on the dashboard](cards/MV-73-outcome-funnel-rail.md) — _SHIPPED 2026-06-28 on branch mv-73-outcome-funnel-rail (founder merges to master to close -> Done). 3rd sub-slice of the MV-45 umbrella (after MV-71/MV-72); design-division audit #15. The signed-in dashboard's 'Your applications' rows (components/outcomes/outcome-funnel.tsx) showed only a stage badge -- no at-a-glance 'where am I on the journey'. Added a calm flat four-step rail Applied -> Offer -> Visa lodged -> Granted per row, slotted under the [program . stage badge] line and above the [verdict . intake . updated] meta; MV-39 self-report buttons stay last; existing stage badge kept. Design Codex-locked (gpt-5.5 Option A): the rail STOPS honestly and NEVER lights an unreached Granted -- a rejection exits red-hollow at Offer ('Rejected'), a refusal exits red-hollow at the Granted position labelled 'Refused' (not 'Granted'), a withdrawal is the student's own choice so it exits NEUTRAL GREY (never red) one step past the furthest reached milestone ('Withdrawn'); no intermediate steps are inferred -- fill only what the event stream confirms. Trust: data is the real DB event stream (outcome_events, RLS-scoped), zero fabrication. Plumbing: added events:EventType[] onto OutcomeFunnelRow (buildOutcomeFunnel now carries each attempt's reached event types) so a withdrawal keeps its pre-exit position (the single final 'stage' loses it); the rail geometry/states live in a pure unit-tested buildOutcomeRail(events) in lib/outcomes/funnel.ts (MV-72 buildIntakeTimeline pattern) and the component stays presentational. a11y: shape+label carry state (never colour alone) + one honest aria-label per rail ('Application progress: ... Refused, Granted not reached.'). TDD RED->GREEN +16 (11 helper terminal-state cases + 1 plumbing + 4 component render); gate green: typecheck clean, lint 0 errors (1 pre-existing build.mjs warning), full vitest 246 files / 1500 tests (was 1484 on master). No scorer/DB/migration; goldens byte-identical. Dossier: cards/MV-73-outcome-funnel-rail.md. Founder-owed: merge PR -> Done; visual spacing (dot/label sizes, baseline insets) is a blind call -- dashboard is auth-gated, can't browser-verify here._
- **MV-51** · P1 · [Optimistic save-state flip (Not saved / Shortlisted / Applied)](cards/MV-51-optimistic-save-state-flip.md) — _Founder-reported perf bug (2026-06-26): the status pills freeze (disabled) for the whole server round-trip — no optimistic update — and 'Applied' is slowest because the route chains captureApplication→freeze→insertAttempt→insertEvent. Fix: components/matches/shortlist-button.tsx choose() — capture prev, setStatus(next) immediately, then await fetch; revert on !ok/throw. No API change; optionally fire captureApplication non-blocking. Root cause: shortlist-button.tsx:26-36 + app/api/shortlist/route.ts:27-47 + lib/outcomes/on-apply.ts:33-49. Existing 4 button tests tolerate an optimistic version; extend for flip+rollback. Effort S, NO data dep. Verified by 6-agent read-only workflow 2026-06-26. RECOMMENDED FIRST SLICE._
- **MV-52** · P1 · [Surface doc-acquisition guidance on academic + English-test rows](cards/MV-52-doc-acquisition-guidance-rows.md) — _SHIPPED 2026-06-26 (branch mv-51) → In Review. Wired two already-ledgered datasets onto the rows that lacked acquisition guidance: nepal-document-processing-times.ts (TU equivalence 3 working days, A.087) onto bachelors/masters-transcript rows (conditional 'if your degree is from Tribhuvan University' copy, no profile field needed); nepal-english-test-centres.ts onto a new IELTS-only 'Where to sit IELTS in Nepal' info step (British Council 9 + IDP 4 locations + NPR 36,000 fee). Trust-first: PTE/TOEFL centre rows NOT fabricated (dataset defers them) — row gated on testKind==='ielts'. Source-display guard mirrors biometrics. lib/checklist/generator.ts only; no component change (checklist-item already renders note+source). TDD +4, gate green (typecheck, lint 0 err, 1368 tests). Founder merges PR → Done._
- **MV-53** · P2 · [Global document checklist + persisted 'I have this' toggle](cards/MV-53-global-document-checklist.md) — _SHIPPED 2026-06-26 (branch mv-51, +18 tests, suite 1395, gate green). Option B (Codex-concurred): a new orthogonal table public.document_status (owner, kind, obtained, primary key (owner,kind)) carries upload-independent 'obtained' state — documents (file_path/file_size NOT NULL) and lib/checklist/generator.ts left UNTOUCHED, so zero golden churn (generator + document/checklist tests stayed green, 80 untouched). kinds check-constraint + Zod enum are both the exact DOCUMENT_KINDS (machine-verified 20/20, same order). Shipped: migration 20260626000000_add_document_status.sql (owner-scoped RLS mirroring the outcomes idiom, UPDATE policy has using+with-check, authenticated not service_role, no backfill); status-repo.ts (listObtainedKinds / setObtained = upsert-ON / delete-OFF); validation/documents.ts; POST app/api/documents/status (mirrors outcomes/event: safeParse 422, getUser 401, owner from session); global page app/(app)/checklist/all iterating GROUPS/DOCUMENT_META; optimistic 'use client' toggle with rollback; reachable via a new 'Your overall document checklist' card on the existing /checklist landing (least-invasive — NAV_APP/mobile-tab-bar untouched; per-program views intact). Founder residuals (not blockers): apply migration to prod; merge PR→Done; nav label/IA sign-off._
- **MV-54** · P2 · [Scholarships: surface held application open/close dates](cards/MV-54-scholarship-key-dates.md) — _SHIPPED 2026-06-26 (branch mv-51, awaits PR merge). Surfaced the held Australia Awards application window (applicationOpens 2026-02-01 / applicationCloses 2026-04-30, australia-awards-scholarship.ts) as a calm 'Applications open 1 Feb 2026, close 30 Apr 2026' key-dates line. New optional ScholarshipRow.applicationWindow + deterministic ISO-slice formatter in select-scholarships.ts; conditional <p> in scholarships-panel.tsx rendered ONLY when held. Trust-first: the 3 au-scholarships rows hold no dates so show no line (honest absence, never invented). TDD +3 (incl. absence guard); typecheck clean, lint 0 err, suite 1371 (was 1368). Founder: merge PR -> Done._
- **MV-56** · P2 · [My Plan: assemble funds-release-from-Nepal walkthrough](cards/MV-56-funds-release-walkthrough.md) — _SHIPPED 2026-06-26 (branch mv-51, awaits PR merge). MV-37's A–E timeline already groups apply-for-noc + prepare-fund-remittance into Phase C (adjacent) — so the 'scattered' half was already fixed; this card makes them READ as one walkthrough using ledgered data, WITHOUT merging (Codex-confirmed: merging breaks the different gates [finance.source vs australia], per-kind completion.ts/sources.ts/VISA_PREP_KINDS wiring, and per-step done-tracking). Two copy-only generator changes: (1) lead apply-for-noc with the ledgered B.016 NOC definition (already on the per-program checklist via SOF_DEF; now in the plan too — framed generally 'study abroad', not AU-only); (2) prepare-fund-remittance appends an AU-gated cross-reference to the NOC step ('its own step in this plan … bank releases the money once it's issued'), suppressed when no destination so it never dangles. No phase/sources/completion change; no dataset edit (only consumes B.016's summary → no reconcile/drift impact). TDD +2 (definition surfaced; cross-ref present-when-AU / absent-when-unset). Gate: typecheck clean, lint 0 err, suite 1377 (was 1375). Founder: merge PR → Done. Pairs with MV-57 (RESEARCH-BLOCKED lodgement/pre-departure half)._
- **MV-57** · P3 · [My Plan: journey-spine connective steps (offer→CoE→OSHC→lodge→track)](../superpowers/specs/2026-06-28-journey-spine-plan-sequencing-design.md) — _Founder ask (2026-06-26) — RESEARCH-BLOCKED half of the plan gap. Plan says 'Once your offer arrives' (lib/plan/generator.ts:214) but never sequences how to GET the offer (submit uni application) or lodge Subclass 500 (ImmiAccount/VFS), and pre-departure is absent. Pre-departure/arrival/lodgement data sits as PENDING (unsliced) rows in docs/research-briefs/findings/H.jsonl (68 pending) + B.jsonl (VFS/payment G14-18); offer-acquisition (uni application-portal steps) has NO ledger data — needs NEW research. UNBLOCKS WHEN: H/B pending rows sliced into lib/data/source + offer-portal research sourced. Effort L, DATA-DEP._
- **MV-58** · P3 · [Scholarships: research-degree honesty signal + Sydney pool-aggregate relabel](cards/MV-58-scholarship-honesty-signal.md) — _SHIPPED 2026-06-26 (branch mv-51) — the ledger-backed, honestly-shippable subset extracted from MV-55. Two honesty fixes on the scholarships list: (1) new AuScholarship.researchDegreeOnly flag → studyEligibility line 'Research degrees only — PhD or research master's' on UniMelb GRS (backed by J2.007/008; coursework students no longer misled into HDR-only awards); (2) Sydney $135M relabeled from an implied single award to 'across all its scholarships per year' (figure KEPT + J2.009 + drift guard untouched — presentational fix, not a deletion). researchDegreeOnly NOT set on Sydney (mixed pool → honest absence). TDD +4, suite 1375, reconcile drift-guard green, typecheck/lint clean. Per-award eligibility/process/figures deferred to MV-55 (data-blocked, NEEDS-REVERIFY). Founder: merge PR→Done._
- **MV-59** · P2 · [Working-with-agents panel: three gov depth rows (G.078/G.082/G.095)](cards/MV-59-working-with-agents-gov-depth.md) — _SHIPPED 2026-06-26 (branch mv-59) — working-with-agents trust-defense lane (G.jsonl cluster). Added the 3 pending gov-primary depth-companions to the existing ungated trust panel: G.078 (OMARA register also searchable by business location → verify-register), G.082 (an authorised recipient must not give immigration assistance unless registered → formal-representation), G.095 (govt analysis ESTIMATES a student who still uses an agent may pay ~AUD 255/enrolment, pairs with the AUD 510 avg-commission row → commission-ban). Data-only add to au-working-with-agents.ts (16→19 rows) + copy-lock test; G.078/082/095 flipped pending→used via FLIP_STATUS writer + value_status set prose-only; registry comment 16→19. TDD; typecheck/lint clean; suite 1396 passed/0 failed. Faithful restatements, fact-only, no scoring. Codex+analysis split the actionable register CTA into MV-60. Founder: review copy → merge PR→Done._
- **MV-74** · P2 · [Dashboard readiness map (decomposed verdict)](cards/MV-74-readiness-map.md) — _Design LOCKED + founder-approved 2026-06-28 (branch mv-74-readiness-map). Dashboard "Your readiness" card decomposing the single banded verdict into an honest map: rows Academics & English / Money & funding / Visa readiness (band from each scored dimension's factor influences: risk->risk, only-positive->strong, else->needs-work, under-informed->add-detail) + Documents (cheap honest count: not-started / in-progress, never "ready"). Completeness = quiet header line (not a row); Applications stay in OutcomeFunnel. SUPERSEDES the rejected global journey-rail design (linear step-N-of-6 funnel duplicated PromptCard+StatsRow; always-on marker = nagging chrome) per Codex gpt-5.5 + dashboard-read validation. Surfaces the engine's existing dimensions breakdown (funds+English ARE collected/scored, not data-blocked). Pure buildReadiness(signals) helper + presentational ReadinessMap REPLACING StatsRow; zero extra queries (reads primary AssessmentPayload dimensions already loaded). No scoring/migration; goldens byte-identical. Spec docs/superpowers/specs/2026-06-28-readiness-map-design.md. NEXT = BUILD per spec (TDD) after compact._
- **MV-67** · P2 · [Fix global-error.tsx false 'saved data is safe' claim (anon trust)](cards/MV-67-global-error-copy.md) — _BACKLOG (agent-ownable, founder-gated merge). app/global-error.tsx (the root-layout last-resort boundary shipped in MV-62) renders 'We hit an unexpected error loading MyVisa. Your saved data is safe. Please try again.' at ~line 36 (confirmed present). That saved-data claim is NOT always true: global-error is the ONLY error boundary an ANONYMOUS visitor can reach (it fires when a layout itself throws -- e.g. app/(focused)/layout.tsx or the root layout getUser() -- past the per-segment error.tsx). An anon user whose assessment hit a persist-miss (POST /api/assess returned 200 with id:null) may have NOTHING saved server-side, so promising their data is safe is a false reassurance on a trust-first product. FIX (surgical, copy-only): reword app/global-error.tsx to drop the unconditional saved-data claim and keep only what is always true -- e.g. 'We hit an unexpected error loading MyVisa. This is usually temporary - please try again.' Do NOT touch app/(app)/error.tsx (signed-in only, so 'your saved data is safe' is correct there) or app/(focused)/error.tsx (already avoids the claim -- both confirmed present). TDD: update the global-error describe block in tests/app/error-boundaries.test.tsx to assert the honest copy, watch it fail, then change the file. Gate: typecheck + lint + npx vitest run tests/app/error-boundaries.test.tsx (+ full suite before PR). No scoring/DB/migration; goldens byte-identical. Branch mv-67-global-error-copy off master, PR, founder-gated merge. Sourced from the founder 2026-06-28. Dossier: cards/MV-67-global-error-copy.md._
- **MV-75** · P2 · [Fold profile accordion onto disclosure primitive](cards/MV-75-disclosure-consolidation.md) — _Phase B (audit #21, P2). Carved out of MV-44 2026-06-28 (that slice shipped #11 matches-tablist). Two divergent accordion implementations: components/ui/disclosure.tsx (the primitive, has a trailing chevron) vs components/profile/section-accordion.tsx (its own impl, lacks the chevron). Fold section-accordion onto the ui/disclosure primitive with an optional trailing-slot (so profile sections gain the chevron + a single keyboard/aria behaviour to maintain). DRY refactor, not user-blocking. Verify both surfaces (profile section headers + wherever disclosure is used) stay visually + behaviourally correct; goldens byte-identical. TDD. Spec: docs/audits/2026-06-25-design-division-polish-audit.md #21. → SHIPPED 2026-06-28 (TDD): Disclosure gains an optional trailing slot; SectionAccordion reimplemented as a thin wrapper over it, inheriting the chevron + aria-controls + ease-calm. Public API unchanged; results/profile consumers behave identically. +3 tests, suite 1560/248 files, typecheck+lint clean. In Review on branch mv-75-disclosure-consolidation, PR #31, founder-gated merge._
- **MV-76** · P2 · Verdict reveal + Applied-commit motion (audit #25) — _Phase B (audit #25, P2). BUILT+SHIPPED 2026-06-28 -> PR #30 (In Review, branch mv-76-verdict-reveal, founder-gated merge). Two-beat verdict reveal + a neutral outcome confirm beat, both Codex trust-checked (no score leak, no celebration on a refusal, reduced-motion sound). Verdict card keeps animate-rise (beat 1); the band-label pill ALONE gets a new animate-settle (beat 2) whose ~120ms hold is baked into the keyframe (0%,22% of 0.55s) NOT animation-delay, so the global prefers-reduced-motion guard collapses it with no blank flash and the never-shown score is never animated. MV-39 OutcomeSelfReport: a successful POST shows a deliberately neutral "Saved" (text-ink-soft, no colour/glyph -- the same control reports a refusal as well as an offer) that eases in via animate-fade (no hold, can not be clipped by a fast router.refresh) and clears when the legal-next-steps set advances (render-time seenKey reset, not an effect). Codex round folded 2 fixes pre-merge: clear confirmed on any new attempt (never "Saved"+error together); fade not settle for the note. TDD +4 (suite 248 files / 1561). Optional staggered factor-bars deliberately skipped (animating a numeric surface = trust risk). Spec docs/superpowers/specs/2026-06-28-verdict-reveal-design.md; audit docs/audits/2026-06-25-design-division-polish-audit.md #25._
- **MV-77** · P1 · Dashboard "Your journey" panel (MV-45 #3 where-am-I rail) — _IN REVIEW 2026-06-28 (PR #29, branch mv-77-journey-rail, founder-gated merge). Carved from the MV-45 umbrella -- the global cross-stage "where am I" orientation rail (consultancy-replacement: here is the sequence, you are here, next is X). A DASHBOARD PANEL (founder pick over persistent chrome -- we already have AppBar + MobileTabBar). 6 stages Assessed->Profile->Matches->Plan->Documents->Apply, each progress-derived from real signals only (mirrors buildReadiness/ReadinessMap); +1 query (listShortlistForUser), Plan load widened to all-items (zero net extra). Codex gpt-5.5 xhigh: SOUND-WITH-TWEAKS, all 3 adopted (Matches reached-word "shortlisted" not "done"; Assessed gated on scored result; Plan engagement covers completed items). Pure buildJourney() in lib/journey/journey.ts + presentational JourneyRail; honest frontier + visible skipped-gap; sr-only word-state summary; no reach-red. Spec: docs/superpowers/specs/2026-06-28-journey-rail-design.md. TDD. Apply=#15-funnel detail stays in MV-73 (no dup)._
- **MV-78** · P2 · [AI guide go-live trust hardening (Codex review)](cards/MV-78-guide-go-live-hardening.md) — _Codex (gpt-5.5 xhigh) pre-go-live trust review of the now-LIVE AI guide (MV-63, key set in Vercel 2026-06-29). SHIPPED the one material, clean must-fix: a provider timeout (AbortSignal.timeout 20s) in lib/guide/deepseek.ts so a hung DeepSeek call aborts → the route serves its calm 503 instead of hanging (the calm-503 trust promise was otherwise broken). TDD +1. The system prompt + context builder are already trust-sound (scoring internals omitted, banded-only, no-draft, auth-gated). Remaining 7 Codex findings TRIAGED in the dossier for founder to prioritize: worth-doing = #1 untrusted client assistant-history (prompt-injection) + #7 per-line cost-note; SKIP/YAGNI = #2 hard output-filter (system prompt is the v1 gate); low/near-FP = #4 base-url allowlist, #5/#6 reason numbers, #8 source deferral. TDD; typecheck+lint clean._
- **MV-79** · P1 · [Guide: neutralise client-forged assistant history (prompt-injection defense)](cards/MV-79-guide-untrusted-history.md) — _IN REVIEW 2026-06-29 (branch mv-79-guide-untrusted-history, founder-gated merge). Codex finding #1 from the MV-78 go-live trust review (the meatiest residual). POST /api/guide/chat spread the browser history verbatim, incl. role:"assistant" turns -- the chat API treats those as the model's OWN authoritative prior output, so any client could forge "Your visa is guaranteed approved" and the guide would build on it (worst fabrication path for a trust-first guide). Fix: new pure buildSafeHistoryMessages() (lib/guide/history.ts) folds the whole transcript into ONE user-role block framed as unverified/browser-reported, authority pointed back at the server-built system context -- no client message ever reaches the provider as role:assistant. Multi-turn continuity preserved (labelled Student asked/Guide replied); current question stays the trusted final user turn; server re-grounds facts every turn anyway. TDD +4 (3 helper + 1 route injection-defense); typecheck+lint clean; suite 251 files / 1588 green._
- **MV-80** · P0 · [FY2026-27 data re-verify: 16 overdue reverifyBy records (DHA fees, wages, ART, ATO)](cards/MV-80-fy2026-27-reverify.md) — _The designed 1-July freshness timer FIRED: tests/data/freshness.test.ts red with 16 records at reverifyBy 2026-07-01 (AU_SUBCLASS_500_APPLICATION_CHARGE_AUD; AU_SKILLED_VISA_CHARGES[0-3]; AU_TAX_FIGURES[1-4]; AU_STUDENT_WORKER_WAGES[1-6]; NEPAL_REFUSAL_RECOVERY[11]). A 4-agent read-only scout verified every record against its authoritative live source on 2026-07-02: 12/16 CHANGED — subclass 500 AUD 2,000→2,500; skilled 189/491/186 → 6,135/6,140/6,140; 191 505→630; NMW 24.95→26.44 in force (announced→current collapse needed); Hospitality casual intro rows re-based on 25.74; ART fee 3,580→3,727; all 4 ATO tax figures unchanged. Evidence packet with per-record source URLs: docs/audits/2026-07-02-fy2026-27-reverify-scout.md. Fix = update values + lastVerified 2026-07-02 + reverifyBy 2027-07-01, open sources during the edit (never blind-copy), full suite green, branch+PR._
- **MV-82** · P1 · [Overhaul Phase 0a: Mobbin rebrand research digest](cards/MV-82-mobbin-rebrand-research.md) — _First slice of the founder-approved "Elevated calm" UI/UX overhaul (spec: docs/design/2026-07-03-elevated-calm-overhaul-spec.md). Mobbin sweep of Phantom/Linear/Headspace/Airbnb/Notion/Wise/Revolut/Cleo/Cash App (+Duolingo flow mechanics ONLY) across onboarding/auth/results-reveal/conversion/empty/error flows; fixed extraction table per app (hue+saturation strategy, paper strategy, accent count, radius, type scale, motion choreography, mascot placement, dark-mode derivation rule); synthesis = 5 patterns for calm-but-Gen-Z wizards + 3 tells of premium fintech-calm. Deliverable: docs/design/2026-07-XX-rebrand-research.md. REQUIRES A FRESH SESSION (Mobbin MCP tools only register post-auth session start)._
- **MV-83** · P1 · [Overhaul Phase 0b: palette candidates (full rebrand, contrast-proven)](cards/MV-83-palette-candidates.md) — _2-3 candidate palettes, each = all 23 semantic tokens x light+dark with exact hexes + full WCAG matrix (AA 4.5:1 text / 3:1 UI). Placeless names; seeds: Night indigo / Deep blue / Dusk plum — validated against MV-82 findings. Verdict trio stays green/amber/red semantic space with possible-ink-style companions; token NAMES frozen (rebrand = values-only swap). Deliverables: docs/design/2026-07-XX-palette-candidates.md + scripts/contrast-check.mjs + 3 throwaway preview branches (globals.css swap only) for Vercel previews. FOUNDER GATE: palette pick. Spec: docs/design/2026-07-03-elevated-calm-overhaul-spec.md._
- **MV-84** · P1 · [Overhaul Phase 0c: rebrand token swap — Dusk plum values into app/globals.css (names frozen)](cards/MV-84-dusk-plum-tokens.md) — _Direct hand-off from the MV-83 palette pick (founder picked Dusk plum). Swapped all 23 semantic design tokens x light+dark from teal-era values to Dusk plum in app/globals.css — token NAMES frozen (components reference names only, so this is a values-only rebrand); values copied verbatim from the WCAG-proven single source of truth scripts/palette-candidates.data.mjs (dusk-plum). Light: primary #0f5e54->#6a2b57 (deep plum), warm paper #f6f5f1->#f4f1ea, reach warm-nudged #b1503a->#a4472f (plum-adjacency gate), possible darkened #b07d22->#8f6218 so amber clears AA even as text. Dark: primary orchid re-hue #4eb39f->#c98bb4, warm near-black bg #111210->#141014; verdict-trio dark values unchanged (already the palette's shared dark set). Rewrote the stale light possible/possible-ink comment (dropped #f6f5f1/ratio literals, points to the harness); dark comment still accurate, kept. Deterministic transcription check: all 46 values == canon, 0 mismatches/leaks/extras. Updated the only hardcoded-hex test (tests/styles/globals-a11y.test.ts #10). Gate green: scripts/contrast-check.mjs exit 0 (162 checks pass), typecheck clean, lint 0 errors (1 pre-existing build.mjs warning), full suite 1588 passed / 250 files — only failure = pre-existing tests/data/freshness.test.ts (deferred MV-80 1-July timer, untouched). Goldens byte-identical (no component/class changes). BOARD NOTE: this MV-84 was re-pointed from the old provisional 'design-system v2 spec (ADR)' scope, which is re-filed as MV-88, to match the founder's live intent + the MV-83 hand-off. Founder-owed: Vercel-preview browser sweep (results/dashboard auth-gated; spacing a blind call) + merge PR -> Done. Spec: docs/design/2026-07-03-elevated-calm-overhaul-spec.md._
- **MV-88** · P1 · [Overhaul Phase 0c (doc): type-scale ADR (--text-* scale)](cards/MV-88-type-scale-adr.md) — _RESCOPED from the over-broad 'design-system v2' bundle to the TYPE SCALE ONLY (simplicity-first) — Motion v2, primitive contracts, and the no-framer-motion call are deferred to their own future ADRs. Standalone decision doc docs/design/2026-07-03-type-scale-adr.md (foldable into the overhaul umbrella spec on sign-off). Grounded in a 2026-07-03 audit: 273 text-[Npx] occurrences across components/+app/, 15 distinct sizes 10-26px (15px x77 workhorse, 14/13 x37, 17 x30, ... 26 x1). DECISION = a named 9-step @theme scale: caption 11 / small 13 / meta 14 / body 15 / control 16 / lead 17 / title 20 / headline 21 / display 24 — FAITHFUL (anchored to the app's high-frequency sizes) so migration is ~90% a 1:1 rename; only 27 sites snap (each <=2px: 10->11, 12->13, 18/19->20, 22/26->24). Reconciles the inherited body base = 17px (globals.css:189) as 'lead'. Alternatives A(chosen)/B(disciplined 6-step, more churn)/C(lint allow-list)/D(Tailwind defaults) recorded; 3 open questions for sign-off (20/21 merge?; adopt B instead?; line-height). DOC-ONLY: zero source/test files touched -> no regression risk, suite unaffected; typecheck+lint green. APPLYING the scale (add @theme block, rename 273 sites, 3 primitives, add no-arbitrary-text-size lint) is a SEPARATE founder-gated slice. FOUNDER GATE: ADR sign-off (batch review with MV-83). Spec: docs/design/2026-07-03-elevated-calm-overhaul-spec.md._
- **MV-89** · P1 · [Overhaul Phase 0c-2: rebrand completion — off-globals brand surfaces to Dusk plum + permanent token-contrast guard](cards/MV-89-rebrand-completion.md) — _First slice carved from the MV-87 umbrella after MV-84: every brand surface that CANNOT read CSS custom properties, swapped to Dusk plum, plus the permanent WCAG guard. (1) app/global-error.tsx 5 inline hexes (renders when CSS itself may have failed) → #f4f1ea/#211a20/#5c5058/#6a2b57 CTA + #fdf7fb (now on-primary, was literal #ffffff). (2) app/layout.tsx gains a viewport themeColor export (there was NONE — browser chrome had no brand tint): --bg per prefers-color-scheme (#f4f1ea light / #141014 dark; matches served theme since MV-43 follows the OS). (3) Icons: new app/icon.svg provisional mark (plum rounded square + on-primary M strokes; superseded when the MV-85 Wayfinder mascot lands) + app/favicon.ico REGENERATED as PNG-in-ICO 16/32px via new re-runnable scripts/generate-favicon.mjs (zero-dependency rasteriser; evidence: sampled interior pixel = #6a2b57 exactly). (4) CLAUDE.md Design-Language hexes → plum era. (5) NEW permanent guard tests/styles/token-contrast.test.ts: re-proves every component fg/bg pair (imports the pair policy from scripts/palette-candidates.data.mjs so guard and harness can never disagree) against the LIVE app/globals.css on every test run, both themes, WCAG 2.1 math identical to contrast-check.mjs (float composite, 1e-9 epsilon) + 23-token names-frozen count assertion — a future values-only rebrand can no longer merge with a contrast regression. NEW guard tests/styles/brand-surfaces.test.ts pins all off-globals surfaces to plum + asserts the retired teal set absent. TDD (brand-surfaces red-first). Stacked on mv-84-dusk-plum-tokens — merge AFTER PR #41._
- **MV-99** · P1 · [Step 4 multi-subject: primary field owns the verdict + up to 2 'also considering' fields (broaden matches, exploratory label, honest competitiveness note)](cards/MV-99-step4-multi-subject.md) — _Founder ask (2): let students pick multiple subjects without breaking the honest single verdict. Decision (mine + Codex): Option A -- one primary fieldOfStudy owns the verdict + scoring baseline exactly as today, plus up to 2 additive alsoConsidering fields that only broaden which programs surface. Scope win: scoring engine untouched, all golden scores byte-identical (proven inert). Codex guardrail built: also-considering programs carry an explicitly exploratory match reason so a student never reads the primary verdict onto them. New pure modules lib/wizard/also-considering.ts + lib/scoring/field-note.ts (honest 'less/more competitive admit' note). Matching = 3-tier soft sort (primary->also->rest). Additive types + Zod (disjoint/dedupe refine), wizard multi-select (cap 2, primary excluded, disabled at cap), profile-editor parity, recap 'also considering' line, JSONB persistence (no migration). Deferred: per-field verdicts (Option C) + results-page note surfacing._
- **MV-100** · P1 · [Matches progressive disclosure (signed-in): top N per verdict band + show-more, Reach collapsed behind its count](cards/MV-100-matches-progressive-disclosure.md) — _Founder ask 6. The signed-in /matches page rendered every card in every band at once (~86), so the page opened as a wall and buried the Strong picks. Founder-picked (via AskUserQuestion) design: top N per band + "show more", chosen over a sort/filter control bar -- rejected because re-sorting by cost or rank could float a cheap Reach above a Strong pick, cutting against the verdict-first honesty the app is built on. verdict-group.tsx became a client component with per-band expanded state + an initialVisible prop (default 3); hidden cards are NOT mounted until expanded (~6 in the DOM instead of ~86), so it is a real perf win, not CSS hiding. Reach passes initialVisible={0} = fully collapsed behind its count, de-emphasising stretch schools while keeping the true total visible and one click away. A11y: real button with aria-expanded + aria-controls, explicit label, singular/plural noun. The anonymous results page already discloses (3 free + locked) and is untouched. NOTE (2026-07-17): MV-120 later deflated most students to all-Reach, which turned initialVisible={0} into an empty page for them -- that regression is MV-121, and it does not make this design wrong._
- **MV-101** · P1 · [Results-page competitiveness note: surface MV-99 honest also-considering line near the verdict (not just in the wizard)](cards/MV-101-results-competitiveness-note.md) — _MV-99 follow-up. The honest "Business is a less competitive admit than Computer Science" line only showed in the wizard at point-of-choice; a student reviewing results never saw it. Compute competitivenessNote(primary, alsoConsidering) in assembleAssessment (where the profile is in scope) and carry it on AssessmentPayload -- same pattern as preferenceNote -- then render a calm CompetitivenessNote callout under the VerdictCard on the results page. Verdict stays scored on the primary field alone; the note is presentational context only. Additive optional payload field (no migration). Depends on MV-99 (alsoConsidering + field-note.ts) -- stacked PR on the MV-99 branch. TDD +4._
- **MV-102** · P1 · [Per-field verdicts (Option C): promote 'also considering' fields to real banded verdicts on results](cards/MV-102-per-field-verdicts.md) — _MV-99/101 follow-up. The also-considering fields only earned a muted competitiveness line; a student who is Reach for their primary but Possible for a secondary could never see that concrete pivot -- a self-serve dead-end. Option C re-scores the SAME student in each also-considering field (runAssessment with only fieldOfStudy swapped), keeping ONLY the band, and renders compact clearly-conditional secondary bands beneath the unchanged primary VerdictCard, with an honest low-pressure pivot callout when a secondary strictly outranks the primary. New pure module lib/results/secondary-verdicts.ts + presentational components/results/secondary-verdicts.tsx (replaces the results render of CompetitivenessNote). Primary path byte-identical (goldens unchanged), no DB/migration. Forced integrity fixes: from-sections forwards alsoConsidering (signed-in path), matches exploring-label reworded off 'not covered by your verdict', IntendedStudyPatch disjoint/dedup refine. Codex must-fixes: editor always sends alsoConsidering (clears stale extras), AI-guide context carries the secondary bands (word-only). Spec docs/superpowers/specs/2026-07-04-per-field-verdicts-design.md rev 3._
- **MV-90** · P1 · [Overhaul Phase 1: Card + VerdictPill primitives — extract the 28×-copy-pasted card shell and verdict pill into components/ui](cards/MV-90-card-verdictpill-primitives.md) — _Phase 1 slice from the MV-87 umbrella (the correction from MV-42: only VERDICT_LABELS shipped; the primitives never did). Extract a shared Card shell primitive and a VerdictPill primitive into components/ui/, then migrate every copy-pasted call site to them with rendered output byte-identical (exact className strings preserved so goldens/presentational tests stay green unchanged). Closes design-division audit finding #12 (incl. the rounded-xl radius drift — folds onto the 12px card radius, render-identical in this app's scale). Scout correction: census = 69 shell sites across 7 static families (not 28), 8 pill sites + 1 unpilled label; #13 (Button loading contract), #18 (fake-loading theatre) and #26 (Meter) are separate follow-up slices, NOT here. TDD: primitive unit tests first, then site-by-site migration with the suite as the harness. Scope guard: presentational refactor only — class SET per site preserved (order normalizes shell-first); deliberate unifications: possible pills gain the contrast-tuned text-possible-ink, 2 pill outliers gain inline-flex items-center, labels all source from VERDICT_LABELS (kills 5 local dicts + 1 hardcode). Excluded: form-control shell (29 sites — future Input primitive), ternary-tone shells, near-miss singletons._
- **MV-91** · P1 · [Overhaul Phase 1: Button loading contract — in-flight primitive + migrate the drifted loading buttons](cards/MV-91-button-loading-contract.md) — _Third of the Phase-1 "Primitive completion" trio (Card+VerdictPill shipped in MV-90; audit finding #13). components/ui/button.tsx gains a loading?:boolean + loadingLabel?:ReactNode contract: when loading the button is disabled (disabled||loading, never double-submits), sets aria-busy="true" (absent when idle), and renders {loadingLabel ?? children} followed by ONE canonical mono ellipsis (aria-hidden … in font-mono — the design-system "system is working" voice). Fixes the concrete drift in document-card.tsx (Upload said "Uploading…" but View said "Loading..." — three ASCII dots, same component) and folds assess/refresh-button.tsx, a raw <button> that re-implemented the primary/lg class string, back onto the primitive (<Button size="lg" loading={pending} loadingLabel="Refreshing">). Adds the missing aria-busy a11y signal to every loading button. Deliberately NOT touched (not loading buttons): shortlist-button optimistic toggle, plan-item-card small-pill busy actions (separate family), document-status-toggle checkbox. TDD: NEW tests/components/ui/button.test.tsx (contract) + extended document-card.test.tsx (in-flight upload disables+aria-busy). Independent branch off origin/master (targets disjoint from MV-90)._
- **MV-92** · P1 · [Overhaul Phase 1: Input / Select field primitive](cards/MV-92-input-select-primitive.md) — _Completes the Phase-1 primitive arc (Card/VerdictPill=MV-90, Button-loading=MV-91, form fields=this). Extracts the single most-duplicated string left in the repo — the form-field shell rounded-md border border-line-2 bg-surface px-3 py-2 text-[16px] text-ink focus:border-primary, copied verbatim 28× across the 8 profile-editor files — into NEW components/ui/input.tsx exporting Input (<input>) + Select (<select>), sharing one fieldShell, cn()-merging any per-site className, pure passthrough (no injected attrs = DOM unchanged). Migrated all 28 field sites; the two <input type=checkbox> and the guide-chat textarea (a distinct single-site shape) left out of scope per simplicity-first; about-you w-24 preserved. Intended migrate→verify Workflow died on the account session limit (8 subagents, 0 edits) → fell back to a deterministic checkbox-safe Node transform, LF-normalised. TDD +7 (input.test.tsx red-first). Gate: tsc 0, lint 0 err, suite 1603 pass / 1 fail (pre-existing MV-80 freshness), all 8 editor suites green = behaviour-preserving._
- **MV-93** · P1 · [Overhaul Phase 0c (doc): Motion v2 ADR (CSS-first motion system)](cards/MV-93-motion-v2-adr.md) — _Second of the two decision docs the overhaul needs locked before Phase-2 build (companion to the MV-88 type-scale ADR; third slice carved from the over-broad 'design-system v2' bundle). Standalone doc docs/design/2026-07-03-motion-v2-adr.md (foldable into the overhaul spec on sign-off). Grounded in a 2026-07-03 audit of the app's CSS motion system: @theme has 1 easing (--ease-calm cubic-bezier .22/.61/.36/1) + 3 keyframe animations (fade/rise/settle — ALL opacity+transform, already compositor-safe; settle bakes its 0-22% hold into keyframe PHASES not animation-delay so the reduced-motion guard collapses it cleanly) + a global prefers-reduced-motion collapse; usage tally ease-calm x24, transition-colors x14, animate-pulse x14, duration-150 x9, ... transition-all x1 (a latent smell); NO framer-motion/motion in package.json. DECISION = stay CSS-FIRST, no runtime animation library (bundle discipline on a low-end-Android funnel; narrow Phase-2 escape hatch = one tree-shaken micro-primitive for a single surface only). TWO motion classes: movement = opacity+transform only (compositor); state = paint props only (color/bg/border). HARD RULES: never transition-all; one easing; never bypass reduced-motion (bake delays into keyframe phases). Vocabulary = ease-calm + fade/rise/settle + pulse(skeletons). Stagger convention = <=5 siblings / 40-60ms / nothing after ~700ms. Alternatives A(chosen)/B(framer-motion, ~30KB rejected)/C(no ADR) recorded; 3 open questions for sign-off (duration tokens?; second easing?; pre-define slide-* keyframes?). DOC-ONLY: zero source/test files touched -> no regression risk, suite unaffected; typecheck+lint green. APPLYING it (remove the 1 transition-all; optional --dur-* tokens) is a SEPARATE founder-gated slice. ID MV-93 because 90/91/92 are claimed by in-flight branches. FOUNDER GATE: ADR sign-off (batch with MV-88). Spec: docs/design/2026-07-03-elevated-calm-overhaul-spec.md._
- **MV-94** · P2 · [Overhaul Phase 1 (apply): drop the last transition-all (Motion v2 ADR item 1)](cards/MV-94-drop-transition-all.md) — _First apply-slice off the locked Motion v2 ADR (MV-93). The ADR's hard rule is never transition-all — name the properties. The audit left exactly 1 site: components/ui/progress-dots.tsx, where the active-step pill used transition-all to animate its width grow (w-1.5→w-6) + colour (bg-bg-tint→bg-primary). Replaced with the explicit transition-[width,background-color] (matches the codebase style, e.g. button.tsx transition-[background-color,transform]; width is the sanctioned intentional-pill-grow pattern also used by accuracy-meter/factor-bars, not layout thrash). Rendered behaviour byte-identical — the two props already animated under transition-all; this only names them. TDD red-first: new class-assertion test locks transition-[width,background-color] present + transition-all absent (mirrors completeness-ring.test.tsx). Audit: 0 transition-all remaining in components/+app/ after. Uncontroversial + orthogonal to the ADR's open questions, so safe ahead of full sign-off. Suite 1598 (1597 pass, +1 new test; the 1 red = pre-existing MV-80 freshness timer, unrelated). Independent slice; ID MV-94 because board tops out at MV-89 and 90-93 are claimed by in-flight branches._
- **MV-95** · P2 · [Overhaul Phase 1: honest recap — drop the fake 3000ms Analyzing theatre (audit #18)](cards/MV-95-honest-recap.md) — _Carves the honest-recap half of the overhaul spec's Phase-1 MV-90 (Motion v2 + honest recap); the motion-system halves stay gated on the Motion v2 ADR (PR #48). Every wizard-completer was held on a fixed 3000ms 'Analyzing your profile' timer before results, regardless of when the real /api/assess result landed — pure latency theatre at the highest-traffic funnel moment, exactly the anti-pattern a trust-first product should never ship. Fix (components/assess/profile-recap.tsx only): durationMs default 3000->600 (a genuine reveal beat, never padding — AssessFlow already gates the real transition on payload && recapElapsed, so results now appear at max(real API latency, 600ms) = honest waiting only); summary stagger i*0.5s->i*0.08s so the reveal completes within the shorter window; label 'Analyzing your profile'->'Your answers' (honest summary label, not a fake-work claim; the existing pulse dot honestly signals in-flight work). Auto-advance kept over the spec's explicit 'See my result' button (lower-friction, more honest, smaller). Orthogonal to the unsigned type/motion ADRs + pending mascot: reuses the sanctioned animate-rise keyframe, adds no motion primitive. TDD +2 (tests/assess/profile-recap-timing.test.tsx, RTL+fake-timers, red-first; locks the two objective trust invariants — short handoff window + no /analyz/i label — not the exact copy/timing, which stay founder-tunable). Blind calls flagged on the card: exact copy, 600ms/stagger feel, button-vs-auto fork._
- **MV-96** · P2 · [Overhaul Phase 1: corridor theme provider (np-au, expansion-ready)](cards/MV-96-corridor-provider.md) — _Ships the post-onboarding corridor personalization ARCHITECTURE (spec MV-91) — the last Phase-1 slice not gated on the type-scale ADR (#47), Motion v2 ADR (#48), or the mascot (MV-85): (1) new lib/theme/corridor.ts registry (np-au → label + danphe mascotVariant; corridorForHomeCountry case-insensitive 'nepal' → np-au, else null = neutral brand; DEFAULT_CORRIDOR for the signed-in MVP, same Nepal assumption lib/scoring/from-sections.ts:42 already pins). (2) globals.css [data-corridor=np-au] light+dark blocks overriding ONLY --accent/--accent-tint (danphe teal-blue #15687a/#7cc4d4 — flagged founder call; WCAG AA-proved 5.47–13.2 both themes; zero blast radius: no component consumes accent yet). (3) AssessFlow results phase wraps Results in a display:contents data-corridor carrier from profile.homeCountry (anon activation = results onward; unknown country → no attribute). (4) (app) shell layout wraps chrome+content in the same carrier (signed-in always-on, Phase-2-ready; no DB read added). Server-safe/no-flash by construction — attribute renders from known state, no pre-hydration script. TDD red-first +11 tests incl. NEW permanent guard tests/styles/corridor-tokens.test.ts (accents-only invariant + WCAG re-proof + marketing-sources-never-carry-data-corridor scan). Registry omits copy-nods until a consumer exists. First consumers: spec-MV-94 results accents, Phase-2 shell, mascot variant resolution._
- **MV-97** · P1 · [Overhaul Phase 1: wizard funnel — CGPA step-3 input, AUD step-7 corridor, English equivalents, gap preview, direction-aware slide + word-cascade recap](cards/MV-97-wizard-funnel-overhaul.md) — _Carved from the MV-87 umbrella wizard slice. Step 3 gains a Percentage/GPA-4.0 grade-system toggle (new lib/scoring/grade-scale.ts converts across scales via the engine normalizer; honest percentage-equivalent caption; scale-aware slider) — CGPA flows end-to-end since scoring already normalizes CGPA to a percentage. Step 7 replaces USD with AUD (NPR to AUD, live A$ conversion from the single fx-rates source, indicative NPR-per-AUD caption, persisted-USD sessions auto-migrate; new formatAud). English step shows the live IELTS equivalent for PTE/TOEFL plus a provisional-6.0 note. Steps 5/6 get a positive gap-preview note so the conditional gap screen is never a surprise. Motion: direction-aware slide-fwd/slide-back between steps plus a staggered step-shell reveal; post-wizard recap assembles word-by-word (reduced-motion safe, no fake analyzing text). Opacity/transform only per the Motion v2 ADR. TDD +16 across education/budget/english/graduation/use-wizard-state/recap. DEFERRED to their own slices: step-4 multi-subject (single-select enum to array is a data-model change touching Zod/scoring/matches/persistence), matches-page progressive disclosure, footer CLS/perf._
- **MV-98** · P1 · [Footer-first CLS fix + perf: pin footer (min-h-dvh flex), reserve loading height, lazy-load posthog](cards/MV-98-layout-cls-perf.md) — _Fixes the founder-reported footer-first flash. Root cause: route-group shells put <main> and <Footer> as plain siblings with no min-height / full-height column, so during streaming the short loading.tsx fallback let the footer paint high, then the taller real page shoved it down. Fix: (app)/(marketing)/(focused) layouts become flex min-h-dvh flex-col with a flex-1 <main> (footer pinned to viewport bottom); (app)/(marketing) loading skeletons reserve min-h-[60vh]; analytics-provider lazy-loads posthog via dynamic import inside the effect after the key check (off the initial bundle for anon visitors). Corridor/data-corridor, AppBar, MobileTabBar, auth redirects preserved. New source-level ratchet tests/styles/layout-cls-ratchet.test.ts. Deferred: duplicate auth.getUser() dedup (auth-touching, own slice); browser-verify the streaming no-jump under throttled network._
- **MV-81** · P1 · [CI integration job env mapping fix (renamed Supabase CLI status output)](cards/MV-81-ci-integration-env-fix.md) — _The integration job's 'Export Supabase test env' step has failed on every run for ~10 days (run 28576388321: both SUPABASE_TEST_URL/SUPABASE_TEST_SERVICE_ROLE_KEY empty in the smoke step's env) because current Supabase CLI versions emit unprefixed names (API_URL, SERVICE_ROLE_KEY, SECRET_KEY) in `supabase status -o env` output that the old fallback chains never checked. Fixed by extending both fallback chains to include the unprefixed names and adding a fail-fast guard that exits 1 with a names-only diagnostic (never values) when the mapping still misses, so a future CLI rename is a one-glance fix instead of a silent empty-env failure two steps downstream. Only the one step + its comment touched; continue-on-error preserved. Verification is the PR's own Actions run reaching the smoke step with non-empty env._
- **MV-103** · P2 · [Persistent "where am I" journey marker in the signed-in chrome (MV-45 #3b)](cards/MV-103-journey-marker.md) — _BUILT 2026-07-05 on branch mv-103-journey-marker off origin/master (founder merges to master to close -> Done). Second half of docs/superpowers/specs/2026-06-28-global-journey-rail-design.md: MV-77 shipped the six-step Your journey rail as a DASHBOARD panel, but the orientation disappears on /matches, /plan, /documents -- exactly where what-next nudges toward a consultancy. Added a slim persistent marker in the (app) chrome under the AppBar: mini six-dot rail + current-label . step N of 6 + chevron, one link to /dashboard, returns null on /dashboard (usePathname) where the rich rail already lives; dots hide below md, text carries. New lib/journey/signals.ts getJourneySignals reuses the SAME six repos + deriveJourneySignals as the dashboard so marker and rail can never disagree and trust predicates stay in one place; layout wraps it in try/catch so a signals failure degrades to no marker, never a broken page. Reuses buildJourney verbatim + shipped strong-token dot vocabulary (reconciled from the older teal spec); never reach-red. TDD RED->GREEN +8 (6 component + 2 layout incl. degrade-on-failure); existing 4 layout tests untouched. Gate green: typecheck clean, lint 0 errors (1 pre-existing build.mjs warning), full vitest 273 files / 1744 tests (was 271 / 1736). No scorer/DB/migration; no golden surface. Founder-owed: merge PR -> Done; visual spacing is a blind call -- signed-in shell is auth-gated, cannot browser-verify headlessly._
- **MV-104** · P2 · [Wizard funnel follow-ups (founder testing): AUD living-cost callout + recap loading slowdown](cards/MV-104-wizard-funnel-fixes.md) — _Two defects the founder hit in local testing after MV-97 landed. (A) STEP-7 CALLOUT: warned Australian living costs alone are ~USD 14k-22k/yr and its threshold divided any non-USD budget by 135 (the NPR rate), so a post-MV-97 AUD budget was read as NPR and the warn mis-fired for EVERY Australia-bound student. Rewired to the canonical toAud() converter + the sourced AU_DHA_LIVING_CAPACITY_AUD figure (A$29,710/yr, DHA Subclass 500, findingRefs A.015/B.002); message and threshold now reference the same sourced number. (C) LOADING TOO FAST: re-landed the mv-97 8307ea9 intent onto master (durationMs 600->2000, per-word stagger 0.04->0.07 capped 0.5s->1.2s). TDD +3 callout tests + updated recap timing. No scorer/DB/migration; no golden surface._
- **MV-105** · P3 · [Wizard step 8: multi-select goals (primary + secondary, e.g. PR + high-ranked uni)](cards/MV-105-step8-multi-goal.md) — _Founder wants step 8 (goal) to allow more than one motivation. Layer A (BUILDING): additive secondaryGoals?: Goal[] beside the unchanged primary goal -- pure reconcile/toggle helpers (cap 2), Zod cap+disjoint refine, two-zone wizard step, recap 'also aiming for' line, profile-editor parity, from-assessment/from-sections wiring, all TDD. TRUST FINDING (Codex read the code): goal drives NEITHER verdict (engine.ts) NOR plan (generator.ts) today -- only preference-fit reads it, and only 3 of 6 goals have program signals. So scoring stays inert (scoring-inert test = the honesty guarantee) and no copy claims a goal-specific verdict/plan; the picker is lightly inert (captured + shown in recap, does not change matches yet) and microcopy says so. Layer B (trade-off callout + matches/results surfacing + reorder-vs-contextualise) DEFERRED to a UI/UX session._
- **MV-106** · P2 · [Overhaul Phase 1: apply the type scale (text-[Npx] -> named 9-step tokens)](cards/MV-106-type-scale-apply.md) — _Wave A step 1 of the elevated-calm overhaul continuation. Applies docs/design/2026-07-03-type-scale-adr.md (MV-88 shipped the DOC only; this is the application the ADR deferred). Added the 9-step scale (caption 11 .. display 24) to @theme in app/globals.css; migrated 414 text-[Npx] occurrences across 109 files to text-<token> via a nearest-step snap map -- 262 pure renames; the rest snap <=2px staying within role, incl. 125 post-ADR fractional 11.5/12.5 sites de-jittered 0.5px to caption/small. Required lib/utils.ts cn fix: registered the scale in tailwind-merge's font-size group (extendTailwindMerge) so bare-word size tokens don't collide with text-<colour> and get silently dropped inside cn() -- a real regression the naive rename would have shipped, caught by the VerdictPill exact-string test. Net rendered change = ONLY the intended pixel snaps (the cn fix restores pre-codemod output). TDD: new type-scale ratchet (0 text-[Npx] left + tokens defined) + cn guard + updated button/verdict-pill assertions. Founder blind-call flagged: anchor values shipped verbatim from the ADR and are a one-line @theme retune at review; the 125 fractional 0.5px snaps land on auth-gated screens._
- **MV-107** · P2 · [Overhaul A2: motion duration tokens (--transition-duration-* ladder) + fence transition-all / JS-anim-lib bans](cards/MV-107-motion-duration-tokens.md) — _Wave A step 2 of the elevated-calm overhaul (after A1 type scale, MV-106). Off the Motion v2 ADR (docs/design/2026-07-03-motion-v2-adr.md): (1) added a named duration ladder to @theme -- --transition-duration-fast 150 / medium 200 / slow 300 / slower 700; Tailwind v4 generates duration-<name> from the --transition-duration-* namespace (the ADR --dur-* placeholder was dropped -- it does not generate a utility, verified via standalone compile). (2) Migrated all 18 raw duration-N sites to the tokens 1:1 (pure rename, zero rendered change). (3) Fenced the ADR bans: a new ratchet locks zero transition-all + zero raw duration + no JS-anim dependency; eslint no-restricted-imports bans framer-motion/motion/gsap, with the ADR narrow escape hatch served by an explicit reviewed eslint-disable. TDD red-first (2 fail -> 4 pass). ADR flipped Proposed->Adopted; open Q2 (2nd easing) + Q3 (slide keyframes) remain. Token names are one-line-tunable at review._
- **MV-108** · P2 · [Overhaul A3: landing (marketing) non-mascot restyle -- ISO pills + two-beat hero + per-section reveals + CTA micro-interaction](cards/MV-108-landing-restyle.md) — _Wave A step 3 of the elevated-calm overhaul (after A1 type scale MV-106 + A2 motion tokens MV-107). Ships the NON-MASCOT half of the spec's MV-92 (Landing); the hero mascot mark stays deferred (Gate G: MV-85 + MV-86 + hand-traced SVGs). (1) New shared primitive components/ui/iso-pill.tsx (bordered mono ISO-code pill, aria-hidden) replaces flag emoji on marketing/destinations surfaces -- absorbs the killed MV-49 flag-pill half; imagery policy bans flag iconography on marketing. Added explicit iso field to MarketingDestination (all 6 rows; uk->GB, the ISO-3166 alpha-2 for the UK, not a naive id.toUpperCase). (2) Two-beat hero entrance (animate-rise beat 1, animate-settle beat 2, animate-fade preview) -- reduced-motion-safe WITHOUT touching the shared guard (settle bakes its hold into keyframe phases; the guard zeroes duration but not delay, so a raw delay stagger would flash). (3) One mount-based reveal per below-fold section (NOT IntersectionObserver -- no-JS/SEO/reduced-motion safe). (4) Hero + TrustCallout CTAs stay semantic Links (no link->button a11y regression; Button polymorphism is MV-89's lane) but gain the Button press micro-interaction. Eyebrow gained an optional className (cn-merged) so the hero eyebrow can animate; other call sites unaffected. TDD: +IsoPill unit test, +no-marketing-flag-emoji ratchet (scoped to components/marketing + components/destinations). No existing tests needed changes. Corridor-neutral brand already satisfied (plum = neutral global brand pre-onboarding)._
- **MV-109** · P2 · [Overhaul A4: auth (sign-in) non-mascot restyle -- two-beat entrance (rise header + settle card), reduced-motion-safe](cards/MV-109-auth-restyle.md) — _Wave A step 4 of the elevated-calm overhaul (after A1 MV-106 + A2 MV-107 + A3 MV-108). Ships the NON-MASCOT half of the spec's MV-95 (Auth); the 160px mascot welcome mark stays deferred (Gate G: MV-85 + MV-86 + hand-traced SVGs) -- the neutral 44px plum diploma mark stands. Most of MV-95 was already met: Card primitive (auth-card already wraps the block in Card padding=lg, default radius=panel 16px), type scale (lede text-lead, privacy text-small, email fallback text-meta already tokenised; the h1 keeps text-[clamp(28px,3.4vw,38px)] -- a hero display heading, deliberately an arbitrary clamp like the A3 landing headings, since the 9-step scale tops out at display 24 and is for UI/body text), honest email fallback preserved (test-guarded). The only net-new was entrance rise: animate-rise on the header block (beat 1) + animate-settle on the Card (beat 2), the same two-beat A3 gave the landing. Reduced-motion-safe WITHOUT touching the shared guard -- both rise and settle are keyframe-baked (no raw animation-delay); the guard zeroes duration but not delay, so settle's baked hold sequences beat 2 without a flash. The Google CTA press micro-interaction is already served by the Button primitive's base recipe. Two classNames + 1 TDD test (asserts header rise + card settle); existing 3 tests unchanged and green (honest-fallback trust behaviour re-verified). Corridor-neutral brand already satisfied (auth is pre-corridor; mark uses bg-primary plum = neutral global brand)._
- **MV-110** · P2 · [Overhaul Phase 2: dashboard shell motion restyle -- entrance reveals + calm dot transitions (non-mascot MV-97)](cards/MV-110-dashboard-restyle.md) — _Phase-2 kickoff (after Phase-1 funnel overhaul finished: A1 type MV-106 / A2 motion MV-107 / A3 landing MV-108 / A4 auth MV-109). Ships the NON-MASCOT core of spec MV-97 (Dashboard). A ground-truth pass found most of MV-97 already shipped by prior slices: all 5 components/dashboard/* already adopt <Card> (MV-84), use design tokens + zero text-[Npx] (MV-106), and snapshot-card delegates its verdict to VerdictCard -> shared <VerdictPill> (MV-89); readiness-map bands already tokenised. The genuine delta = the missing MOTION vocabulary: (1) one animate-rise entrance per card/section root across greeting/journey-rail/prompt-card/readiness-map/snapshot-card -- reduced-motion-safe (NO per-child animationDelay stagger; the guard zeroes duration not delay so a raw stagger flashes); (2) calm dot state transitions on journey-rail (transition-[bg,border,shadow] duration-medium ease-calm) + a missing duration-medium token on the stage Link. Presentational only -- no data/logic/behavior change; no use client added. FLAGGED no-change (founder calls): readiness-map band colours (already tokenised, no legacy hex) + greeting hero clamp text-[clamp(28px,3.6vw,40px)] (legal >24px). Mascot milestone-beat + corridor accents DEFERRED (Gate G / own follow-up). TDD: extended 4 dashboard tests with entrance assertions + NEW journey-rail.test.tsx (4 tests). Not browser-verified (dashboard auth-gated) -- suite + 3 style ratchets, per prior dashboard slices. Remaining Phase-2 non-mascot: MV-99 profile, MV-100 guide, MV-101 chrome._
- **MV-125** · P2 · [Overhaul Phase 2: profile shell restyle -- completeness-ring entrance reveal (ring/accordion/type-scale already shipped) (non-mascot MV-99)](cards/MV-125-profile-restyle.md) — _Phase-2 continuation after the dashboard slice (MV-110). Ships the NON-MASCOT portion of spec MV-99 (Profile). A ground-truth pass found ALL THREE spec checklist items already shipped by prior slices: (1) CompletenessRing already tokenised text-primary (dusk-plum, MV-84) with transition-[stroke-dashoffset] duration-slower ease-calm -- nothing to re-tint, zero raw hex in components/profile/**; (2) section-accordion already folds onto the shared components/ui/disclosure.tsx primitive (chevron transition-transform duration-medium ease-calm + rotate-90, from Audit #21); (3) editors already on the type scale (text-[ grep NONE; MV-106 ratchet cleared all text-[Npx]). The genuine delta = entrance-reveal PARITY with the dashboard: added ONE animate-rise to the CompletenessRing <Card> shell root (the single stable root in components/profile/** that mounts once on navigation). Reduced-motion-safe (one reveal per root, no per-child animationDelay stagger; SectionAccordion deliberately NOT given animate-rise -- 13 rows would be a multi-sibling stagger). Presentational only -- no data/logic/save-flow change; no use client added. FLAGGED no-change (founder calls): ring text-primary tint (deliberate token); accordion-list container reveal lives in app/(app)/profile/page.tsx (out of components/profile/** scope) = a deferred one-line touch. TDD: extended completeness-ring.test (reveal + tint/dashoffset preservation) + NEW section-accordion guard (stays on the disclosure primitive's tokenised transition). Not browser-verified (profile auth-gated) -- suite + 3 style ratchets, per prior slices. Remaining Phase-2 non-mascot: MV-100 guide, MV-101 chrome._
- **MV-126** · P2 · [Overhaul Phase 2: guide shell restyle -- message-entrance fades + mono typing indicator (mascot avatar + 503 pose DEFERRED, Gate-G) (non-mascot MV-100)](cards/MV-126-guide-restyle.md) — _Phase-2 continuation after the profile slice (MV-99). Ships the NON-MASCOT portion of spec MV-100 (Guide). Two of the four spec items are MASCOT and Gate-G-blocked (head-mark avatar 24/32 on assistant messages + 503 sheltering pose -- both need MV-85 brief + MV-86 imagery-policy + hand-traced SVGs, DEFERRED to the mascot tail). Ground-truth on the two non-mascot items: (1) message entrance fades NOT shipped -> built (prepended animate-fade to the message <li> bubble className in guide-chat.tsx; each <li> mounts independently so no per-child animationDelay stagger -- reduced-motion-safe); (2) calm typing indicator was ALREADY calm/non-bouncing (plain 'The guide is thinking...' span, no animate-bounce, no raw duration) -- added font-mono so it renders in IBM Plex Mono, meeting the spec's 'mono ellipsis' letter. Presentational only -- no data/logic/streaming/503/Zod change; use client already present. FLAGGED (visual judgment call): font-mono on the typing indicator satisfies the spec's exact wording; one-class revert if the founder prefers byte-identical. TDD: +2 guide-chat tests (bubbles carry animate-fade w/ bounce/pulse/raw-duration guards; indicator carries font-mono, no forbidden motion). Not browser-verified (guide auth-gated + 503 until DEEPSEEK_API_KEY) -- suite + 3 style ratchets, per prior slices. Remaining Phase-2 non-mascot: MV-101 chrome/cleanup (the last one)._
- **MV-127** · P2 · [Overhaul Phase 2: chrome + cleanup -- mobile-tab-bar active-accent transition + calm logo hover + delete 5 unused scaffold SVGs (opengraph-image DEFERRED, Gate-G) -- the LAST non-mascot slice](cards/MV-127-chrome-cleanup.md) — _Phase-2 continuation after the guide slice (MV-100); the LAST non-mascot Phase-2 slice. Ships spec MV-101 (Chrome + cleanup). Ground-truth: (1) wordmark/logo ALREADY refreshed to dusk-plum tokens + type scale via MV-84 -- a new mark is Gate-G brand-asset work, so only a calm hover affordance added (transition-opacity duration-fast ease-calm hover:opacity-80 on the Link); (2) mobile-tab-bar had transition-colors ease-calm but no duration token + no active indicator -> added duration-fast timing + an always-present aria-hidden accent bar (h-0.5 bg-primary, transition-opacity, opacity-100/opacity-0 toggle) that FADES on the active tab (reduced-motion-safe transition, not a delay stagger); (3) deleted 5 verified-unreferenced create-next-app scaffold SVGs (file/globe/next/vercel/window). DEFERRED: opengraph-image.tsx (optional + outward-facing brand asset, founder-owned Gate-G). Presentational only -- no data/logic/nav change. FLAGGED (visual judgment calls): logo hover:opacity-80 + the 2px top accent-bar placement (both one-class reverts). TDD: +4 tests (tab-bar duration token + active/inactive accent-bar opacity + motion ratchets; logo hover classes + structure preserved). Not browser-verified (auth-gated chrome) -- suite + ratchets, per prior slices. After MV-101 the overhaul has ONLY the Gate-G mascot tail left (MV-85/86 + SVGs)._
- **MV-111** · P1 · [MV-105 Layer B — honest goal trade-off note (Option 1)](cards/MV-111.md) — _DONE. Turns the inert Layer A secondaryGoals capture into ONE honest, score-inert + order-inert trade-off note when the primary goal and a secondary goal pull in different directions. New pure lib/goals/conflicts.ts goalTradeoffNote() mirrors lib/scoring/field-note.ts competitivenessNote (returns at most one note from an ORDERED tension table); presentational components/matches/goal-tradeoff-note.tsx (sibling of PreferenceNote, never a modal); carried on the anon AssessmentPayload (assemble.ts + results.tsx) + computed inline on signed-in matches (page.tsx). v1 shipped 2 framing-only tension pairs (permanent-residency<->highest-ranked > lowest-cost<->highest-ranked) that assert no external fact; fastest-admission<->research + synergy pairs + best-employment deliberately DEFERRED to a sourced follow-up. Inertness is structural (applyPreference never receives secondaries) + guarded by the untouched secondary-goals-inert test. Plan: docs/superpowers/plans/2026-07-07-goal-tradeoff-note.md. Dossier: cards/MV-111.md._
- **MV-112** · P1 · [Landing page redesign — v7 Cursor cadence](cards/MV-112.md) — _Rebuild the signed-out marketing landing (app/(marketing)/page.tsx) in the premium v7 'Cursor cadence' style: confident hero + alternating product sections each anchored by ONE real interactive artifact (live verdict panel -> plan-step accordion -> documents checklist -> autoplay guide typewriter -> freshness verify-sweep table -> sparkle close). New client islands components/marketing/* (verdict-panel, plan-steps, documents-checklist, guide-thread, freshness-table, sparkle-cta, reveal, hero-marker) + typed data lib/marketing/* (sample-profiles, plan-steps, checklist-items, guide-answers, freshness-rows); delete superseded hero-preview/how-it-works/tile/trust-callout + drop TrustStrip from the page. LOCKED invariants: server-rendered FILLED rest state (not the reference's JS-builds-empty), hydration parity (no matchMedia/IO/rAF/Math.random in render), honesty split (sample cost = no verified citation; kind:'sample'|'sourced'; freshness provenance visible at rest), Genuine Student (GS) terminology, no href=# (CTAs -> /assess, Sign in -> /auth), preserve getUser()->/dashboard redirect, no em-dashes, both themes, 1160px. Presentational only (no scoring/API/DB/Zod). Spec: docs/superpowers/specs/2026-07-08-landing-redesign-design.md. Reference: docs/superpowers/specs/assets/2026-07-08-landing-v7-reference.html. Dossier: cards/MV-112.md._
- **MV-113** · P1 · [Hotfix — MV-112 landing layout bugs (reveal stuck hidden + verdict bar inline)](cards/MV-113.md) — _Post-merge live visual check of the MV-112 landing found it badly broken in production: every scroll-reveal-wrapped section artifact was invisible and the verdict meter bars overlapped/clipped. Two CSS/class-contract bugs the no-layout-engine jsdom suite passed clean. Fix 1 (reveal): components/marketing/reveal.tsx named its pre-reveal phase 'hidden', which collides with Tailwind's global utility .hidden{display:none}; the scoped .mv-reveal.hidden rule only set opacity, so Tailwind's display:none won -> the element left layout -> IntersectionObserver could never report it intersecting -> stuck hidden forever. Renamed the phase token to 'off' (scoped .mv-reveal.off = opacity:0 + translateY(18px), keeps the box in layout) in reveal.tsx and app/(marketing)/landing.css. Fix 2 (bar): verdict-panel.tsx renders the dim meter .bar as a <span> (inline by default) so its height:6px was ignored and it grew to ~47px line-box height, overlapping the next row; added display:block to .mv-landing .bar. Both verified live in the preview (hero, verdict panel, plan accordion, checklist, guide thread, freshness table, close CTA all render). Dossier: cards/MV-113.md._
- **MV-114** · P2 · [Hotfix — verdict-panel toggle spacing + instant pill](cards/MV-114.md) — _Follow-up polish on the MV-112 landing after user-reported 'buttons aren't spaced properly and the animation is laggy' on the Your-assessment profile toggle. Two issues jsdom (no layout engine) missed. Spacing: the .toggle segmented control had padding but no gap between the two .toggle-opt pills, so the fully-rounded (border-radius:999px) pills touched and the active plum pill read as cramped; fixed with .mv-landing .toggle{gap:3px} in app/(marketing)/landing.css. Lag: components/marketing/verdict-panel.tsx's select() delayed setActiveId by 150ms and activeId drove both the pill highlight AND the rendered profile content, so the pill lagged the click; introduced a separate selectedId state (set synchronously on click) for the pill + radio checked, leaving activeId for the delayed content crossfade — matches the v7 reference where aria-pressed flips instantly and only render() is delayed. Added a swapRef timer cleared on re-tap and unmount to prevent flicker now that the pill is instant. Presentational only. Dossier: cards/MV-114.md._
- **MV-115** · P2 · [Signed-in chrome inside the min-h-dvh column (footer pins, no dead scrollbar)](cards/MV-115-app-chrome-column.md) — _Audit finding #1 (docs/audits/2026-07-08-jsdom-blind-audit.md, Tier 1, CONFIRMED medium, bug class G). The (app) layout wrapped the chrome in a display:contents div (boxless corridor-token carrier) and placed AppBar (~66px) + JourneyMarker (~37px) as siblings ABOVE the min-h-dvh flex column that holds main + Footer — so those two chrome elements became flow children of <body> sitting above the 100dvh column, making the document always ~100dvh + ~103px tall: a persistent dead scrollbar on every short signed-in page (empty states) and on the streamed loading.tsx fallback during every route transition, with the footer below the fold. Marketing/focused layouts put the header inside the column (correct); (app) had regressed the MV-98 footer-pin invariant while keeping its comment. Fix: move AppBar + JourneyMarker inside the flex min-h-dvh flex-col column before <main>, mirroring app/(marketing)/layout.tsx. MobileTabBar (position:fixed, no flow height) stays a sibling; the contents corridor scope still wraps everything so the data-corridor accent hook is unchanged. Structure-contract guard added to tests/app/app-layout.test.tsx (the parent of main contains footer + appbar + journey-marker), RED on the pre-fix tree → GREEN. Presentational/structural only — no scoring/API/DB/Zod; goldens untouched. Dossier: cards/MV-115-app-chrome-column.md._
- **MV-116** · P2 · [Disclosure trigger focus ring visible under overflow-hidden (WCAG 2.4.7)](cards/MV-116-disclosure-focus-ring.md) — _Audit finding #2 (docs/audits/2026-07-08-jsdom-blind-audit.md, Tier 2, CONFIRMED medium, bug class E). Disclosure wraps its trigger + panel in <Card overflow-hidden>; Card defaults to rounded-lg + border + no padding and the trigger button is w-full with px-5 py-4, so the button box is flush with the Card's clip box. The global focus ring (globals.css:242) is outline:2px solid primary; outline-offset:2px — painted 2px OUTSIDE the box — so overflow-hidden clipped it away and every collapsed profile section header (8 groups via section-accordion.tsx) showed no visible keyboard focus outline. overflow-hidden is load-bearing (clips the header hover:bg-bg-tint to the card's rounded top corners), so it can't be dropped. Fix: inset the ring on the trigger only — append focus-visible:[outline-offset:-2px] so it paints 2px inside the box, within the clip; the global rule's border-radius:inherit makes the inset outline inherit the card's 16px radius (concentric). Class-contract guard in tests/components/disclosure.test.tsx asserts the trigger carries the inset utility (RED->GREEN). Presentational only — no scoring/API/DB/Zod; goldens untouched. Dossier: cards/MV-116-disclosure-focus-ring.md._
- **MV-117** · P2 · [Plan + freshness accordions animate open (kill the native-<details> snap)](cards/MV-117-accordion-animate.md) — _Audit finding #5 (docs/audits/2026-07-08-jsdom-blind-audit.md, Tier 3, CONFIRMED medium, bug class G) — the last remaining landing medium. Both landing accordions used native <details>/<summary> with a grid-template-rows:0fr->1fr ease on the collapsible child (landing.css .step-detail / .fdetail). A closed native <details> display:none's its non-summary content, so the grid-rows transition has no prior laid-out frame to interpolate from — the body snaps open in one frame while the chevron rotates over 0.3s (visible mismatch on the landing's primary interaction). verdict-panel's .dim (a <div> + JS .open class, detail always rendered) animates, proving intent. Fix: drive both expands with a JS .open class. plan-steps.tsx became a client component (single-open, openN seeded from the data's open step 02, <button className=step-head aria-expanded>); freshness-table.tsx uses an openRows Set (multi-open), keeping the IO lit sweep + verified/vdot untouched (finding #6 is a separate slice); landing.css three details[open] selectors -> class-based .step.open/.fitem.open (grid-rows + chevron), and removed the ::-webkit-details-marker + list-style:none rules orphaned by summary->button. .step-detail/.fdetail are now always display:grid (0fr at rest) so the transition interpolates; buttons inherit font/colour via Tailwind preflight (like .dim-head) and get the existing .mv-landing button:focus-visible ring; the open step is server-rendered filled (useState initial from static data, no window/clock in render), so MV-112 FILLED-rest + hydration invariants hold. Presentational only — no scoring/API/DB/Zod; goldens untouched. Dossier: cards/MV-117-accordion-animate.md._
- **MV-118** · P1 · [Hydration hardening — stop reading window/clock in render (assess-flow, wizard, conversion-paths expiry trust bug, intake)](cards/MV-118.md) — _Fourth jsdom-blind slice: move impure render-body reads of window/the clock to the server (compute into the payload) or behind a mount effect, on the anonymous assess->results trust paths where SSR->hydrate mismatches are invisible to the no-layout-engine jsdom suite. #3 components/assess/assess-flow.tsx readPersistedResults() (reads window.sessionStorage) seeds useState(phase) in render -> SSR phase='wizard' vs client 'results' whole-subtree mismatch + wizard flash on the MV-28 recovery path; #7 components/wizard/use-wizard-state.ts readPersistedWizard() same pattern (step-1 flash) folded in; #4 components/results/conversion-paths.tsx expiryDate() reads new Date() + formats in render (UTC vs Asia/Kathmandu TZ mismatch) AND the adjacent TRUST bug — expiry derived from the live clock at view time not the stored creation timestamp, so the recovery route always shows '3 days from now' regardless of real age; fix derives expiry from the stored creation timestamp (server-computed, threaded via props, 3-day product window); #11 components/results/intake-timing.tsx buildIntakeTimeline default now=new Date() -> inline left:% float mismatch, fix computes offsetPct server-side. HARD CONSTRAINT: must not regress MV-28 (change WHEN restore happens, not WHETHER). Guards assert the SSR-stable / prop-derived contract jsdom can see. Dossier: cards/MV-118.md._
- **MV-119** · P2 · [Cosmetic hardening — rail dot / tab jitter / freshness pulse / guide cleanup](cards/MV-119.md) — _The fifth jsdom-blind slice (audit #8/#9/#6/#10): four cosmetic defects a green ~1900-test jsdom suite cannot catch (no layout engine, no real animation/scroll timers). #8 components/dashboard/journey-rail.tsx: the baseline connector was top-[13px] but each dot centre sits at 9px (py-1 + half h-2.5) -> top-[9px] (matches the outcome-funnel top-[5px] pattern). #9 components/matches/matches-tabs.tsx: the active tab's font-medium made it wider so the tablist reflowed on every switch; keep the bold active cue but reserve the bold width on every tab with a hidden aria-hidden font-medium ghost grid-stacked under the visible label. #6 components/marketing/freshness-table.tsx: verified was hardcoded at mount, firing all five vdot vpulses below the fold; drive it from the on-scroll IO sweep so each pulse fires in view, with reduced-motion/no-IO applying the static end-state at once (provenance text unchanged, no-JS-safe). #10 components/marketing/guide-thread.tsx: the IO cleanup did not bump runId.current, so the in-flight autoplay typewriter kept scheduling setState after unmount; bump it in cleanup. Presentational only, no trust dimension. Dossier: cards/MV-119.md._
- **MV-128** · P2 · [46 dossiers store a stale Column: field the README already forbids (board state living outside board.json)](cards/MV-128-dossier-column-drift.md) — _Found while building MV-123. README anti-drift rule 1 says card state lives ONLY in board.json and spells out the consequence: "This is why dossiers have no Column: field." Reality never matched: 59 dossiers carry a **Column:** line and 49 DISAGREE with board.json, every one claiming In Review/In Progress/Backlog for work board.json records as done. MV-123 fixed 3 (the renamed MV-125/126/127); 46 remain. Why it matters: a second, stale copy of state is how an agent reaches a wrong conclusion confidently -- it nearly happened during MV-123, where both renamed dossiers said "In review" for work merged 2026-07-07 and only a git merge-base check settled it. Fix: strip the field from all 46, then add the rule to validate.mjs that fails the board if a dossier carries one (the rule is the deliverable; a cleanup without it drifts back). DELIBERATELY sequenced after PR #81: that PR carries dossiers with Column: lines, so landing the rule inside MV-123 would have booby-trapped the founder's own merge._ · [#86](https://github.com/Spooderinbed/merovisa/pull/86) merged, ci passing
- **MV-123** · P2 · [board.json duplicate card ids will corrupt the next batch flip](cards/MV-123-board-duplicate-id-collision.md) — _undefined_
- **MV-122** · P1 · [/trust claims uploads verify your assessment; they do not (C-1c)](cards/MV-122-trust-page-upload-claim.md) — _undefined_ · [#87](https://github.com/Spooderinbed/merovisa/pull/87) merged, ci passing
- **MV-121** · P1 · [An all-Reach student opens an empty matches page (MV-120 fallout)](cards/MV-121-reach-group-collapsed-dead-end.md) — _undefined_
- **MV-120** · P0 · [Matches budget must mean tuition + living costs, not tuition alone (audit C-3) -- the app currently contradicts itself on the same number](cards/MV-120-matches-budget-living-costs.md) — _P0 live decision-correctness bug. The wizard asks for one yearly figure meaning 'Tuition plus living costs' (budget-step.tsx:83-84), but the matches engine compares that whole number against tuitionMin ALONE (compute.ts:81-86), uses tuitionGap===0 as an affordability gate promoting cards to 'strong' (compute.ts:89-95), and prints a green 'Budget covers AUD X tuition.' (compute.ts:139-151). SELF-CONTRADICTION: budget A$45,000 vs tuitionMin A$40,000 -> matches says 'Strong / budget covers', while scoring's financial.ts:80 on the SAME number builds capacityAud = 29,710 living + 44,500 tuition = 74,210 and forces 'reach / well below DHA financial-capacity requirement'. Student is really ~A$24,710 short. Isolated to the matches engine -- financial.ts already models the full floor correctly and even prices dependents, so the sourced constant (AU_DHA_LIVING_CAPACITY_AUD, DHA-sourced + freshness-tracked) already exists; no new data research. FIX: additive userLivingCostAud on MatchInputs (null => today's tuition-only fallback); add costGap to scoreSnapshot and KEEP tuitionGap (persisted untyped in score_snapshot Json on frozen MV-08 predictions -- a rename orphans historical rows); drive the verdict off costGap; rebase the reach cliff on requiredTotal; copy names both components; thread the constant in at BOTH adapters (anonymous + signed-in), keeping it OUT of compute.ts so the engine stays destination-agnostic. LOAD-BEARING: the verdict condition at line 89 is the defect, the copy is the symptom -- a copy-only fix still ships 'Strong'. WARNING: a currently-GREEN test pins the bug (tests/matches/compute.test.ts:60-75 asserts strong for 45,000 vs 40,000); ~13 test files affected -- goldens need reasoned justification, not mechanical re-baselining. Live browser pass required (jsdom is blind to this cross-page consistency claim). Codex plan review in flight._
- **MV-129** · P1 · [English editor caps PTE/TOEFL sub-scores to the IELTS scale (audit C-7)](cards/MV-129-english-editor-scale-cap.md) — _Audit C-7, confirmed uncarded 2026-07-17. Built 'Scoring + input' (founder choice): the live harm was the raw PTE/TOEFL-vs-IELTS SCORING bug (profile-strength.ts over-awarded every non-IELTS taker; callouts/rules.ts never warned them) — routed both through toIeltsEquivalent. Input: the profile editor's overall already scaled per-test, so the fix hides the IELTS-only per-band sub-inputs for PTE/TOEFL (wizard english-step was already correct). Gate green: 298 files/1954 tests, typecheck clean, lint 0 errors._ · [#85](https://github.com/Spooderinbed/merovisa/pull/85) merged, ci passing
- **MV-130** · P1 · [OAuth claim failure has no recovery on /assess (audit C-9)](cards/MV-130-assess-claim-failure-recovery.md) — _Audit C-9, uncarded. app/(focused)/assess/page.tsx reads only ?new and has no ?error handling, so a failed claim/OAuth silently drops the highest-intent user at the conversion moment. Fix: surface the failure honestly plus a retry path (reuse the MV-28 / MV-65 recover-in-place pattern). Distinct from MV-28 (anon results recovery)._ · [#103](https://github.com/Spooderinbed/merovisa/pull/103) merged, ci passing · [#102](https://github.com/Spooderinbed/merovisa/pull/102) merged, ci failing
- **MV-132** · P1 · [FX rates are unsourced and unguarded, and they gate the DHA verdict (audit F-20)](cards/MV-132-fx-rates-source-and-guard.md) — _Audit F-20, uncarded (MV-09 consolidated FX but explicitly deferred source + reverifyBy + volatility). lib/data/policy/fx-rates.ts has hardcoded rates with no source/lastVerified/guard, consumed by financial.ts + from-sections.ts, so it silently gates the budget verdict (MV-120). Violates the CLAUDE.md every-data-point-has-source rule. Fix: source + reverifyBy on each rate, extend the freshness guard (MV-80 pattern), disclose the as-of date._ · [#97](https://github.com/Spooderinbed/merovisa/pull/97) merged, ci passing
- **MV-133** · P1 · [A DB read error renders as no-programs, not an outage (audit read-side empty-state)](cards/MV-133-repo-read-error-empty-state.md) — _Audit section 7 item 18, uncarded. lib/programs/repo.ts returns [] on error at 4 sites (if error-or-no-data return []), so a transient DB/network failure is indistinguishable from a genuinely empty result and the student is told the product has nothing for them. Read-side sibling of MV-02 (which fixed the write-side ok:true swallow). Fix: distinguish failure from empty; render the MV-62 error state on failure._ · [#96](https://github.com/Spooderinbed/merovisa/pull/96) merged, ci passing
- **MV-134** · P1 · [Destinations page markets corridors the product does not support (audit C-11)](cards/MV-134-destinations-false-claim-copy.md) — _Audit C-11 plus the Today stop-live-false-claims P0, uncarded. app/(marketing)/destinations/page.tsx presents six-countries-done-well (plus adjacent overclaims like 485=2-4yr and all-figures-current) while only Nepal to Australia ships, a live production false claim that bounces students to consultancies. SAME class as MV-122, DIFFERENT surface. Founder owns the wording. Grep all marketing surfaces before closing._ · [#131](https://github.com/Spooderinbed/merovisa/pull/131) merged, ci passing
- **MV-135** · P1 · [Anonymous assessments have no purge/deletion path (audit O-2)](cards/MV-135-anonymous-assessment-purge.md) — _Audit O-2 (the audit said card O-1/O-2 as blockers; O-1 rides MV-08, O-2 was uncarded). The 3-day expiry is ACCESS expiry, not deletion; anon data persists unbounded with no purge job and no self-serve delete, a standing privacy/retention liability. Blocked on a founder retention-window decision, then a purge job + delete affordance + honest privacy copy. Distinct from MV-05 (account deletion + policy text)._ · [#99](https://github.com/Spooderinbed/merovisa/pull/99) merged, ci passing
- **MV-139** · P1 · [Wizard asks about prior visa refusals before predicting on them (audit F-1)](cards/MV-139-wizard-prior-refusals.md) — _MV-124 Slice 9 (audit F-1), split off when the slice started. The scoring engine penalises prior visa refusals (one -15, multiple -35, a band drop) and the profile editor collects them, but the anonymous 9-step wizard never asked -> an optimistic anonymous verdict that silently dropped a band the moment a student voluntarily declared the refusal in the editor (the app punished honesty). Founder + Codex both picked the full fix over disclosure-only: added priorRefusals to ProfileSchema (closes the zod-strip risk so the answer reaches scoreVisa), a new FINAL wizard step (explicit selection required, no silent 'none', no-shame framing), and a from-assessment mapping for claim-time symmetry. TDD red->green; wizard step-count goldens updated 8->9 / 9->10 with justification. Gate: typecheck 0, lint 0, 1979 tests/302 files. Live-verified (Step 9 of 9 renders, CTA disabled->enabled on selection)._ · [#88](https://github.com/Spooderinbed/merovisa/pull/88) merged, ci passing
- **MV-142** · P1 · [Closed scholarship application windows read as closed (audit F-19)](cards/MV-142-closed-scholarship-window.md) — _MV-124 Slice 6 (audit F-19), split off when the slice started. The Australia Awards row under Scholarships renders its application window unconditionally in the present tense ("Applications open 1 Feb 2026, close 30 Apr 2026") — but that window closed 2026-04-30, so 79 days later it still tells a Nepal student a shut door is open. Fix (Parts 2+3, ship without founder): inject today into selectScholarships(today); once today >= applicationCloses render "Applications closed 30 Apr 2026 — check DFAT for the next round's dates" (never invent the next intake); ScholarshipsPanel gains an optional today prop defaulting to the real date; a clock-independent display-freshness guard so a passed window can never render present-tense again. Part 1 (a reverifyBy date on the record) stays founder-gated — needs a DFAT next-intake check._ · [#91](https://github.com/Spooderinbed/merovisa/pull/91) merged, ci passing
- **MV-141** · P1 · [Never destroy a document before validating its replacement (audit C-8)](cards/MV-141-upload-atomicity.md) — _MV-124 Slice 3 (audit C-8), split off when the slice started. A document upload that failed validation DESTROYED the original: the route deleted the existing Storage object + row up front (route.ts:65-69), THEN checked magic bytes / uploaded / inserted -- so a renamed HEIC over a stored passport scan returned 'contents do not match' while the passport was already erased, and the vault card still read 'Uploaded' until reload (permanent data loss). Fix: reordered to validate -> upload the replacement to a fresh UUID path -> atomic upsertDocument on the unique (owner,kind) index -> only THEN remove the superseded object. Every failure path (bad magic bytes, failed Storage upload, failed row write) now leaves the original fully intact; new upsertDocument helper closes the delete-then-insert window per the build-order risk note. TDD red->green; gate: typecheck 0, lint 0, 2000 tests/304 files._ · [#90](https://github.com/Spooderinbed/merovisa/pull/90) merged, ci passing
- **MV-140** · P1 · [A match card's reasons must all be true (audit C-10 + C-5)](cards/MV-140-match-reasons-must-be-true.md) — _MV-124 Slice 4 (audit C-10 + C-5), split off when the slice started. Field is a soft sort, not a hard filter (rankByField), so off-field programs surface for everyone. Two honesty defects rode on that: (C-10) the field branch had no else arm, so a pure off-field program carried NO field reason at all -> read as aligned; and 5 of 12 wizard fields have zero catalogue programs (law/arts/hospitality/agriculture/other), so a Law student saw a page of nursing/IT presented as their matches with nothing saying we don't list Law. (C-5) every card's Nepal AL3 line said seasoning was 'expected' -- DHA authority voice contradicting the PolicyBanner's recommendation voice on the same page. Fix: added a truthful field-outside reason kind (else arm); softened the AL3 line to recommendation voice (one edit, both callers, anon-equivalence stays green); a shared uncoveredField(userField, programs) helper derived from the INJECTED catalogue (never a hardcoded list; empty catalogue discloses nothing -- that's MV-133's outage concern) feeding a new calm FieldCoverageNotice on BOTH the anonymous results page and the signed-in matches page. Per MV-124 corrections: C-10 was understated, C-5 overstated (plan/checklist recommendation voice is correct + research-mandated to keep -- left untouched); the soft sort was NOT converted to a hard filter (that trades a dishonest list for an empty page). TDD red->green; gate typecheck 0, lint 0, 1994 tests/304 files (incl. a real signed-in page-render test, not jsdom-blind to the mount). Live pixel pass deferred (verbatim PolicyBanner Card reuse; reason renderer keys off positive only)._ · [#89](https://github.com/Spooderinbed/merovisa/pull/89) merged, ci passing
- **MV-143** · P1 · [Unknown is not zero: abstain instead of fabricating a match verdict (audit C-4 Layer A)](cards/MV-143-unknown-is-not-zero.md) — _MV-124 Slice 5 (audit C-4, LAYER A only — the abstain gate), split off when the slice started. compute.ts floors every unknown verdict input to 0 (userGradePercent ?? 0, userEnglishOverall ?? 0, userBudgetAud ?? 0), so a signed-in student who typed only their name got fabricated "Reach · Grade short by 65%" cards, a frozen MV-08 prediction off those zeros, and a plan seeded off the same invented reach. Fix: new pure lib/matches/sufficiency.ts#hasSufficientInputs (false ONLY when grade + English + budget are all absent; any one present ⇒ still render cards, never over-gate), checked upstream at the matches page (a matches-need-inputs prompt naming the missing inputs), lib/outcomes/freeze.ts (new 422 not-enough-data variant, no zeroed prediction persisted) and lib/plan/invalidate.ts (skip match-driven items). compute.ts + the MatchVerdict union are UNTOUCHED — a real `unknown` verdict band is deferred Layer B. Codex (GPT-5) adversarial review returned fix-then-ship; 4 findings all remediated TDD (auto-close falsely completing add-safer-options on abstain, vague gate copy, a bands-only over-gate premise, overstated comments) — its band-as-presence prescription was correctly rejected (it would reintroduce the fabrication). Gate: typecheck 0, lint 0, full suite green._ · [#92](https://github.com/Spooderinbed/merovisa/pull/92) merged, ci passing
- **MV-144** · P1 · [Honest profile-completeness meter, not a fake accuracy score (audit C-6)](cards/MV-144-honest-completeness-meter.md) — _MV-124 Slice 8 (audit C-6). The old "profile accuracy" meter (lib/results/accuracy.ts) was dishonest three ways: completeness started at 25 and added only +3 for English taken (max 28) while the tiers required Verified>=40 / Complete>=75, so EVERY user was stranded at "Basic" (top tiers mathematically unreachable); the two document suggestions added zero points (dead items that never moved the bar); and it named a tier "Verified" + promised "band-level verification" though the app verifies nothing (nobody reads the uploaded PDF; the engine scores the typed numbers) — a false trust claim, the exact dishonesty the app exists to replace. Founder chose Option A: rebuild as an honest PROFILE COMPLETENESS meter (how much of the picture you've given us, never whether we checked it). New lib/results/completeness.ts#computeProfileCompleteness(profile, docs?): neutral reachable tiers Started(<50)/Detailed(50-99)/Full picture(=100); real weights — grade/English/budget x3 (the verdict-driving inputs the engine otherwise floors/assumes), field of study/target level/prior visa history x1 (denominator 12 on the anon surface, reaches exactly 100 when full); every suggestion raises the bar (no dead items); documents (transcript/financials x1) count ONLY when a docs presence signal is passed (signed-in vault), so they are never dead items on the anon results surface; dependents excluded (undefined = solo, no clean presence signal). New components/results/completeness-meter.tsx replaces accuracy-meter.tsx. The persisted payload key `accuracy` is deliberately kept (now typed ProfileCompleteness) so old stored rows don't crash. Second commit (Codex-reviewed): stale stored payloads — /assessment/[id] and the anon sessionStorage restore replay the stored payload, so pre-fix assessments still rendered the old "Verified" dishonesty; new normalizeStoredProfileCompleteness recomputes legacy/malformed meters from the persisted profile snapshot on read at BOTH sites, current payloads untouched (active users already refreshed by reScoreAssessment). Two separate dependents/financial-capacity bugs Codex surfaced are de-scoped to a follow-up chip. Gate green: typecheck 0, lint 0, goldens byte-identical, 2031 tests / 305 files. Commits 82ebda8 + 0b4a6a2. Dossier: cards/MV-144-honest-completeness-meter.md._ · [#93](https://github.com/Spooderinbed/merovisa/pull/93) merged, ci passing
- **MV-145** · P1 · [Dependents survive "not sure" and account bootstrap (financial-capacity honesty)](cards/MV-145-dependents-financial-capacity.md) — _The two dependents/financial-capacity bugs Codex surfaced while reviewing MV-144 (chip task_9db5afb3), de-scoped from it to keep that slice the meter only. Both corrupt the DHA financial-capacity floor (lib/scoring/financial.ts prices declared dependents via auDependentsCapacityAud), so the verdict is wrong on money. Bug 1: lib/results/assemble.ts resolves destination "not-sure" -> "australia" BEFORE scoring (so the Australia DHA gate applies), but components/wizard/steps/budget-step.tsx gated the family/dependents control on destination === "australia" only — a not-sure student is scored against an Australia floor that silently assumes no dependents. Bug 2: lib/profiles/from-assessment.ts mapped every wizard answer EXCEPT dependents, so on claim / first signed-in assess a declared partner/children was dropped; the signed-in re-score reads dependents back from sections.family via lib/scoring/from-sections.ts#dependentsFromFamily, so the floor silently fell and an under-funded verdict could falsely soften. Fix: show the control for "australia" OR "not-sure"; map wizard dependents -> family section (spouse / spouse-and-kids), guarded so applying-alone omits the section (round-trips through dependentsFromFamily byte-for-byte, no dead alone write). Verified end-to-end: financial.ts genuinely reads dependents; ProfileSchema keeps dependents so profile_snapshot carries it; the mapping is the exact inverse of dependentsFromFamily. No scorer path touched (goldens byte-identical). Gate green: typecheck 0, lint 0, 2035 tests / 305 files (+4). Code commit 9d4aed7. Dossier: cards/MV-145-dependents-financial-capacity.md._ · [#94](https://github.com/Spooderinbed/merovisa/pull/94) merged, ci passing
- **MV-147** · P0 · [Non-Google sign-in: email auth so students without a Google account can save/recover (audit F-6)](cards/MV-147-google-only-auth-gap.md) — _Audit F-6 (P0), flagged in the report as "Not on the board." Sign-in is Google-only (components/auth/auth-card.tsx:38-63 renders "Email sign-in isn't ready yet"); saving, converting, every (app) route, and the ONLY anonymous-recovery path all hard-require a Google account. A student without Google hits a wall at the exact conversion moment — a straight bounce for a consultancy-replacement product. Fix: add a non-Google email path via Supabase Auth (magic link / OTP recommended — password-free), wired into the SAME /auth/callback claim/bootstrap flow Google uses so save/convert/recover behave identically (do not fork the claim path). Supabase Auth sends its own OTP emails, so this does not hard-depend on the app-level transactional email infra F-7 flags absent — only production deliverability does. Founder nod on auth method; coordinate with the F-2 consent/age gate (MV-05). Distinct from F-7 (transactional email) and F-2/MV-05 (privacy/consent)._ · [#98](https://github.com/Spooderinbed/merovisa/pull/98) merged, ci passing
- **MV-148** · P2 · [Board PR enrichment: live PR/CI/review state per card, zero drift](cards/MV-148-board-pr-enrichment.md) — _Multiple parallel Claude sessions each own one card/branch/PR; the board must show PR ownership, CI rollup, review decision, and files-touched per card at a glance. Derived at build time in build.mjs from one gh pr list call, joined by MV-ID in branch/PR names — never written into board.json (anti-drift rule 1). Fail-soft when gh is unavailable; warn-only reconciliation for inreview-without-PR and merged-without-done. Scoped after evaluating and declining mission-control (2026-07-27)._ · [#101](https://github.com/Spooderinbed/merovisa/pull/101) merged, ci passing
- **MV-149** · P1 · [Umbrella: Stage 1 tenancy foundation (MV-150 schema · MV-151 server boundary · MV-152 RLS · MV-153 negative-test gate)](cards/MV-149-stage1-umbrella.md) — _READY (tracking) — umbrella for Stage 1 of the revised consultancy plan (tenancy foundation): organizations/memberships/cases/assignments/invitations/audit_events, the server-only case-permission boundary, case-aware RLS, and the real-Supabase cross-tenant test harness. Carries four slices — MV-150 (additive schema + deny-all RLS), MV-151 (getCaseContext/requireCasePermission + service-role exception list), MV-152 (SECURITY DEFINER RLS policies), MV-153 (negative catalogue + positive matrix harness) — in the DAG 150→{151,152}→153. Stage exit gate is MV-153: the authorization matrix passes positive AND negative real-DB tests under npm run test:integration; a green unit/lint run is NOT sufficient evidence. Not blocked by the Stage 0 legal gate (D-B): Stage 1 runs entirely on seed data, no real student data. Carries two still-open founder decisions as design inputs (deciding nothing): fate of an unclaimed case, and the bounded case-creation→student-notice window._ · [#112](https://github.com/Spooderinbed/merovisa/pull/112) merged, ci passing
- **MV-150** · P1 · [Stage 1 tenancy core schema: six additive tables, deny-all RLS, regenerated types](cards/MV-150-tenancy-core-schema.md) — _Ships one additive migration creating the six Stage-1 tenancy tables (organizations, organization_memberships, cases, case_assignments, invitations, audit_events) per the plan's "New core tables", with the revision's invitation shape (unique token_hash, hash-only, nullable accepted_at/revoked_at/expires_at feeding the downstream single-acceptance CAS) and append-only audit (role-independent immutability trigger + SECURITY DEFINER writer choke point, bare-uuid tombstone columns). All six tables get enable+force RLS, explicit named deny-all policies, and revoke-all-from-client grants — real case-aware policies and grant review are MV-152's. Every FK and every column the Stage-1 RLS helpers will read is indexed (advisor-clean); text+check enums per house convention; lib/supabase/types.ts regenerated. Strictly additive: touches no existing table — the owner NOT NULL relaxation, program_predictions immutability-trigger backfill, predictions→attempts→outcomes composite-FK rebuild, document header/versions replacement, and storage.objects policy retirement from "Known schema obstacles" are all explicit Stage-2 non-goals._ · [#106](https://github.com/Spooderinbed/merovisa/pull/106) merged, ci passing
- **MV-151** · P1 · [Case-permission server boundary (getCaseContext / requireCasePermission) + service-role exception list](cards/MV-151-case-permission-boundary.md) — _Builds the server-only case-permission layer for the consultancy workspace: getCaseContext(actorUserId, caseId), requireCasePermission(actorUserId, caseId, permission), and the role→permission matrix (owner/admin = whole org, counsellor = assigned-only, student = linked-case-only, inactive membership = nothing, role read from the DB never from JWT/metadata). Establishes the enforcement-boundary doctrine — the authenticated client + RLS is the load-bearing tenant boundary and this layer is defense-in-depth — plus a machine-enforced service-role exception list (a registered allowlist + lint/test guard) so no new case-touching path can reach createSupabaseAdminClient unregistered. Unit + behavioral tests against a mocked client only; real-DB positive/negative proof is MV-153's exit gate. Depends on MV-150; consumed by MV-152 and MV-153._ · [#109](https://github.com/Spooderinbed/merovisa/pull/109) merged, ci passing
- **MV-152** · P1 · [Case-aware RLS policies — replace deny-all with role-aware tenant isolation](cards/MV-152-case-aware-rls-policies.md) — _Replaces MV-150's deny-all default policies with real case-aware RLS on all six tenancy tables (organizations, organization_memberships, cases, case_assignments, invitations, audit_events), routing every membership/assignment/case lookup through `private` SECURITY DEFINER STABLE helpers with pinned `search_path = ''` so policies stay non-recursive and plan on MV-150's indexes. Covers org owner/admin full-org access, counsellor assigned-only, student linked-case-only, and inactive-membership = no access; every UPDATE carries USING + WITH CHECK, grants are authenticated-only (anon gets nothing), and audit_events keeps its append-only shape. This is the load-bearing tenant-isolation layer per the plan's enforcement boundary — tested as the authenticated user, never service-role — with an EXPLAIN check proving index usage and once-per-statement helper evaluation, and a clean security+performance advisor pass. P1, agent-owned; blocked by MV-150, coordinates with MV-151's role matrix, consumed by MV-153's exit-gate harness._ · [#108](https://github.com/Spooderinbed/merovisa/pull/108) merged, ci passing
- **MV-153** · P1 · [Cross-tenant negative-test harness + positive authorization matrix (Stage 1 exit gate)](cards/MV-153-cross-tenant-negative-tests.md) — _The Stage 1 exit gate: a real-Supabase integration harness that proves the tenancy authorization matrix both DENIES and GRANTS. Extends the *.itest.ts pattern + localhost hard-guard from tests/integration/anon-purge.itest.ts, but adds the capability the two existing itests lack — per-role AUTHENTICATED clients (anon key + user JWT), because RLS only proves anything when queries run as the actual user; the service-role admin bypasses RLS and is used only for seeding/teardown and for exercising MV-151's service-role exception wrappers. A two-org fixture factory mints 2 orgs x users in every role (owner/admin/counsellor/student) x cases + assignments + student links. Implements the Stage-1 slice of the plan's negative catalogue (cross-org list/read/change/delete deny across all six tenant tables; unassigned-counsellor deny; student cross-case deny; revoked-member immediate loss; role forgery from user/app-metadata rejected; service-role path rejects unauthorized case ids) AND the positive matrix in the same run (an all-deny bug passes every negative test while breaking the product). Wires the CI integration job to export the new anon key and keeps the 0-skipped/>0-passed guard so the suite can never green having tested nothing. Depends on MV-150+151+152. Stage 1 cannot exit until this suite is green as a gating check._ · [#112](https://github.com/Spooderinbed/merovisa/pull/112) merged, ci passing · [#110](https://github.com/Spooderinbed/merovisa/pull/110) merged, ci passing
- **MV-154** · P1 · [Umbrella: Stage 2 case-aware core (MV-155 personal cases + backfill · MV-156 owner-nullable + composite-FK rebase · MV-157 case-aware data access · MV-158 anonymous claim · MV-159 RLS · MV-160 tighten + stage exit)](cards/MV-154-stage2-umbrella.md) — _Umbrella (tracking) for Stage 2 of the revised consultancy plan (case-aware core): additive-migration-sequence steps 2–13, moving the nine student-owned tables (profiles, assessments, plan_items, user_program_state, documents, document_status, program_predictions, application_attempts, outcome_events) off owner-keyed onto case-keyed, and moving the server layer off "actor equals student" onto MV-151's getCaseContext/requireCasePermission. Six slices — MV-155 (personal case per existing owner at migration + nullable case_id + backfill, incl. the program_predictions immutability-trigger obstacle), MV-156 (owner NOT NULL relax on the eight tables + predictions→attempts→outcomes composite-FK rebase onto unique(id, case_id)), MV-157 (repositories/routes onto case authorization + dual-write), MV-158 (the anonymous claim — the plan's "hardest dual-write site"), MV-159 (case-aware RLS on the migrated tables, extending MV-152's SECURITY DEFINER pattern), MV-160 (case_id NOT NULL, drop the compensating checks, prove the exit) — in the DAG 155→156→[157+158, ONE PR]→159→160. MV-157 and MV-158 ship as a single pull request (one branch, separate commits): master IS production and Vercel auto-deploys from it, so "merge but do not deploy" was never executable and merging MV-157 alone opens the owner-set/case_id-null claim window both cards forbid; the combined merge is additionally gated on MV-155's and MV-156's migrations being APPLIED AND RECONCILED on the hosted database, not merely merged. Stage exit gate, verbatim from the plan: "existing students see the same correct data, while case-scoped repositories no longer depend on actor equals student" — proven by MV-160 as STUDENT-VISIBLE PAYLOAD equivalence per user × per domain plus row-identity parity (NOT byte-identical rows, which MV-155's new column, the set_updated_at trigger and MV-156's surrogate keys all make impossible), split into a synthetic CI suite that never skips and a rehearsal-only live replay the integrator runs against a copy. Stage 2 expands NO grant: write is proved on five tables, read on nine, and INSERT on profiles/assessments/plan_items/documents is deferred to Stage 3 with its grant list in specs/2026-08-02-stage2-migration-and-access-matrix.md §6, pinned by a 42501 negative. Service-role retreat is measured as disposition, not as zero: each of the ten legacy-owner-scoped entries is either retired or re-justified with a named later stage. This is the FIRST stage that mutates LIVE user data, so the reversibility doctrine binds every slice: a rehearsal on a copy of live data and a stated rollback with its named point of no return, recorded before any production run. Not blocked by the still-open pilot-consultancy item (Stage 0 exit); IS the hard prerequisite for Stage 3, because a consultancy case has no Auth user and no row can exist for one while owner stays NOT NULL._ · [#114](https://github.com/Spooderinbed/merovisa/pull/114) merged, ci passing · [#113](https://github.com/Spooderinbed/merovisa/pull/113) merged, ci passing
- **MV-155** · P1 · [Stage 2 personal cases + `case_id` columns + owner→case backfill across nine student-owned tables](cards/MV-155-personal-cases-and-backfill.md) — _Stage 2 steps 2-7, and the first card in the whole programme that mutates LIVE user data. Creates one personal case per existing Auth user AT MIGRATION (not first-use), adds a NULLABLE `case_id` to all nine student-owned tables (profiles, assessments, plan_items, user_program_state, documents, document_status, program_predictions, application_attempts, outcome_events), backfills it from the current `owner` relationship, and ships ALL the per-case uniqueness indexes — FULL, not partial on `case_id is not null`, because PostgREST's bare `on_conflict=` cannot infer a partial index and every MV-157 upsert against one would raise 42P10 (NULLs are distinct in a unique index, so the nullable window is safe without the predicate) — alongside the surviving per-owner ones (MV-157 creates none of them and ships no migration; MV-160 drops the legacy ones). Carries the plan's `program_predictions` immutability obstacle as a first-class criterion: `private.reject_prediction_update()` is narrowed to permit exactly a `case_id` null→not-null transition with every other column byte-identical, and MV-160 restores the unconditional body as its last statement. Rewrites the six flat table-level write grants into explicit column lists, with a deliberate ASYMMETRY that is the point of the section: `case_id` is OUT of every UPDATE list (re-pointing a row into another case is the hazard) and IN the five INSERT lists on user_program_state, document_status, program_predictions, application_attempts and outcome_events — without which MV-157's dual-write hits 42501 with no card owning the gap. The two PostgREST UPSERT paths are closed by a SECURITY DEFINER trigger deriving `case_id` from `owner`, so no UPDATE(case_id) grant is needed — qualified `when (new.owner is not null)`, so a consultancy row (owner NULL) keeps the case_id it supplies and is bounded by MV-159's WITH CHECK instead; unqualified, the trigger had nothing to derive from and its overwrite hardening made MV-160's counsellor-write proof unsatisfiable. All three new `private` definer objects carry `revoke all on function … from public` in the same migration, per the Stage 1 discipline — `authenticated` already holds USAGE on `private`. Anonymous (owner-null) assessments stay deliberately case-less and keep purging under MV-135's 3-day policy; the documents header/versions REPLACEMENT, `storage.objects` re-scoping, owner-nullable relaxation, composite-FK rebase, repository migration and case-aware RLS are all explicit non-goals (Stage 4 / MV-156 / MV-157 / MV-159). Ships with a rehearsal on a restored copy of live data and an ordered rollback that expires when MV-157 merges — and is superseded by the stage-level reverse sweep the moment MV-156 applies._ · [#116](https://github.com/Spooderinbed/merovisa/pull/116) merged, ci passing · [#115](https://github.com/Spooderinbed/merovisa/pull/115) merged, ci passing · [#114](https://github.com/Spooderinbed/merovisa/pull/114) merged, ci passing · [#113](https://github.com/Spooderinbed/merovisa/pull/113) merged, ci passing
- **MV-156** · P1 · [Owner nullable on the eight NOT NULL tables + predictions→attempts→outcome_events composite-FK rebase onto the case](cards/MV-156-owner-nullable-composite-fk.md) — _Step 8 of the plan's additive sequence plus two of the six known schema obstacles, in one transactional migration: relax `owner` to nullable on all eight NOT NULL student-owned tables (profiles, plan_items, user_program_state, documents, document_status, program_predictions, application_attempts, outcome_events — `assessments.owner` is already nullable and is not one of them), and re-base the predictions→attempts→outcome_events ownership chain from `unique (id, owner)` composite-FK targets onto `unique (id, case_id)` with re-pointed child keys and covering indexes. This is the hard prerequisite for Stage 3 — a consultancy case has no Auth user, so no row can exist for one while the constraint stands — and it carries two traps the plan's prose does not: `user_program_state` and `document_status` key `owner` in their PRIMARY KEY, so a PK column cannot simply drop NOT NULL (each needs a surrogate `id` PK plus a FULL unique on the legacy pair — `user_program_state_owner_program_idx (owner, program_id)` and `document_status_owner_kind_idx (owner, kind)` — and MV-160 must drop those two as well. CORRECTED 2026-08-03 from `partial unique … where owner is not null`: PostgREST's bare `on_conflict=` emits a plain column list and cannot infer a PARTIAL index, so `setObtained` and `upsertProgramState` — both live today — would raise 42P10 on real requests for the whole MV-156→MV-157 window; the predicate bought nothing because NULLs are distinct in a unique index. Nothing is re-keyed onto `case_id`: a PK column cannot be null and `case_id` stays nullable until MV-160, so the surrogate was the only available shape), and Postgres FKs default to MATCH SIMPLE, so a composite key containing a nullable `case_id` is silently unenforced for null rows — the divergence-is-structurally-impossible invariant the chain was built for is absent precisely during the half-migrated window. Mitigated by a validated `check (owner is not null or case_id is not null)` named `<table>_ownership_axis_present` on ALL EIGHT tables — explicitly NOT `check (case_id is not null)`, which was drafted and rejected because a CHECK is role-independent, service_role does not bypass it, and every live outcomes insert today writes owner and no case_id, so that shape takes the production write path down with 23514 for the whole MV-156→MV-157 window; the accepted consequence is that MV-160's SET NOT NULL gets NO scan-free flip and takes a full scan (negligible at 39 rows). MV-160 drops all eight checks, none survives into Stage 3. Also mitigated by retaining the legacy owner chain alongside the new case chain until MV-160, by ordering `drop not null` last inside one transaction, and by an itest that demonstrates the MATCH SIMPLE hole with the check dropped so nobody removes it without knowing what it buys. Mutates live schema on real user data, so MV-154's rehearsal-on-a-copy and stated rollback apply — the rollback must also DROP the two surrogate `id` columns (restoring the composite PKs alone leaves a third schema shape, not the original), and it is clean only while zero NULL-owner rows exist._ · [#118](https://github.com/Spooderinbed/merovisa/pull/118) merged, ci passing · [#117](https://github.com/Spooderinbed/merovisa/pull/117) merged, ci passing
- **MV-157** · P1 · [Case-aware data access: repositories and routes move off actor-equals-student onto case authorization + `case_id` (dual-write)](cards/MV-157-case-aware-data-access.md) — _Steps 9-10 of the plan's additive migration sequence, and the half of the Stage 2 exit gate that says "case-scoped repositories no longer depend on actor equals student". Moves the student-data repositories (profiles, assessments-read, plan, matches/user_program_state, documents, document_status, outcomes orchestrators) and the 15 routes / 11 Server Components above them off the `.eq("owner", userId)` predicate onto a resolved `case_id` + MV-151's `requireCasePermission`, with one writer helper enforcing the dual-write rule (case_id ALWAYS; owner only where the case has a student_user_id, derived from `cases.student_user_id` and never caller-supplied, so the two can never disagree). Ships NO migration: the case-keyed uniqueness indexes are MV-155 §E's — two cards creating the same indexes means one apply fails or is a silent no-op — so what remains here is the application half, moving every PostgREST `onConflict` target onto MV-155's case-scoped indexes and proving each with a real-DB upsert-twice test. Introduces `lib/cases/personal-case.ts` with `ensurePersonalCase(sessionUser, db?)` taking the VERIFIED Supabase User object and deriving display_name/email internally, so no client-supplied identity string can reach `cases`. Also rewrites every `legacy-owner-scoped` entry in MV-151's service-role exception registry: paths whose grants already allow it (shortlist) flip to the authenticated client and leave the list; the rest keep an entry whose `requiredCaseCheck` now names a real case check, with the flip deferred to Stage 3's grant review or Stage 4 Storage. HARD-BLOCKED BY MV-156 (Done before this card starts — it moves the primary keys this card writes repository code against), and SHIPS IN ONE PULL REQUEST WITH MV-158: master is production and Vercel auto-deploys, so merging this card alone creates the owner-set/case_id-null claim window; the combined merge is gated on MV-155's and MV-156's migrations being applied and reconciled on the hosted database. Preserves the personal student experience byte-for-byte (per-repo and per-route equivalence snapshots); the production-shaped before/after regression and the stage exit are MV-160's. Owner NULLABLE + composite-FK rebase (MV-156), case-aware RLS on these tables (MV-159), and the document header/versions replacement (Stage 4) are explicit non-goals._ · [#120](https://github.com/Spooderinbed/merovisa/pull/120) merged, ci passing · [#119](https://github.com/Spooderinbed/merovisa/pull/119) merged, ci passing · [#118](https://github.com/Spooderinbed/merovisa/pull/118) merged, ci passing
- **MV-158** · P1 · [Anonymous claim under the case model: create-or-resolve a personal case, dual-write owner + case_id atomically, every MV-130 / MV-135 leg preserved](cards/MV-158-anonymous-claim-case-model.md) — _The plan's named "hardest dual-write site" (Known schema obstacles, line 329), carved as its own card because it is the one write path where a partial failure silently destroys a converting student's work — and shipped in ONE PULL REQUEST with MV-157, because master is production and Vercel auto-deploys from it, so merging MV-157 alone opens exactly the window this card closes. Moves the anonymous-assessment claim onto the case model across all four entry points — Google OAuth, the MV-147 emailed 6-digit code, the emailed link, and the signed-in recover-in-place POST /api/assess/claim — so a claim create-or-resolves the claimer's personal case (idempotent, before any write, through MV-157's `ensurePersonalCase(sessionUser)`, which takes the verified Auth User and derives display_name/email internally rather than accepting caller-supplied strings it could never prove came from a session), then binds owner + case_id + claimed_at in ONE conditional statement, because two sequential updates leave an owned-but-case-less row that case-scoped repositories cannot see and the student's assessment vanishes from their own dashboard. Preserves the shipped interactions exactly rather than re-deriving them: MV-130's five honest recovery reasons (already-mine / claimed / expired / error, plus a NEW retryable leg when personal-case resolution fails, which must never render as a false "expired"), MV-135's purge contract (owner is null stays the load-bearing predicate — a claimed-but-case-less row must survive, and anonymous rows stay case-less so the purge index still matches), and the assessments_primary_idx partial unique, which during the dual-key window must be satisfied under BOTH the legacy (owner) and the new (case_id) predicates or the promote fails and the dashboard re-pins to the first assessment ever claimed — the exact MV-16 regression. Also asserts the invariant MV-160's tighten relies on: every successful claim leg leaves owner non-null whenever claimed_at is set. Requires real-DB integration tests (extends tests/integration/claim-path.itest.ts) because a supabase-js mock cannot reject a write a partial unique index rejects; the gate is typecheck · lint · test · test:integration, all four now GATING under the no-bypass master ruleset, run on the combined branch._ · [#120](https://github.com/Spooderinbed/merovisa/pull/120) merged, ci passing
- **MV-159** · P1 · [Case-aware RLS for the migrated student-owned tables (Stage 2 step 11)](cards/MV-159-rls-migrated-tables.md) — _Extends MV-152's RLS discipline from the six tenancy tables to the nine migrated student-owned tables (profiles, assessments, plan_items, user_program_state, documents, document_status, program_predictions, application_attempts, outcome_events), so the database — not just MV-157's repositories — enforces case scoping: `private` SECURITY DEFINER STABLE helpers with pinned empty `search_path` (including a new uncorrelated `actor_case_ids()` for the measured InitPlan shape), USING + WITH CHECK on every mutating policy, `case_id`/`owner` kept out of the column-level UPDATE grant so re-pointing a row into another case is unexpressible, and a grant set unchanged from the POST-MV-155 baseline — captured column-granularly from the migrated stack, never transcribed from a dossier, because MV-155 §H already replaced six flat verbs with column lists and put case_id into the five INSERT lists. This card re-scopes rows; it hands `authenticated` no verb it lacked. That refusal was tested and UPHELD by the integrator against MV-160's original exit criterion, which required counsellor INSERTs on profiles/assessments/plan_items/documents — verbs `authenticated` simply does not hold, so no policy here could have granted them; those four move to Stage 3 with their exact grant list in specs/2026-08-02-stage2-migration-and-access-matrix.md §6, pinned by a 42501 negative in MV-160's suite. Replaces the three `pp/aa/oe` INSERT policies' inline `exists` subqueries (an anti-recursion violation and a silent-denial hazard once peer tables gain policies) with case-parentage helpers, ships the transitional `owner OR case_id` disjunct that keeps not-yet-backfilled rows visible to their real owners until MV-160 drops it, keeps anonymous assessments invisible to every authenticated client, preserves the program_predictions immutability trigger and outcome_events append-only shape, and adds the `to authenticated` clause `documents`' two legacy policies never had (they apply to PUBLIC including anon, and only the absent grant keeps anon out — note they are ALREADY in the `(select auth.uid())` InitPlan form since 20260618120000, so there is no bare-auth.uid() defect to find). Requires the tenant-isolation harness to be EXTENDED — the Stage 1 suite's 429 assertions prove nothing about profiles/assessments/plan today — with student-data fixtures in both orgs, the live-grant completeness guard widened to nine tables, mutation evidence that each new policy's revert turns a named test red, and a visibility rehearsal on a copy of live data proving all 7 real owners see byte-identical row sets. P1, agent-owned; hard-blocked by MV-155 (case_id + backfill + the §H grant baseline) and MV-156 (owner nullable + composite-FK rebase onto unique(id, case_id)); consumed by MV-160._ · [#122](https://github.com/Spooderinbed/merovisa/pull/122) merged, ci passing · [#121](https://github.com/Spooderinbed/merovisa/pull/121) merged, ci passing · [#120](https://github.com/Spooderinbed/merovisa/pull/120) merged, ci passing
- **MV-160** · P1 · [Stage 2 tighten + exit gate: case_id mandatory, compensating checks dropped, before/after data-equivalence proof](cards/MV-160-tighten-stage2-exit.md) — _The Stage 2 exit gate (plan steps 12-13): makes `case_id` NOT NULL on the eight tables whose domain requires it (assessments stay nullable — anonymous rows are case-less until claimed or purged under MV-135, covered instead by a CHECK that an owned or claimed assessment must carry a case), drops ALL EIGHT of MV-156's `<table>_ownership_axis_present` checks now that case_id is mandatory on exactly those eight (none survives into Stage 3), restores `private.reject_prediction_update()` to its unconditional body as the migration's LAST statement — closing the handoff MV-155 opened and nobody had accepted — and removes every legacy owner assumption: no unique constraint, PK or FK target keyed on `owner` (seven owner-keyed uniques drop, including program_predictions' four-column key and MV-156's two `where owner is not null` partials), no residual `unique (id, owner)` targets, no repository scoping argument that is a user id. It also SHIPS the policy rewrite that drops MV-159's transitional owner disjunct — step (d) of its migration, after the SET NOT NULLs that make the disjunct redundant — because MV-159 said four times that removal was MV-160's job while MV-160 held no policy DDL at all, so the "no predicate reads owner" assertion would have gone red against MV-159's own correct output; the removal is behaviour-preserving on SELECT/UPDATE and strictly tightening on INSERT, and MV-159's window-scoped disjunct test is retired with it. Plain `owner` LOOKUP indexes deliberately SURVIVE and are asserted present — assessments_anon_purge_idx and assessments_owner_idx are load-bearing for MV-135's purge and the account-delete sweep — and the `owner` columns themselves are not dropped and no row is deleted, so the rollback is pure DDL re-application. The headline is a BEFORE/AFTER data-equivalence PROOF in two halves, because one of them cannot live in CI: a SYNTHETIC suite on seeded fixtures that captures and replays in-run and NEVER skips (the original single-suite design loaded a gitignored, deliberately-destroyed PII snapshot and skipped when absent — which Stage 1's fail-closed CI scores as red on every run), plus a REHEARSAL-ONLY live replay the integrator runs against a restored copy and records as Done evidence, gating the production apply. Both share one serializer and one exclusion list: per-user per-domain payload hashes plus row-identity parity, not row counts, because counts pass a migration that reassigns rows between students; the only excluded fields are the columns Stage 2 newly ADDED — the nine `case_id`s and MV-156's two surrogate `id`s. CORRECTED 2026-08-06: the two `updated_at` exclusions came OUT, because `private.mv155_backfill_personal_cases()` DISABLES the two `set_updated_at` triggers rather than firing them, so those timestamps never move and excluding them was hiding the only mechanism that keeps them still (matrix spec §9.2 amended, which had blessed the wrong list). The operational half of the exit gate is narrowed to what the real grant set permits — counsellor READ on all nine tables, counsellor WRITE on the five `authenticated` can actually write, and a 42501 NEGATIVE on INSERT into profiles/assessments/plan_items/documents so the Stage 3 deferral cannot rot — behind a machine-enforced source sweep modelled on tests/supabase/service-role-exceptions.test.ts, whose `owner:`-payload allowlist carries exactly one justified entry — CORRECTED 2026-08-06 to `lib/assessments/repo.ts`'s anonymous `owner: null` literal, because MV-157's writer helper performs no write at all (it derives and returns a fragment) and a payload-scoped detector cannot see it. The dual-write is still deliberate and its removal is still a Stage 6 item; deleting it, or unhooking a repository from it, is caught by a separate choke-point assertion instead. Snapshot payload is gitignored (real PII) and only the hash report is committed. P1, agent-owned; blocked by MV-155/156/157/158/159; carries Stage 2's exit onto MV-154._ · [#125](https://github.com/Spooderinbed/merovisa/pull/125) merged, ci passing
- **MV-162** · P2 · [Aesthetics-report polish: typography unification + 7 hierarchy fixes](cards/MV-162-aesthetics-report-polish.md) — _Ships the eight items from the external aesthetics review that need no palette decision — item 2 (mono confined to data tokens only, so eyebrows/chips/prose stop fighting the sans; 151 call sites, 73 files, guarded by a live-tree rule test), item 3 (hero decluttered, emphasis narrowed to "pay anyone"), item 6 (stunted rules removed from the /how scoring-dimensions block; prose kept), item 8 (equal-height destination cards with pinned footers), item 10 (larger journey step marker), item 12 (scholarship rows lifted, titles bold), item 14 (account deletion moved off /profile into a new /settings route), item 15 (Guide moved from last to second in both navs). The three colour items (1, 9, 13 — blue palette, red-for-urgency) are deliberately held for a founder decision and no colour token changes._ · [#126](https://github.com/Spooderinbed/merovisa/pull/126) merged, ci passing · [#124](https://github.com/Spooderinbed/merovisa/pull/124) merged, ci passing
- **MV-161** · P1 · [Unbounded client-writable columns on the five case INSERT surfaces (supersedes_prediction_id is a live P0)](cards/MV-161-unbounded-insert-columns.md) — _BUILT TDD + full gate green, awaiting founder gate (PR open — DO NOT self-merge; the prod apply is separately founder-gated). RED reproduced FIRST on a local stack: the plant ADMITTED and the victim's account-delete BLOCKED P0001, exactly as carved — the attacker satisfies every axis MV-159 bounds and only the pointer column is hostile. Fixed by one top-level conjunct on pp_insert_case and oe_insert_case (migration 20260805120000) plus private.outcome_event_case_id(), the fourth parent-case helper, needed because supersedes_event_id is self-referential where the prediction pointer already had prediction_case_id. PLACEMENT NOTE: the conjunct went TOP LEVEL rather than inside the case arm as the card said — provably equivalent today (the transitional arm needs case_id IS NULL, against which the parentage clause already yields NULL and a WITH CHECK refuses), but top level stops the bound resting on an argument about a NEIGHBOURING clause, which is the exact 'protected by an accident with a scheduled removal date' shape rounds 2 and 3 both found. GREEN: plant REFUSED 42501 from the POLICY (an FK would be 23503, and supersedes_* has only a simple self-FK), identically refused under an FK simulation that drops all FOUR composites — strictly stronger than MV-160's two; the in-case correction control stays ADMITTED so the bound is not a blanket refusal that would close the correction path the column exists for; and the victim's delete completes, proven against a counterfactual that measures the P0001 lock still real. MUTATION: reverting either conjunct turns THREE named tests red — structural, enumeration and behavioural, the last reading 'expected undefined to be 42501', i.e. ADMITTED rather than merely uncaught — with no cross-talk, so each conjunct is independently load-bearing. THE ENUMERATION PASS shipped as a TEST not a document: a third (COLUMN) axis on the completeness guard after verb-aware and branch-aware, both sides derived at run time from information_schema and pg_policy, over all 44 client-writable INSERT columns — 17 bounded, 27 exempt-with-reason — failing CI four ways: an unaccounted column, a stale exemption, a placeholder reason (that one fired during the build), and positively asserting case_id/owner/both pointers stay on the bound side so a failure cannot be 'fixed' by exempting an ownership axis. outcome_events.decision_authority and verified_at flagged REVISIT WITH STAGE 3 — they carry no authority only because the same predicate pins source='self_reported' and verified_by IS NULL. Spec §4 amended in-PR: new rule 5 (the column axis, with the placement equivalence proof) plus the §4.7/§4.9 policy-form cells. GATE: typecheck clean, lint clean, npm test 2598/2598 across 328 files, test:integration 802/802 across 9 files on a LOCAL stack. Harness lesson recorded on the card: a crashed vitest worker skips afterAll and its orphaned fixture rows fail a LATER suite's backfill assert, and piping test:integration into tail reports tail's exit code. Follow-up spawned: the four Stage 1 tenancy tables have the same unbounded-column shape and no guard._ · [#123](https://github.com/Spooderinbed/merovisa/pull/123) merged, ci passing · [#122](https://github.com/Spooderinbed/merovisa/pull/122) merged, ci passing
- **MV-164** · P1 · [Host guard on the Stage 2 §A2 capture driver: refuse production by construction, not by comment](cards/MV-164-stage2-capture-host-guard.md) — _scripts/stage2/capture-read-path-snapshot.mjs holds the service-role key, enumerates EVERY Auth user, reads all nine student read paths for each, and writes the lot to a JSON file — real students' profile sections, names, emails, financial and academic detail (equivalence-report §5). It is documented rehearsal-only in three places (its own header, the report §3, its service-role-exceptions entry, which calls it 'NOT AN APPLICATION PATH') and NOTHING IN THE CODE ENFORCED THAT: report §3 hands you the copy-pasteable command, and pointed at the production ref obfvrxixtautamflzxzq it performs a whole-population personal-data extraction, contradicting the Stage 0 decision record D-A. Not an incident — the script imports no dotenv so it does not auto-load .env.local, and reaching production needs credentials exported deliberately; this is make-it-safe-by-construction. SHIPPED: new import-free module scripts/stage2/capture-host-guard.mjs — production ref REFUSED WITH NO OVERRIDE (--rehearsal-host is not one), localhost/127.0.0.1/::1 free, any other remote refused unless --rehearsal-host (a restored copy may legitimately sit on its own hosted project, so opt-in not ban), unparseable fails closed; every refusal names the host, the reason and the fix. Host matching is a PARSE not a substring test: URL.hostname lowercased, ref must equal a WHOLE DNS label — whole-label because obfvrxixtautamflzxzq-copy is a different project, any-label because db.<ref>.supabase.co is production and a first-label match waves it through. Wired into runCli ahead of createClient AND listUsers, reading the flag with args.has (not args.get: the existing parser stores undefined for a TRAILING flag, so get reads a correctly-typed command as absent). Registry requiredCaseCheck rewritten off 'n/a' plus a new group-D prose-vs-source test; report §3 gains the guard subsection and §4 the G-table. §A1's serializer/hash/exclusion list DELIBERATELY UNTOUCHED (+16/−1 on the capture script, all import/call/usage) — §A1 in CI and §A2 on the rehearsal host must run the same comparison or MV-160's green record is void. TESTS 34 in seven groups incl. group G asserting the guard is WIRED UP on call ORDER; plus a live-CLI smoke proving real exit-1 refusals and that localhost and trailing --rehearsal-host really pass. MUTATION G1-G9 all RED: G1 delete the call (3 red), G2 move it after the client+listUsers (2), G3 neuter production detection (10), G4 whole-label→startsWith (2), G5 parse→rawUrl.includes (5), G6 flag becomes a production override (2), G7 remote allowed by default (3), G8 unparseable fails open (3), G9 assert never throws (3). G1/G2 are the inert-guard shape this repo has been bitten by — every behavioural test in groups A-F stays green with the guard exported and never called. HONEST LIMIT: it classifies the host in SUPABASE_URL, so it cannot see through a custom domain or a proxy and does not authenticate the target._ · [#128](https://github.com/Spooderinbed/merovisa/pull/128) merged, ci passing · [#127](https://github.com/Spooderinbed/merovisa/pull/127) merged, ci passing
- **MV-165** · P0 · [Stage 2 §A2 on REAL production data: amend the boundary to the one still reachable, then run it](cards/MV-165-stage2-a2-real-data.md) — _equivalence-report.md §A2 is the gate on applying Stage 2 to production, and it required a snapshot captured 'before MV-155 first mutated the copy'. That is PERMANENTLY unreachable, measured 2026-08-07: prod has been post-MV-155 since 2026-08-02; the org is on the FREE plan so there are no automated backups and no PITR; PITR is not retroactive so upgrading cannot manufacture a 2026-08-01 restore point; and a filesystem-wide search found NO dump of any kind (99 hits, all repo rehearsal helpers, all dated 2026-08-03+). AMENDED to the reachable boundary — pre-tighten -> post-tighten on real production data, since today's prod IS the pre-tighten state. §3.0 records all four reasons so a later reader cannot read it as convenience, states that the MV-155->158 half can never be proven by snapshot diff, gives its compensating evidence as a zero-residue counts table, and says explicitly that the table proves THE INVARIANT HELD and NOT that every field is byte-identical — the stronger claim is unobtainable. document_status (0 prod rows) is flagged VACUOUS, as is the 0-row anonymous population. RESULT: §A2 GREEN — EQUIVALENT, zero differences, exit 0, whole-snapshot hash a08f6993…3be9 identical before and after. 174 rows across 10 users (7 carry data; 3 are real accounts with none), and the per-user RLS-scoped reads SUM EXACTLY to the service-role table totals, so every prod row is visible to exactly one student. MV-160's sweep repaired ZERO rows and minted zero cases — the MV-157 dual-write did not leak, AND its repair path was therefore never exercised by live data (recorded, not glossed). Proof shown non-inert: §A1's M5 reproduced on the live copy went RED naming student-06/profile/completeness with exit 1, then green again at the same hash. BUILT: supabase/rehearsal/MV-161-rollback.sql — MV-160-rollback deliberately does NOT unwind MV-161 (its §8 ASSERTS the P0 pointer bound survived), so it lands one migration short of production; the missing reverse script closes a real gap in the reversibility doctrine, restores MV-159's two INSERT predicates verbatim, drops the helper, asserts 9 post-conditions, and BOTH its guards were shown to bite (R1 no-op re-run REFUSED; R2 wrong-order REFUSED). Pre-state verified against prod's own catalog on six probes (8 axis checks, 45 policies, 31 mentioning owner, case_id nullable on nine, helper absent, max version 20260803180000). Prod data copied via a one-off service-role READER + psql loader with column parity asserted before any insert and per-table hashes proving byte-identity; auth.users recreated with matching uuids. MV-164 host guard NEITHER weakened NOR bypassed — re-proven live (refused production WITH --rehearsal-host, exit 1) and git status scripts/stage2/ is empty. Also filled §B's real repair count and the 2 orphan storage.objects ids (8 objects vs 6 documents rows, 0 dangling). APPLIED TO PRODUCTION 2026-08-07 by the composer session after reviewing this verdict: MV-161 (20260805120000) then MV-160 (20260805140000), both byte-identical to the proven files (sha256 checked immediately before each call, comments not stripped). Post-apply verification 15/15 pass — assessments.case_id STILL NULLABLE (the trap where a success would mean the anonymous rows were destroyed) and 174 rows across the nine tables, the exact §A2 figure. Step (d), one of the two the migration header marks SILENT, is verified by comparison rather than self-assertion: production's deparsed policy set hashes bf5eaa750b06df575c83ba75e3784e06 over 24 policies, IDENTICAL to the rehearsal host §A2 proved equivalent on. Two items carried forward not closed: prod holds 0 anonymous unclaimed assessments so step (c)'s exception has no live data exercising it, and document_status is still 0 rows. One defect found and FIXED same day — apply_migration stamps its own wall-clock version instead of the repo filename, which would have made the next db push re-run both files and fail; corrected to 20260805120000/20260805140000, verified 24 migrations with zero stale rows. Record in equivalence-report.md §6. All PII destroyed._ · [#130](https://github.com/Spooderinbed/merovisa/pull/130) merged, ci passing · [#129](https://github.com/Spooderinbed/merovisa/pull/129) merged, ci passing
- **MV-166** · P1 · [Write the Stage 3 spec (consultancy workspace): access matrix, route model, grant resolution, and the slice carve](cards/MV-166-stage3-spec.md) — _Stage 3's entire planning today is FOUR BULLETS in the consultancy plan (lines 639-646), where Stage 2 entered execution with a 13-step sequence plus a dedicated 12-section spec. Stage 1's documented failure mode was six slices each deriving an access matrix from the plan's prose independently — the Stage 2 spec's §9 lists ELEVEN contradictions that had to be reconciled afterwards, and MV-154 carries a stage-level amend-the-spec-in-your-own-PR rule added only because the prose version failed twice running. This card writes docs/superpowers/specs/2026-08-XX-stage3-workspace-and-access-matrix.md, grounded in a LIVE READ of the schema (never the plan's prose), extending the canonical Stage 1 access matrix cell-for-cell rather than re-deriving it; resolves on paper the three debts Stage 2 handed forward (the four deferred write grants on profiles/assessments/plan_items/documents INSERT, pinned shut by MV-160's 42501 assertions; the nine remaining legacy-owner-scoped service-role paths; case routes and consultancy UI); and produces the slice carve — IDs from MV-167 up, DAG with a reason per edge, a Stage 3 umbrella, and a statement of which slices are reachable on TEST DATA versus which need the Stage 0 D-B legal gate that is currently SHUT with counsel. Ships NO product code and NO SQL: a non-empty git diff over app/ lib/ components/ supabase/ tests/ means the card overran. Not blocked — Stage 2 is complete and live in production (PR #129, master b3347a9), and writing the carve is exactly the work that is useful while counsel is out._ · [#133](https://github.com/Spooderinbed/merovisa/pull/133) merged, ci passing · [#132](https://github.com/Spooderinbed/merovisa/pull/132) merged, ci passing
- **MV-168** · P1 · [Stage 3 slice 1 — the consultancy write grants, and retiring MV-160's 42501 pin in the same PR](cards/MV-168-consultancy-write-grants.md) — _PREREQUISITE, not a late slice: nothing can be written into a student-less case until these land. The ONLY SQL in Stage 3 — no schema change. Adds INSERT grant+policy on profiles and plan_items and a NARROWED UPDATE (is_primary) grant+policy on assessments, each mirroring the five-sibling WITH CHECK template already shipped (case_id IS NOT NULL AND case_id = ANY(actor_case_ids()) AND (owner IS NULL OR owner = case_student_id(case_id))) — that third conjunct is the consultancy-row clause and omitting it is the bug. Stage 2 spec §6 defers TEN verbs, not four; spec §6.1 dispositions all ten: 3 granted here, 4 refused permanently (assessments INSERT is refused because client-writable result/rule_version would let any actor mint their own verdict against the server-side scoring rule; assessments DELETE; plan_items DELETE), 1 deferred to Stage 4 (documents INSERT — its only caller also needs a Stage 4 Storage policy, so granting it alone retires no service-role path), 2 confirmed never. RETIRES THE PIN IN THIS SAME PR: the pin is ONE test (stage2-tighten.itest.ts 'DEFERRED HALF') holding FOUR assertions, not eight — invert the profiles and plan_items ones, leave the assessments and documents ones GREEN with a corrected comment, amend Stage 2 spec §6 and add a dated §12 entry. ALSO converts three .upsert() call sites to read-then-insert — upsertProfileForCase, upsertProgramState, setObtained — because grant 1 is INSERT-only while the call site is an UPSERT: PostgREST compiles it to INSERT ... ON CONFLICT DO UPDATE SET with every payload column including case_id in the SET list and checks privileges at plan time, so it is 42501 on the FIRST call with no row present, and granting UPDATE(case_id) is forbidden by design (spec §6.1). Retires service-role entry 8 (with MV-172); entry 9 NARROWS, it does not retire. See spec §6.1 + §8.2 for why widening a production grant before any caller exists is safe here._ · [#134](https://github.com/Spooderinbed/merovisa/pull/134) merged, ci passing
