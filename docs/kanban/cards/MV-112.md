# MV-112 — Landing page redesign (v7 "Cursor cadence")

**Column:** In Progress · **Priority:** P1 · **Owner:** agent
**Branch:** `mv-112-landing-redesign` (off `origin/master`) · **Merge:** FOUNDER-GATED
**Spec:** [docs/superpowers/specs/2026-07-08-landing-redesign-design.md](../../superpowers/specs/2026-07-08-landing-redesign-design.md)
**Reference:** [docs/superpowers/specs/assets/2026-07-08-landing-v7-reference.html](../../superpowers/specs/assets/2026-07-08-landing-v7-reference.html)
**Plan:** docs/superpowers/plans/2026-07-08-landing-redesign.md (written at build start)

## What this is

Rebuild the signed-out marketing landing (`app/(marketing)/page.tsx`) in the
premium v7 "Cursor cadence on our own skin" style: a confident hero, then
alternating product sections each anchored by ONE real interactive artifact.
Every claim is either a labelled **sample** or a **sourced + dated** fact — the
page sells the product by *showing* it, honestly.

Spec is founder-locked ("looks good to me", 2026-07-08) and Codex (GPT-5.5,
xhigh) reviewed it twice; all blockers/should-fixes/nice-to-haves folded in
(spec commit `2f4c150`, changelog §14). Build is unblocked.

## Section cadence (spec §3)

1. Sticky header (brand · nav · Sign in)
2. Hero — confident headline + hand-drawn marker on "pay anyone", flat plum CTA,
   the **live verdict panel** (§4.4) in a bordered stage, proof strip
3. "From verdict to plan" (`id="how"`) — **plan-step accordion** (§4.5)
4. "Documents" (`id="what"`) — **interactive checklist** (§4.6)
5. "The guide" — **autoplay guide typewriter** (§4.7)
6. "Sourced & dated" (freshness band) — **freshness table** + verify-sweep (§4.8)
7. Close — **sparkle CTA** (§4.2)
8. Footer (text only)

## Locked invariants (do not violate)

- **Server-rendered FILLED rest state.** The reference builds DOM in JS from
  empty; our React build INVERTS that — server-render the *filled* state
  (verdict panel shows Aarav final cost, plan step 02 open, checklist 2/6
  checked, guide `ielts` exchange rendered, freshness rows pre-verified, fill
  widths set inline). JS only *enhances*.
- **Hydration parity.** Islands are `'use client'` but first client paint ==
  SSR. No `matchMedia` / `IntersectionObserver` / `requestAnimationFrame` /
  `Math.random` during render — all run only in `useEffect` post-mount (sparkle
  particles seeded post-mount only).
- **Honesty split.** Sample-student numbers carry NO sourced `verified` citation
  (sample cost is a `≈` estimate beside a "Sample profile" label); sourced facts
  always render `source · verified <month>`. Enforced via a discriminated
  `kind: 'sample' | 'sourced'` union. Freshness provenance is visible at rest,
  never gated behind a click.
- **Genuine Student (GS)** terminology — Australia replaced "GTE / Genuine
  Temporary Entrant" with the Genuine Student (GS) requirement in 2024. No
  user-facing "GTE" string anywhere.
- **No dead links.** No `href="#"`. Hero CTA + verdict "See full breakdown" +
  closing sparkle CTA all → `/assess`; "Sign in" → `/auth`; footer text only.
- **Preserve the signed-in redirect.** Server shell keeps
  `getUser()` → `redirect("/dashboard")` before any landing markup.
- **No em-dashes** in the landing copy modules (guard test).
- **Both themes** (`prefers-color-scheme` + `:root[data-theme]` wins both ways;
  dark uses `background-color` not `background` shorthand), **1160px** max width.
- **Three approved flat-rule exceptions only** (§4.1–4.3): hand-drawn hero
  marker, sparkle CTA (closing only), faint paper grain. Nothing else departs
  from flat; each degrades to a calm static state under reduced motion.
- **No-JS vs reduced-motion are split** (spec §4.4, §4.7): no-JS = complete
  default rest state (native radio path offered for zero-JS switching);
  reduced-motion-with-JS = interaction preserved, animation suppressed.
- Presentational slice only — **no scoring / API / DB / Zod / auth changes.**

## Files

**Create — client islands** `components/marketing/`:
`verdict-panel.tsx`, `plan-steps.tsx`, `documents-checklist.tsx`,
`guide-thread.tsx`, `freshness-table.tsx`, `sparkle-cta.tsx`, `reveal.tsx`,
`hero-marker.tsx` (or inline).

**Create — typed data** `lib/marketing/`:
- `sample-profiles.ts` — Aarav (GPA 3.2 → "Possible", est ≈A$42,600) / Shruti
  (GPA 3.8 → "Strong", est ≈A$44,200); 4 dims each (Academic / English /
  Finances / Visa risk). `kind: 'sample'`, no citation.
- `plan-steps.ts` — 5 steps (state / title / detail / cite); step 02 open at rest.
- `checklist-items.ts` — 6 items (label / source / initial done); 2 default checked.
- `guide-answers.ts` — 3 `{q,a,c}` exchanges keyed `ielts` / `funds` / `gte`
  (+ `order`); `ielts` is the SSR rest exchange.
- `freshness-rows.ts` — 5 rows (A$29,710 living-cost / Home Affairs, GS
  requirement, ≈A$33,000 tuition, 485 = 2–4 yrs, OSHC); each verified Jun 2026 /
  next check Jul 2026. `kind: 'sourced'`.

**Rebuild** `app/(marketing)/page.tsx` → v7 structure; preserve the getUser
redirect; drop `<TrustStrip />`.

**Delete superseded** (verify no other importer first):
`components/marketing/hero-preview.tsx`, `how-it-works.tsx`, `tile.tsx`,
`trust-callout.tsx`. Keep `eyebrow.tsx` if the new hero uses it. `TrustStrip`
(`components/layout/trust-strip.tsx`): remove from page; delete the component
only if landing was its sole importer.

**Tokens/CSS:** additive tokens → `tailwind.config.*`; keyframes, grain, marker,
sparkle, accordion `grid-template-rows` → a scoped marketing global CSS.

**Tests:** under the existing marketing test location (see spec §11).

## Acceptance criteria (spec §11)

- [ ] Hero renders H1, CTA label + `href="/assess"`, provenance line, 3 proof claims.
- [ ] "Sample profile" label renders; both profiles' verdict words in the data module.
- [ ] Each sourced figure renders its exact `source · verified` string (A$29,710 /
  s.500 / ≈A$33,000 / 485 / OSHC) — fabrication guard.
- [ ] Guide data module has exactly the 3 approved questions + citations.
- [ ] Rest-state completeness per artifact (no-JS render, effects not fired):
  verdict final cost + "Sample profile"; plan step 02 detail; checklist 2/6 with
  real checkboxes; guide `ielts` exchange + citation; all 5 freshness rows with
  visible source + verified + next-check.
- [ ] Signed-in `getUser()` redirects to `/dashboard` before landing markup.
- [ ] No dead links (no `href="#"`; CTAs → `/assess`; Sign in → `/auth`).
- [ ] Checklist rows are `role="checkbox"`; 2 checked at rest; toggling a 3rd → "3 of 6".
- [ ] Terminology guard: no "GTE" / "Genuine Temporary Entrant" in copy modules.
- [ ] Hydration parity: no hydration warning; no `Math.random`/`matchMedia`/IO in render.
- [ ] Reduced-motion: artifacts render final static states, interaction preserved.
- [ ] Theme parity: stamped `data-theme` wins over the media query both ways.
- [ ] No em-dash in the marketing copy modules.

## Gate (green before review)

`npm run typecheck` · `npm run lint` · `npm test` — all clean.

## Resume notes (cold-start)

- Spec is FINAL at commit `2f4c150`; reference HTML is the pixel/markup source of
  truth ("where prose and reference disagree on a pixel, reference wins; on
  intent/invariant, spec wins").
- Build via **subagent-driven-development** (TDD, fresh subagent per task +
  two-stage review). Plan lives at
  `docs/superpowers/plans/2026-07-08-landing-redesign.md`.
- Branch `mv-112-landing-redesign` is off `origin/master`; **merge is
  FOUNDER-GATED** — never self-merge / `--admin`.
- Exclude untracked `.claude/**` from every commit.

## Commits (branch `mv-112-landing-redesign`)

- `8106903` docs(landing): spec rewrite to v7 Cursor-cadence lane + reference
- `c3dbe19` docs(landing): fold Codex xhigh review into landing spec
- `2f4c150` docs(landing): fold Codex round-2 re-verify into landing spec
