# MV-156 — Owner nullable on the eight NOT NULL tables + predictions→attempts→outcome_events composite-FK rebase onto the case

**Priority:** P1   **Owner:** agent
**Goal:** Make it structurally possible for a student case with **no Auth user** to hold data — relax `owner` to nullable on all eight NOT NULL student-owned tables and re-base the predictions→attempts→outcome_events ownership chain from `unique (id, owner)` onto `unique (id, case_id)` — with a compensating check that keeps the chain's ownership invariant enforced for the whole nullable window, so Stage 3 can create a consultancy case before the student exists without the schema silently dropping the constraint the chain was designed around.

## Context links
- **Sequence step this card is** — plan `docs/superpowers/plans/2026-07-23-consultancy-student-case-workspace.md` "Additive migration sequence" **step 8** (line 311): "Relax `owner` to nullable on the tables that must hold consultancy-created rows, **before the consultancy workspace writes real data**."
- **Obstacle 1 — owner NOT NULL on eight tables** — same plan, §"Known schema obstacles" line 324. Names the eight and states the consequence: "no row can exist for such cases while the constraint stands. Relaxing `owner` to nullable is therefore a prerequisite for the consultancy workspace stage."
- **Obstacle 3 — the composite-FK chain** — same plan, line 326: "`unique (id, owner)` targets. Re-basing on the case requires new `unique (id, case_id)` targets and re-pointed child keys — a coordinated multi-table constraint swap. **While `case_id` is nullable, a composite key that includes it is unenforced for null rows**, so the nullable window must be kept short for these three tables and covered by a compensating check until `case_id` becomes mandatory." This card's Risk notes explain the mechanism (MATCH SIMPLE) the plan states as a fact.
- **Why it must land before Stage 3** — same plan, Stage 3 exit gate (line 646): "an authorized counsellor can create, find, assign, and manage a case **without a student account**." That gate is unreachable while eight tables demand an `auth.users` id.
- **Stage 2 exit gate this card serves but does not prove** — same plan, line 637; proven by **MV-160**.
- **Uniqueness rules that move** — same plan, lines 292–300 (profile per case, primary assessment per case, open plan item per case/kind, program state per case/program). This card does **not** move them (MV-157 does); it records which of them stop constraining the moment `owner` goes nullable.
- **Rehearsal + rollback obligation** — same plan, §"Validation plan" line 740 ("migration rehearsal on a copy of current data, including a rehearsed rollback and a post-cutover reconciliation check") and **MV-154**, which makes this mandatory for every Stage 2 slice that touches live data.
- **Access control is unchanged by this card and must stay that way** — `docs/superpowers/specs/2026-08-02-stage1-canonical-access-matrix.md` is authoritative for every cell; MV-156 adds no policy, no grant, and no permission verb. Case-aware policies on these tables are **MV-159**.
- **Live-data scale** — plan §"Current-state feasibility evidence" (lines 540–556): 9 Auth users, 7 profiles, 74 plan items, 12 user-program-state rows, 6 documents, 10 predictions, 10 attempts, 19 outcome events.
- **The real legacy schema — read all of these, they define the eight:**
  - `supabase/migrations/20260603170655_add_profiles_evolve_assessments.sql` — `profiles.owner uuid not null unique references auth.users(id) on delete cascade`; `private.set_updated_at()`.
  - `supabase/migrations/20260604024609_add_plan_items.sql` — `plan_items.owner uuid not null`; `plan_items_kind_open_idx unique (owner, kind) where status = 'todo'`.
  - `supabase/migrations/20260604002139_add_programs_universities_state.sql` — `user_program_state … primary key (owner, program_id)` ← **owner is in the PK**.
  - `supabase/migrations/20260604060000_add_documents.sql` + `20260605000000_simplify_documents.sql` + `20260605130000_fix_documents_rls.sql` — `documents.owner uuid not null`, `unique (owner, kind)`, INSERT is `to service_role` only.
  - `supabase/migrations/20260626000000_add_document_status.sql` — `document_status … primary key (owner, kind)` ← **owner is in the PK**.
  - `supabase/migrations/20260620000000_add_outcome_validation.sql` — the whole chain: `unique (id, owner)` on all three, `foreign key (prediction_id, owner) → program_predictions (id, owner)`, `foreign key (attempt_id, owner) → application_attempts (id, owner)`, and `private.reject_prediction_update()` (lines 50–61). Read lines 66–70 in particular: the composite FK "makes it structurally impossible for an attempt's owner to diverge from its prediction's owner (S12) — **no trigger**." That sentence is the invariant this card must not lose.
  - `supabase/migrations/20260620010000_index_application_attempts_composite_fk.sql` — the "every composite FK gets a single covering index or the advisor flags it" discipline the new keys must follow.
- **Stage 1, already shipped and not to be re-opened:** `supabase/migrations/20260730120000_stage1_tenancy_core.sql`, `supabase/migrations/20260730180000_case_aware_rls_policies.sql`, `lib/cases/*.ts`, `lib/supabase/service-role-exceptions.ts`, `tests/integration/tenant-isolation.itest.ts`, `tests/integration/case-rls.itest.ts`.
- **Harness idioms to copy:** `tests/integration/anon-purge.itest.ts` (localhost hard-guard that throws at import, env-gated `describe.skipIf`, "assert on THIS row, never a global count") and `tests/integration/tenancy-schema.itest.ts` (catalog assertions via `docker exec` psql — PostgREST cannot read `pg_catalog`/`information_schema`).
- **Siblings:** MV-154 (umbrella, reversibility requirement), **MV-155** (personal cases + `case_id` columns + backfill — the hard blocker), MV-157 (case-aware repositories), MV-158 (anonymous claim), MV-159 (case-aware RLS on these tables), MV-160 (tighten + Stage 2 exit). Exemplars for voice/density: `docs/kanban/cards/MV-150-tenancy-core-schema.md`, `docs/kanban/cards/MV-153-cross-tenant-negative-tests.md`.

## Acceptance criteria

**The eight tables — every one named, every one verified**
- [ ] `owner` is nullable on **all eight**, confirmed from `information_schema.columns.is_nullable = 'YES'` (not by reading the DDL): `public.profiles`, `public.plan_items`, `public.user_program_state`, `public.documents`, `public.document_status`, `public.program_predictions`, `public.application_attempts`, `public.outcome_events`.
- [ ] **`public.assessments` is untouched** — its `owner` has been nullable since `20260603011208` (that is how an anonymous assessment exists). It is the ninth student-owned table and deliberately not one of the eight; a diff that alters it fails this card.
- [ ] **`user_program_state` PK replaced, not "relaxed".** A PK column cannot drop NOT NULL. The table gains a surrogate `id uuid primary key default gen_random_uuid()`, the composite `primary key (owner, program_id)` is dropped, and the legacy rule survives as `unique index … (owner, program_id) where owner is not null`. Verified: an authenticated duplicate `(owner, program_id)` insert still fails with `23505`; two NULL-owner rows for the same `program_id` are accepted.
- [ ] **`document_status` PK replaced identically** — surrogate `id`, composite `primary key (owner, kind)` dropped, `unique index … (owner, kind) where owner is not null`. Same two verifications.
- [ ] No FK anywhere targets either replaced PK (verified against `pg_constraint`, not assumed) — so the swap re-points nothing.

**The composite-FK rebase (three tables)**
- [ ] `public.program_predictions` and `public.application_attempts` each carry a new `unique (id, case_id)` FK target alongside the existing `unique (id, owner)`. (Both are redundant as uniqueness — `id` is already the PK — and exist solely as composite-FK targets, exactly as `unique (id, owner)` does today.)
- [ ] `public.application_attempts` carries `foreign key (prediction_id, case_id) references public.program_predictions (id, case_id)`, and `public.outcome_events` carries `foreign key (attempt_id, case_id) references public.application_attempts (id, case_id)`.
- [ ] Each new composite FK has a **single covering index on its exact column pair** — `application_attempts (prediction_id, case_id)`, `outcome_events (attempt_id, case_id)` — per the `20260620010000` precedent, so the performance advisor reports no unindexed foreign key.
- [ ] **The legacy owner chain is retained, not dropped** — `unique (id, owner)` and the `(prediction_id, owner)` / `(attempt_id, owner)` FKs all still exist after this migration, and MV-160 drops them. Both chains are MATCH SIMPLE and each enforces exactly on the rows where its own column is non-null; together they cover every row.
- [ ] **The invariant is demonstrably restored:** inserting an `application_attempts` row whose `case_id` differs from its prediction's `case_id` is rejected (`23503`), and likewise an `outcome_events` row whose `case_id` differs from its attempt's.

**The compensating check — the thing that closes the MATCH SIMPLE hole**
- [ ] **All eight tables carry the same named, VALIDATED `check (owner is not null or case_id is not null)`**, named `<table>_ownership_axis_present` — including the three chain tables (`program_predictions_ownership_axis_present`, `application_attempts_ownership_axis_present`, `outcome_events_ownership_axis_present`). A row with **neither** axis set is rejected with `23514`; every other shape is accepted. Relaxing `owner` therefore cannot produce a row owned by nothing — invisible to every RLS policy and reachable by no cascade.
- [ ] **Why the disjunct is the correct compensating cover on the three chain tables, and not a weakening.** This card **retains** the legacy `unique (id, owner)` targets and the `(prediction_id, owner)` / `(attempt_id, owner)` FKs. So an owner-set / `case_id`-null row is still structurally enforced by the **owner** chain, and an owner-null / `case_id`-set row by the new **case** chain. The disjunct forbids exactly the one shape *no* chain covers — both axes null — which is precisely the MATCH SIMPLE hole the plan names at line 326. A flat `check (case_id is not null)` does not close that hole any better; it only also closes the live write path.
- [ ] **The pre-MV-157 write shape still succeeds — as `authenticated` and as `service_role`.** After this migration, an insert that writes only `owner` (the shape produced today by `lib/outcomes/on-apply.ts` `captureApplication`, `lib/outcomes/freeze.ts`, and `app/api/outcomes/{prediction,attempt,event}/route.ts`) still SUCCEEDS on all three chain tables. A CHECK is role-independent — `service_role` does **not** bypass it — so a `check (case_id is not null)` here takes the live outcomes write path down with `23514` for the whole MV-156 → MV-157 window, on every insert, in production.
- [ ] Every check is named with the stable `_ownership_axis_present` suffix so MV-160 can drop the family by name; the card records which MV-160 drops (the three chain tables', once `case_id` is a real NOT NULL — MV-160 §C) and which survive into Stage 3 (the five non-chain tables').

**Ordering and reversibility**
- [ ] **One migration, one transaction**, with `alter … alter column owner drop not null` as the **final** statement group — after the case chain and every check are in place, so there is no instant, not even mid-apply, in which `owner` is nullable and the case chain is absent.
- [ ] The migration is **inert for existing rows apart from the two surrogate-key additions**: `git diff` shows no `UPDATE` to any of the eight, no new row-level trigger, and no touch of `private.reject_prediction_update` (that trigger is MV-155's problem, not this card's).
- [ ] A **rehearsal on a copy of live data** is recorded: restore a dump into a scratch local database, apply MV-155 then MV-156, run the itest, and produce a **post-cutover reconciliation** — per-table row counts and a row-identity comparison before/after, byte-identical apart from the surrogate `id` columns.
- [ ] A **rollback script** is written and replayed in the same rehearsal (drop the new checks/FKs/uniques/indexes, restore the two composite PKs, re-`SET NOT NULL` on the eight) — and the card states plainly that this rollback is clean **only while zero NULL-owner rows exist**.

**Types and the app layer**
- [ ] `lib/supabase/types.ts` regenerated: `owner` is `string | null` in the `Row` type of all eight tables.
- [ ] `npm run typecheck` is green **without a single `!` non-null assertion or `as string` cast added at any `owner` site.** Every place that maps a row's `owner` into a `string`-typed domain field either admits the null in its own type or handles it explicitly — `lib/plan/types.ts:16`, `lib/outcomes/repo.ts` (`:11`, `:22`, `:33`, `:221`, `:234`, `:247`), `lib/documents/repo.ts` (`:10`, `:57`, `:67`, `:81`, `:96`), `lib/profiles/repo.ts:21`, `lib/matches/repo.ts:23`. This is the minimum honest change only: **re-keying repositories onto `case_id` is MV-157**.

**Non-goals — do not absorb a sibling's scope**
- [ ] No `case_id` column is added and no backfill runs here (MV-155). No repository or route changes ownership semantics (MV-157). No RLS policy, helper, or grant changes (MV-159). No `case_id` becomes NOT NULL and no legacy owner assumption is removed (MV-160). No document header/versions replacement and no `storage.objects` policy work (Stage 4).

## Test plan
- **New real-DB suite `tests/integration/owner-nullable-rebase.itest.ts`** (`*.itest.ts`, excluded from `npm test`, collected by `vitest.integration.config.ts`, run by `npm run test:integration`). Copy the localhost hard-guard and `describe.skipIf` gate verbatim from `tests/integration/anon-purge.itest.ts`; do catalog reads through `docker exec … psql` as `tests/integration/tenancy-schema.itest.ts` does.
  - **Nullability, all eight**, read from `information_schema.columns` — one assertion per table, named, so a missed table is a named failure rather than a silent gap.
  - **The two PK replacements:** the PK is now the surrogate `id`; the legacy pair still rejects a duplicate for a non-null owner (`23505`); two NULL-owner rows sharing the other key column are accepted.
  - **The rebase:** both new `unique (id, case_id)` targets exist; both new composite FKs exist and reference them (asserted from `pg_constraint.confkey`, not by name); each has a covering index on its exact column pair; a cross-case attempt and a cross-case outcome event are both rejected with `23503`.
  - **The compensating check bites, on exactly the shape it targets:** an insert with **both** `owner` and `case_id` null raises `23514` on each of the eight.
  - **The pre-MV-157 write shape still succeeds — the assertion that would have caught a flat `case_id is not null`.** On each of the three chain tables, insert the shape live code produces today (`owner` set, `case_id` null — `lib/outcomes/on-apply.ts` `captureApplication`, `lib/outcomes/freeze.ts`, `app/api/outcomes/{prediction,attempt,event}/route.ts`) and assert it **succeeds**, twice: once as `service_role` and once as `authenticated` writing its own row. A CHECK is role-independent, so the `service_role` pass is not redundant — it is the one that proves the outcomes API routes survive the MV-156 → MV-157 window.
  - **The MATCH SIMPLE hole is demonstrated, not asserted on faith.** In a scratch transaction (or against a disposable clone, following MV-153's disposable-org precedent so a surprise success cannot take the fixture down), drop **both** `application_attempts_ownership_axis_present` **and** the legacy `(prediction_id, owner)` FK, insert an attempt with `owner` and `case_id` both NULL pointing at a prediction of a *different* case, and assert the database **accepts** it — then roll back. Two coupled facts fall out, and both belong in the PR body: removing the check re-opens the hole, and the *only* thing covering the owner-set / `case_id`-null rows in the meantime is the retained owner chain — which is why MV-160 may not drop that chain before `case_id` is NOT NULL.
  - **RLS still fails closed on NULL-owner rows.** Service-role seeds a NULL-owner row on each of the eight; an authenticated client (anon key + real JWT, per MV-153's client factory) reads **zero** rows and cannot INSERT one; anon reads zero. Relaxing the column must not open a read path, and it must not be silently "fixed" here — case-aware access is MV-159.
  - **Cascade behaviour changed, deliberately:** deleting an Auth user still cascades their owner-set rows away and leaves NULL-owner rows intact.
  - **The predictions immutability trigger survived the DDL:** an `UPDATE` on `program_predictions` still raises, including for `service_role`.
- **Unit:** no unit tests for DDL. Any new null-handling branch introduced by the types regen gets a case in its repository's existing spec — a branch added without a test is how a null becomes a runtime `undefined` in a dashboard read.
- **Regression:** the full `npm test` suite must be green **unchanged** — this card must not require editing an existing test's expectations. If one needs changing, that is a behaviour change and belongs to MV-157.
- **Rehearsal (not a test file, but required evidence):** restore a copy of live data, apply MV-155 + MV-156, run the itest, run the reconciliation comparison, then replay the rollback script and confirm the copy returns to its pre-migration shape. Record the commands and the counts on the card.

## Integration gate
- `npm run typecheck` · `npm run lint` · `npm test` · **`npm run test:integration`** — the last is mandatory, not optional: every guarantee this card makes (nullability, the rebased keys, the compensating check, the MATCH SIMPLE demonstration, fail-closed RLS on NULL-owner rows) exists only against a real Postgres.
- **`master` is protected by an active ruleset with no bypass:** required checks `integration` **and** `validate`, strict/up-to-date, PR required, no direct pushes, no force pushes. The `integration` job is **gating** (`continue-on-error` removed) — a red integration lane blocks the merge. This card cannot be merged on a green `npm test` alone, and cannot be pushed straight to `master`.
- Locally, first `npx supabase start`, then export `SUPABASE_TEST_URL`, `SUPABASE_TEST_SERVICE_ROLE_KEY` and `SUPABASE_TEST_ANON_KEY` from `npx supabase status -o env`.
- Applying to the hosted project is **founder-gated** and, because this is live-data DDL, gated additionally on the recorded rehearsal + rollback (MV-154).

## Dependencies / blocked-by
- **Hard blocker — MV-155.** `case_id` must exist on `program_predictions`, `application_attempts` and `outcome_events` and be backfilled for every existing row, or the new `unique (id, case_id)` targets and composite FKs cannot be created. (The `_ownership_axis_present` check itself validates against today's owner-set rows regardless — it is the FK rebase, not the check, that needs the backfill.) MV-155 also owns the `program_predictions` immutability-trigger workaround; do not touch that trigger here.
- **MV-155 ships a detector, not a live constraint** (`private.mv155_assert_case_backfill()`), so there is no owner-required CHECK for this card to re-express. Still verify that before writing the migration: if MV-155's implementation drifted and DID add an owner-assuming constraint, relaxing the column breaks it, and re-expressing it here as an implication (`owner is null or …`) is the one MV-155 artifact this card may edit.
- **Blocks MV-157** (repositories cannot write a consultancy row while `owner` is NOT NULL), **MV-159**, **MV-160**, and **all of Stage 3** — the Stage 3 exit gate is a counsellor managing a case *without a student account*, which is exactly what the NOT NULL constraint forbids.
- **Coordinates with MV-158** — the claim path writes `profiles` (one of the eight) and reads `assessments.owner is null` as its unclaimed signal. `assessments` is untouched here, so the claim's precondition is unaffected; but MV-158 must not start relying on `profiles.owner` being non-null.
- Rolls up to **MV-154**, which carries the stage-level reversibility requirement this card discharges for its own slice.
- Requires a local Supabase stack (Docker + `supabase` CLI) and a restorable copy of live data for the rehearsal.
- **Not blocked by the Stage 0 legal gate** (decision record `docs/legal/2026-07-29-stage0-decision-record.md` D-B): this card writes no student data. It does, however, alter tables that *hold* real student data, which is why MV-154's rehearsal rule applies even though D-B does not.

## Risk notes
- **The rebase is dangerous because MATCH SIMPLE makes the new constraint silently inert — this is the card's headline risk.** Postgres foreign keys default to `MATCH SIMPLE`: a multi-column FK is satisfied **without any lookup at all** if *any* referencing column is NULL. So from the moment `case_id` is nullable, `(prediction_id, case_id) → program_predictions (id, case_id)` enforces **nothing** for every row whose `case_id` is NULL. There is no error, no warning, no advisor finding: the constraint sits in `pg_constraint` looking healthy while checking nothing. What is absent is precisely the invariant `20260620000000` was written to guarantee — "structurally impossible for an attempt's owner to diverge from its prediction's owner … **no trigger**" — and it is absent during the one window where it matters most: while consultancy rows and legacy rows coexist in a half-migrated schema. The concrete failure is an `application_attempts` row pointing at another case's prediction, or an `outcome_events` row at another case's attempt: cross-case ownership divergence in the three tables that feed outcome-validation calibration, discovered later as corrupted evidence rather than as an error at write time. **Required mitigations, all four:** (1) the validated `check (owner is not null or case_id is not null)` on all three, so no row can exist with *neither* axis set — the one shape neither chain covers (a flat `check (case_id is not null)` was rejected because it also raises `23514` on every live owner-only outcomes insert, `service_role` included, until MV-157 lands); (2) **both chains retained** — this is what actually covers the owner-set / `case_id`-null rows through the whole window, and it is load-bearing, not belt-and-braces; (3) the nullable window kept short — MV-160 sets `case_id` NOT NULL first and only then may the compensating checks and the legacy owner chain be dropped, in that order; (4) the itest that *demonstrates* the hole with the check **and** the legacy FK dropped, so neither can be deleted as "redundant with the FK" by someone who has not seen what happens without them.
- **Two of the eight cannot drop NOT NULL at all, and the naive fix is worse than the error.** `user_program_state` keys `primary key (owner, program_id)` and `document_status` keys `primary key (owner, kind)`; a PK column is NOT NULL by definition and `drop not null` on it fails. An agent who "fixes" that by dropping the PK without replacing the uniqueness leaves a table where one user can hold two shortlist rows for the same program — a duplicate-state bug with no error attached to it. The PK replacement is spelled out in the acceptance criteria for exactly this reason.
- **Every owner-keyed uniqueness rule stops constraining NULL-owner rows the instant `owner` is nullable** — NULLs are distinct in a unique index. That covers `profiles.owner unique`, `documents unique (owner, kind)`, `plan_items_kind_open_idx (owner, kind) where status='todo'`, both replaced-PK partial uniques, and `program_predictions unique (owner, assessment_id, program_id, rule_version)`. The gap is bounded by card order — no consultancy row exists until Stage 3, and Stage 3 is blocked on MV-157, which re-expresses each rule on the case. **One of these is not on the plan's list** (lines 292–300 name profile, primary assessment, plan item, program state and documents, but not the predictions "one frozen run per rule version" rule): flag it to MV-157 and MV-160 rather than assuming the plan's list is exhaustive.
- **Relaxing `owner` does not open a read hole — it closes access, which is correct but will look like a bug.** Every existing policy on these tables is `(select auth.uid()) = owner`; for a NULL-owner row that predicate is NULL, so the row is invisible to every authenticated client and un-writable by them. Consultancy rows are therefore unreadable *by the counsellor who should see them* until MV-159 lands. That is the intended fail-closed interim posture. Do not add a policy here to "make Stage 3 demoable"; a policy written in this card would be written against no canonical matrix cell and is exactly the parallel-derivation failure `docs/superpowers/specs/2026-08-02-stage1-canonical-access-matrix.md` exists to prevent.
- **Cascade coverage shrinks, permanently.** `owner … on delete cascade` cleans up nothing for a NULL-owner row. That is the desired semantics (deleting a student's Auth account must not delete a consultancy case's data, and vice versa — plan line 514), but it means MV-05's right-to-delete and Stage 6's case deletion can no longer rely on the Auth cascade as their sweep. Out of scope here; recorded so it is a known consequence rather than a Stage 6 surprise.
- **This mutates live user data's schema, so reversibility is not optional** (MV-154). Seven profiles, 74 plan items, 12 user-program-state rows, 6 documents, 10 predictions, 10 attempts, 19 outcome events. Two tables get a surrogate key, which writes every row. **The rollback window is finite and must be stated on the card:** re-applying `SET NOT NULL` succeeds only while no NULL-owner row exists. Once Stage 3 writes its first consultancy row, rolling this card back means deleting consultancy data. Rehearse before, not after.
- **The types ripple is the point, not an annoyance.** `owner` flips to `string | null` across eight `Row` types and roughly 200 `owner` references in `lib/` and `app/`. A single `!` or `as string` re-asserts the exact assumption — actor equals student, owner always present — that Stage 2 exists to remove, and it does so in a place `npm run typecheck` will never complain about again. The acceptance criterion bans them.
- **Do not touch `private.reject_prediction_update` here.** DDL does not fire row-level triggers, so `drop not null`, the new uniques, the new FKs and the checks all apply without the trigger being involved — this card genuinely does not need it changed. MV-155 does need it changed, for the backfill. Two cards editing one trigger is a merge collision on the highest-consequence object in the schema.
- **Advisor discipline:** two new composite FKs mean two new required covering indexes, or `unindexed_foreign_keys` fires. The `20260620010000` migration exists solely because that was missed once already on this exact chain.

## Agent resume notes (for a cold start)
1. **First concrete action:** confirm **MV-155 is merged and applied locally** — `case_id` must exist and be non-null on all rows of `program_predictions`, `application_attempts` and `outcome_events`. If it is not, stop: nothing in this card can be created. Verify with, in the `supabase_db_*` container, `select count(*) from public.application_attempts where case_id is null;` → must be 0 on all three.
2. Read, in this order: this card's *Context links*; plan lines **311**, **324**, **326**, **637**, **646**, **740**; then `supabase/migrations/20260620000000_add_outcome_validation.sql` **end to end** (especially lines 28–40, 66–84, 98–125 — the three tables, the two composite FKs, the immutability trigger) and `20260620010000_index_application_attempts_composite_fk.sql`. Then open the five other defining migrations listed under *Context links* and copy the exact `owner` declaration of each of the eight into a scratch note — the two PRIMARY KEY cases are the ones that will bite.
3. Read MV-155's shipped migration. It is expected to ship a DETECTOR (`private.mv155_assert_case_backfill()`) and no owner-assuming CHECK — confirm that. If it drifted and did add one, plan to re-express it as an implication (`owner is null or …`) in this migration.
4. **Write one migration**, `supabase/migrations/<timestamp>_stage2_owner_nullable_case_fk_rebase.sql`, with a timestamp strictly later than MV-155's. Statements **in this order** (the order is load-bearing):
   1. `unique (id, case_id)` on `program_predictions` and `application_attempts`.
   2. `foreign key (prediction_id, case_id)` on `application_attempts`; `foreign key (attempt_id, case_id)` on `outcome_events`.
   3. Covering indexes `application_attempts (prediction_id, case_id)` and `outcome_events (attempt_id, case_id)`.
   4. `check (owner is not null or case_id is not null) not valid`, named `<table>_ownership_axis_present`, on all three, then `validate constraint` on each. **Not** `check (case_id is not null)` — that shape raises `23514` on every live owner-only outcomes insert (including `service_role`, which does not bypass a CHECK) for the whole MV-156 → MV-157 window.
   5. `user_program_state`: add `id uuid not null default gen_random_uuid()`, drop the composite PK, add the surrogate PK, create `unique index … (owner, program_id) where owner is not null`. Repeat for `document_status` with `(owner, kind)`.
   6. The same `check (owner is not null or case_id is not null) not valid` + `validate constraint`, under the same `<table>_ownership_axis_present` name, on the five non-chain tables — so all eight carry one family with one shape.
   7. **Last:** `alter table … alter column owner drop not null` on all eight, one statement each, in the order the acceptance criteria list them.
   Leave the legacy `unique (id, owner)` constraints and the `(prediction_id, owner)` / `(attempt_id, owner)` FKs **in place** — MV-160 drops them. Footer-comment the exact names MV-160 must drop, in drop order: all eight `_ownership_axis_present` checks (`profiles_`, `plan_items_`, `user_program_state_`, `documents_`, `document_status_`, `program_predictions_`, `application_attempts_`, `outcome_events_ownership_axis_present`); then the two legacy composite FKs `(prediction_id, owner)` and `(attempt_id, owner)`; then — and only then, because the FKs depend on them — the three `unique (id, owner)` targets and the orphaned covering index `outcome_events_attempt_id_owner_idx`.
5. **Apply, regenerate, test** (PowerShell):
   - `npx supabase start`, then `npx supabase db reset`.
   - `npx supabase gen types typescript --local > lib/supabase/types.ts` — then restore the `__InternalSupabase.PostgrestVersion: "14.5"` pin by hand and drop the added `graphql_public` block; CLI 2.107.0 produces both deltas and MV-150 recorded them.
   - `npx supabase status -o env` → set `$env:SUPABASE_TEST_URL`, `$env:SUPABASE_TEST_SERVICE_ROLE_KEY`, `$env:SUPABASE_TEST_ANON_KEY`.
   - Write `tests/integration/owner-nullable-rebase.itest.ts` (hard-guard + `skipIf` copied from `tests/integration/anon-purge.itest.ts`; psql catalog reads copied from `tests/integration/tenancy-schema.itest.ts`), then `npm run test:integration`.
   - Fix the `owner: string | null` typecheck failures at the sites listed in the acceptance criteria — **no `!`, no `as string`**.
6. **Rehearse on a copy of live data before proposing the production apply.** Restore a dump into a scratch database, apply MV-155 then MV-156, run the itest, capture per-table row counts and a row-identity diff before/after, then replay the rollback script and confirm the copy returns to its pre-migration shape. Put the commands and counts in Done evidence. The production apply is founder-gated.
7. **Full gate:** `npm run typecheck` · `npm run lint` · `npm test` · `npm run test:integration`. Open a PR — `master` takes no direct push, and both `integration` and `validate` must be green and up to date.
8. **Do NOT:** add `case_id` or backfill anything (MV-155); change any RLS policy, helper or grant (MV-159); make `case_id` NOT NULL or drop the legacy owner chain (MV-160); re-key any repository onto the case (MV-157); touch `private.reject_prediction_update` (MV-155); touch `assessments`, the document model, or `storage.objects` at all.

## Decision log
- 2026-08-02 — Card carved from Stage 2 of the revised consultancy plan (integrator session).
- 2026-08-02 — **The eight are enumerated on the card, and `assessments` is explicitly excluded.** The plan names the eight in prose; the card pins each to its defining migration and its exact `owner` declaration, because two of them (`user_program_state`, `document_status`) are not the simple case the prose implies. `assessments.owner` has been nullable since day one — that is what an anonymous assessment *is* — so it is the ninth student-owned table and not part of this change.
- 2026-08-02 — **`user_program_state` and `document_status` need a PK replacement, not a `drop not null`.** Both put `owner` in their PRIMARY KEY, which is NOT NULL by definition. Surrogate `id uuid` PK + a partial unique on the legacy pair `where owner is not null` preserves today's rule exactly for owner-scoped rows while letting NULL-owner rows exist. Verified no FK targets either PK, so nothing is re-pointed by the swap.
- 2026-08-02 — **The load-bearing target is swapped to the case; the legacy owner chain is retained through the window and dropped by MV-160.** A hard swap was considered and rejected: both chains are MATCH SIMPLE and each enforces exactly on rows where its own column is non-null, so dropping the owner chain now would remove enforcement from the 39 existing legacy rows for the whole MV-156→MV-160 window while buying nothing. Two coexisting redundant constraints cost two indexes and forbid no legitimate shape.
- 2026-08-02 — **A validated CHECK was chosen over `MATCH FULL` as the compensating constraint.** `MATCH FULL` would also force `case_id` non-null (the FK's other column, `prediction_id`/`attempt_id`, is NOT NULL, so all-or-nothing nullness collapses to "case_id required"). Rejected because that is precisely the shape this card must *not* impose during the window (see the entry below): a `MATCH FULL` violation would take the live owner-only outcomes insert down, would raise a foreign-key error naming the parent table rather than saying what is missing, and is unfamiliar enough that a future reader is likely to "restore" it to the default and silently re-open the hole. The CHECK states the requirement in the language of the requirement.
- 2026-08-02 — **Seam check: the compensating check on the three chain tables is the DISJUNCT, not `case_id is not null`.** The first draft shipped a validated `check (case_id is not null)` on `program_predictions` / `application_attempts` / `outcome_events`, on the argument that it is the documented path to a scan-free `SET NOT NULL` in MV-160. That argument is real and it is outweighed: a CHECK is role-independent, `service_role` does not bypass it, and every live insert on those three tables today (`lib/outcomes/on-apply.ts` `captureApplication` → `insertPrediction`/`insertAttempt`/`insertEvent`, and `app/api/outcomes/{prediction,attempt,event}/route.ts`) writes `owner` and no `case_id`. That draft therefore took the production outcomes write path down with `23514` from the moment this migration applied until MV-157 deployed — a window this card does not control. All eight tables now carry the same `check (owner is not null or case_id is not null)`, which forbids exactly the uncovered shape (both axes null) and nothing else, because both chains are retained. **Consequence accepted and handed to MV-160:** its `SET NOT NULL` on those three columns no longer has a matching validated check to skip the verification scan, so it takes a full scan under ACCESS EXCLUSIVE — negligible at 39 rows, and already flagged in MV-160's own risk note.
- 2026-08-02 — **The `ownership_axis_present` check goes on all eight, beyond the plan's letter.** The plan requires a compensating check only for the three chain tables. But relaxing `owner` on the other five newly permits a row with `owner IS NULL AND case_id IS NULL` — owned by nothing, matched by no RLS policy, reached by no cascade, and deletable only by service-role. One check shape across all eight forbids that at negligible cost and gives MV-160 a single family to drop by name. Flagged as an addition so the integrator can trim the five if the seam is judged to belong to MV-160; the three are not optional.
- 2026-08-02 — **`drop not null` is the last statement group, inside one transactional migration.** Ordering the case chain and every check before it means there is no instant — not even mid-apply on a failed run — where `owner` is nullable and the case chain is absent. The plan's "keep the nullable window short" is therefore satisfied twice over: the intra-migration window is zero, and the stage-level window (`case_id` nullable in the column definition, MV-155→MV-160) is covered for the three chain tables on day one by the retained owner chain plus the validated `_ownership_axis_present` disjunct, leaving no row uncovered by *some* chain and only the declared type loose.
- 2026-08-02 — **The MATCH SIMPLE hole is proven by a test, not described in a comment.** Following MV-153's precedent that a claim about a safeguard must be backed by a mechanism that fails when the claim stops being true, the itest drops the compensating check in a scratch transaction and asserts the database *accepts* a cross-case attempt. A comment saying "do not remove this check" is advisory; a test that goes red is not.
- 2026-08-02 — **Type honesty is an acceptance criterion, not a cleanup.** `owner: string | null` is the whole point of the card surfacing in TypeScript. Banning `!` and `as string` at the ~14 named sites is what stops the eight-table relaxation from being cosmetically undone in the layer MV-157 has to change next.

## Done evidence
(pending)
