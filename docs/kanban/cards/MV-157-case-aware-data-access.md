# MV-157 — Case-aware data access: repositories and routes move off actor-equals-student onto case authorization + `case_id` (dual-write)

**Priority:** P1   **Owner:** agent
**Goal:** Move every server repository, route handler, and Server Component that reads or writes student-owned data off the `owner = actor` predicate onto a resolved `case_id` plus MV-151's `requireCasePermission` — dual-writing `owner` only where the case has an Auth user — so the codebase stops assuming the signed-in person is the student, while an existing student opening the dashboard, matches, plan, documents, checklist, guide, or outcomes sees byte-for-byte the same data as before.

## Context links
- **The two sequence steps this card is:** plan `docs/superpowers/plans/2026-07-23-consultancy-student-case-workspace.md` §"Additive migration sequence" **step 9** ("Temporarily write both the legacy owner field and `case_id` for rows that have an owning Auth user; consultancy-created rows carry `case_id` only", line 312) and **step 10** ("Move server repositories and routes to case authorization and `case_id`", line 313). Steps 2-7 are MV-155, step 8 is MV-156, step 11 is MV-159, steps 12-13 are MV-160.
- **The domains and the uniqueness rules that move:** same plan, §"Moving existing data to cases" (lines 277-300) — the domain list (profiles, assessments, plan items, saved/tracked programs, predictions, attempts, outcome events, document status/records) and the five `owner -> case` uniqueness rewrites at lines 292-300.
- **The exit-gate half this card owns:** same plan, "Stage 2 — Case-aware core" exit gate (line 637): *"existing students see the same correct data, while case-scoped repositories no longer depend on actor equals student."* MV-157 delivers the second clause and keeps the first true; **MV-160 proves both against production-shaped data and carries the stage exit.**
- **Enforcement boundary — why this is not just a column swap:** same plan, §"Authorization and tenant isolation" → "Enforcement boundary" (lines 336-345): the authenticated client becomes the default for every tenant read and write, and the service-role client shrinks to a named, justified, case-authorized exception list. Enforcement layer 1 (lines 362-366) is the rule this card makes real: *"verify access inside every Server Component, Route Handler, Server Action, and mutation"*, and *"do not treat routing middleware as the authorization boundary."*
- **Caching rules that now bind these routes:** same plan, lines 384-391 — case-scoped routes render dynamically, any deliberate cache key carries **both** actor and case, `unstable_cache`-style caching of personalized reads is banned by default.
- **The authorization model these calls must not contradict:** `docs/superpowers/specs/2026-08-02-stage1-canonical-access-matrix.md` — authoritative for every cell. A personal case (`organization_id is null`, `student_user_id = actor`) satisfies the **student** row: `case.read` / `case.update` scope `linked`, and no `all-org` claim. Nothing in this card changes a cell; if a call site needs a cell that does not exist, the matrix is amended first, not the call site.
- **The layer being consumed:** `lib/cases/README.md` — the API table, the dual-role rule, "read the scope on an allow", and the **Stage 3 review-gate checklist** (README lines 55-79), which this card is the first PR to actually run against. Also its footnote 1 "Known gap — student permitted fields": `case.update` authorizes the *claim*, not the *field set*.
- **MV-153's Finding 1, which binds every mutation this card touches:** `docs/kanban/cards/MV-153-cross-tenant-negative-tests.md` §Findings — *"`requireCasePermission(actor, caseId, "case.update")` is not sufficient authorization to apply an arbitrary case patch on a student's behalf — today RLS is the only thing stopping it."*
- **The registry this card rewrites:** `lib/supabase/service-role-exceptions.ts` — its own §"STAGE 1 STATUS" says the ten `legacy-owner-scoped` entries "authenticate with `supabase.auth.getUser()` and then filter by `.eq("owner", userId)` against the service-role client. That is precisely the default the plan inverts, and **migrating them onto the authenticated client is Stage 2 work**."
- **The real legacy schema** (owner columns, unique indexes, grants — read before writing anything): `supabase/migrations/20260603011208_init_assessments_and_leads.sql` (assessments; `authenticated` holds **SELECT only**), `20260603170655_add_profiles_evolve_assessments.sql` (`profiles.owner uuid not null unique` line 4; `assessments_primary_idx on (owner) where is_primary` line 59; `authenticated` holds select+update, **no insert**), `20260604002139_add_programs_universities_state.sql` (`user_program_state` **primary key (owner, program_id)**; `authenticated` holds full CRUD), `20260604024609_add_plan_items.sql` (`plan_items_kind_open_idx on (owner, kind) where status='todo'` line 18; select+update, no insert), `20260604060000_add_documents.sql` (`unique (owner, kind)` line 21), `20260626000000_add_document_status.sql` (**primary key (owner, kind)**; full CRUD), `20260620000000_add_outcome_validation.sql` (`unique (id, owner)` composite-FK targets + the immutability trigger; select/insert/delete).
- **Siblings:** MV-154 (Stage 2 umbrella; owns the rehearsal-and-rollback requirement — which binds MV-155/MV-156/MV-160, **not** this card, since this card ships no migration), MV-155 (personal cases + `case_id` columns + backfill — the hard prerequisite), MV-156 (owner NULLABLE + composite-FK rebase), **MV-158 (anonymous claim — the hardest dual-write site, explicitly NOT this card)**, MV-159 (case-aware RLS on these same tables), MV-160 (tighten + stage exit). Exemplar dossiers: `docs/kanban/cards/MV-150-tenancy-core-schema.md`, `docs/kanban/cards/MV-153-cross-tenant-negative-tests.md`.

## Acceptance criteria

### A. One case resolver, one authorization call, no actor-as-subject
- [ ] A single server-only resolver — `lib/cases/personal-case.ts`, exporting `resolvePersonalCaseId(actorUserId, db?)` and `ensurePersonalCase(actorUserId, db?)` — is the **only** way a personal route turns an actor into a case. It resolves `cases where student_user_id = actor and organization_id is null`; `ensurePersonalCase` is idempotent under `cases_personal_student_idx`, the partial unique index on `cases (student_user_id) where organization_id is null` that **MV-155 §A ships unconditionally**. This card asserts that index exists and **stops if it is absent** — an absent index means MV-155 did not land as specified, and re-creating it here would put the same DDL in two migrations.
- [ ] **Go-forward personal-case creation is wired.** MV-155 backfills cases for *existing* owners at migration time; a user who signs up afterwards must get one too. `ensurePersonalCase` is called from the non-claim sign-in seam (`lib/auth/finish-sign-in.ts`'s no-pending-claim branch), so no path can create student-owned data for an actor with no case. The claim branch of that same seam is **MV-158's**.
- [ ] Every migrated route and Server Component calls `requireCasePermission(actorUserId, caseId, <verb>)` (or the `check*` form where denial is an expected branch) **before its first read or write** — `case.read` for reads, `case.update` for mutations — and `actorUserId` comes from `supabase.auth.getUser()` on the server, never from a body, query param, header, or prop.
- [ ] **No repository resolves a case or authorizes anything.** Repos accept an already-resolved, already-authorized `caseId`; authorization lives at the entry point. A repo that calls `requireCasePermission` internally is a defect (it hides the check from the reviewer's grep and re-authorizes per query).
- [ ] The Stage 3 review-gate checklist in `lib/cases/README.md` (lines 55-79) is walked for this PR and the result recorded in Done evidence, item by item. Where an item cannot yet be satisfied (the DB half is MV-159), that is stated, not ticked.

### B. Repositories that move (all eight modules, read side and write side)
Each function below loses its `owner` predicate/parameter and takes `caseId`; the legacy `…ForUser(db, userId)` names go with them (rename to `…ForCase(db, caseId)`), so a stale call site fails to compile rather than silently reading by owner.
- [ ] `lib/profiles/repo.ts` — `getProfile`, `upsertProfile`, `patchProfileSection` (3).
- [ ] `lib/assessments/repo.ts` — the **read** side only: `getPrimaryAssessmentForUser`, `listAssessmentsForUser`, `getOwnedAssessment`, `getRecoverableAssessment`, `getAssessmentClaimState` (5). `createAnonymousAssessment` and `claimAssessment` are **MV-158's**; they keep working unchanged here.
- [ ] `lib/plan/repo.ts` — `listOpenPlanForUser`, `listAllPlanForUser`, `setPlanItemStatus`, `setPlanItemStarted`, `getPlanItemKind` (5).
- [ ] `lib/matches/repo.ts` — `upsertProgramState`, `deleteProgramState`, `listShortlistForUser` (3). **The `onConflict: "owner,program_id"` string moves in the same commit**, onto MV-155's case-scoped target (see section F).
- [ ] `lib/documents/repo.ts` — `listDocumentsForUser`, `getDocumentByKind`, `listDocumentsByKinds`, `insertDocument`, `upsertDocument`, `deleteDocument`, `getSignedDocumentUrl` (7). **Row-level `case_id` only — the header/versions replacement is Stage 4.**
- [ ] `lib/documents/status-repo.ts` — `listObtainedKinds`, `setObtained` (2).
- [ ] `lib/outcomes/repo.ts` — `insertPrediction`, `getPredictionById`, `insertAttempt`, `getAttemptById`, `listAttemptsForPrediction`, `listEventTypesForAttempt`, `insertEvent`, `getOutcomesForUser` (8). Predictions stay insert-only: **no UPDATE is introduced** (the immutability trigger raises for every role, service_role included; the `case_id` backfill path is MV-155's).
- [ ] Orchestrators above the repos, which currently thread a `userId` through: `lib/plan/invalidate.ts` (`invalidatePlan`), `lib/outcomes/freeze.ts`, `lib/outcomes/on-apply.ts` (`captureApplication`), `lib/assessments/re-score.ts`, `lib/journey/signals.ts`. Each takes `caseId`.
- [ ] `lib/assessments/purge.ts` is **unchanged and asserted unchanged**: anonymous rows are case-less by design (MV-155 / MV-135's 3-day policy) and the purge predicate must not learn about cases.
- [ ] After the sweep, `grep -rn '\.eq("owner"' lib/ app/` returns hits in **only** the claim path (MV-158's `lib/assessments/repo.ts` conditional update), the purge path, and `app/api/account/delete/route.ts`'s Auth-account teardown. A CI-visible test pins that allow-list so a re-introduced owner predicate fails rather than merges.

### C. Routes that move (15)
Each authorizes, then reads/writes by `case_id`:
- [ ] **Mutations** — `app/api/profile/section/route.ts`, `app/api/plan/action/route.ts`, `app/api/shortlist/route.ts`, `app/api/documents/upload/route.ts`, `app/api/documents/[id]/route.ts` (DELETE), `app/api/documents/status/route.ts`, `app/api/outcomes/prediction/route.ts`, `app/api/outcomes/attempt/route.ts`, `app/api/outcomes/event/route.ts`, `app/api/assess/route.ts` (the signed-in insert branch), `app/api/assess/refresh/route.ts`.
- [ ] **Reads** — `app/api/outcomes/route.ts`, `app/api/guide/chat/route.ts` (primary assessment + open plan), `app/api/documents/[id]/view/route.ts` (authorize the case **before** minting the signed URL; the mint itself and the at-mint audit event stay Stage 4).
- [ ] **Account teardown** — `app/api/account/delete/route.ts` deletes the actor's case-scoped rows **and** their personal `cases` row, and still leaves any consultancy case intact (plan line 514: deleting an account must not delete data belonging to another valid relationship). Cross-checked against MV-155's backfill so no orphan is left behind.
- [ ] **Explicitly NOT here:** `app/api/assess/claim/route.ts`, `app/api/results/sign-claim/route.ts`, `lib/auth/finish-sign-in.ts`'s claim branch, `lib/assessments/claim.ts` → **MV-158**. `app/api/cron/purge-anonymous/route.ts` → case-less, unchanged. `app/api/leads/route.ts`, `app/api/auth/email/*`, `app/api/dev/sign-in/route.ts` → no case-scoped data (dev sign-in's fixture seeding calls `ensurePersonalCase` so the harness matches production).

### D. Server Components that move (11)
- [ ] `app/(app)/dashboard/page.tsx`, `app/(app)/matches/page.tsx`, `app/(app)/plan/page.tsx`, `app/(app)/documents/page.tsx`, `app/(app)/profile/page.tsx`, `app/(app)/guide/page.tsx`, `app/(app)/checklist/page.tsx`, `app/(app)/checklist/all/page.tsx`, `app/(app)/checklist/[programId]/page.tsx`, `app/(focused)/assess/page.tsx` — each resolves the personal case once, authorizes once, and passes `caseId` down; no page resolves a case twice or per-repo-call.
- [ ] `app/(focused)/assessment/[id]/page.tsx` — the "knowing the id is the credential" page, and the one MV-151's registry says "Stage 2 must re-scope it". After this card: a **claimed** assessment (non-null `case_id`) is gated by `requireCasePermission(actor, case_id, "case.read")` and 404s for anyone else; an **unclaimed, case-less** row keeps id-as-credential until MV-135's purge takes it. Plan line 354 ("knowing a case ID grants no access") is about cases; it does not retroactively govern a pre-claim anonymous row, and that distinction is written into the file.

### E. The dual-write rule (step 9), stated as code, not as discipline
- [ ] **Writes:** `case_id` is written **always**; `owner` is written **only when the case has a `student_user_id`**. Consultancy-created rows (no student user) carry `case_id` with `owner` NULL — possible only once MV-156 lands, which is why MV-156 is a hard blocker for the write half.
- [ ] **Reads:** filter on `case_id` **only**. No migrated read retains an `owner` predicate — not "as a belt", not "for the index". A residual owner filter makes every case-scoping bug invisible until MV-159 (see Risk 1).
- [ ] **`owner` is never a caller-supplied parameter next to `case_id`.** One internal writer helper derives it from the case row (`cases.student_user_id`) so a caller structurally cannot make the two disagree. A repo signature accepting both is rejected in review.
- [ ] **The owner/case agreement has NO live database enforcement during Stage 2.** MV-155 §F is explicit: the invariant is cross-table, no `CHECK` can express it, and a `NOT VALID` check was considered and rejected. What MV-155 ships is `private.mv155_assert_case_backfill()` — an assertion **function**, called by its migration and by tests. It is a detector, not a constraint: a mismatched write is **not** rejected at write time. The single writer helper that derives `owner` from `cases.student_user_id` (criterion above) is therefore the **only** guard until MV-160, and this card proves it holds by calling `private.mv155_assert_case_backfill()` at the end of **every** mutating integration test and asserting it returns clean.

### F — Uniqueness (owned by MV-155). **This card ships NO migration.**
Plan step 7 ("add case indexes and consistency constraints") belongs to **MV-155 §E**, which already ships all six case-scoped uniqueness rules, each partial on `case_id is not null`, beside the legacy owner-scoped ones: `profiles unique (case_id)`, `assessments unique (case_id) where is_primary`, `plan_items unique (case_id, kind) where status = 'todo'`, `user_program_state unique (case_id, program_id)`, `documents unique (case_id, kind)`, `document_status unique (case_id, kind)`. Two cards shipping the same six indexes means one migration fails on apply or is a silent no-op, and it makes both cards' rehearsal logs and rollbacks wrong. What remains genuinely this card's is the **application side** of those rules:
- [ ] **The `onConflict` targets move with the indexes.** Change `lib/matches/repo.ts`'s `upsert(…, { onConflict: "owner,program_id" })` to MV-155's case-scoped target, and audit the same hazard in `upsertProfile`, `upsertDocument` and `setObtained`. PostgREST's `onConflict` is an unchecked string the type system never validates (Risk 3): pointing it at an index that does not exist turns an upsert into a plain insert or a `42P10`, and pointing it at the *legacy owner* index silently keeps the write owner-scoped while every read has moved to `case_id`.
- [ ] **Every `onConflict` string in the codebase is enumerated in the PR body**, with the index each one names and whether it moved. Each is covered by a **real-DB upsert-twice test** — upsert the same logical row twice and assert one row, not two — because that is the only assertion that distinguishes "conflict target matched" from "conflict target silently missed".
- [ ] **Assert, do not create.** MV-155's six indexes are verified present at the start of this card (`\d+` or `pg_index`, not the dossier); if any is absent, stop and fix MV-155 rather than adding it here. The composite-FK targets `unique (id, owner)` → `unique (id, case_id)` on predictions/attempts/outcome_events are **MV-156's**.
- [ ] **Inherited precondition to VERIFY, not a finding to flag:** `user_program_state` (`primary key (owner, program_id)`) and `document_status` (`primary key (owner, kind)`) cannot have a nullable PK column. **MV-156 is this card's direct predecessor and already resolves it** — surrogate `id` PK plus a partial unique on the legacy pair. Confirm both tables arrive with the surrogate PK in place; if they do not, MV-156 did not land as specified and this card is blocked, not empowered to improvise.

### G. The service-role exception registry is rewritten, not left stale
- [ ] Every entry currently marked `legacy-owner-scoped` in `lib/supabase/service-role-exceptions.ts` either (i) **flips to the authenticated client and is removed from the list**, or (ii) keeps its entry with a `requiredCaseCheck` that names the actual `requireCasePermission` call now preceding it, plus a one-line reason the flip is deferred. **No entry keeps its Stage-1 prose.**
- [ ] **`app/api/shortlist/route.ts` flips and leaves the list** — `user_program_state` already grants `authenticated` full CRUD (`20260604002139`, line 84), so the admin client there was never load-bearing; `captureApplication` already runs on the RLS-scoped client.
- [ ] Flips that require a grant the table does not yet give `authenticated` (`assessments` SELECT-only; `profiles`/`plan_items` no INSERT; `documents` no INSERT) are **deferred to MV-159's grant review**, and each entry says so by name. **Do not add grants in this card** — grants are reviewed once, with the policies.
- [ ] Storage-administration entries (`documents/upload`, `documents/[id]`, `documents/[id]/view`) stay service-role under the plan's sanctioned category, but each now carries a real case check before the Storage call.
- [ ] The ESLint fence and `tests/supabase/service-role-exceptions.test.ts` stay green with **no rule weakened** and no new unregistered call site.

### H. The personal student experience is preserved byte-for-byte
- [ ] For a seeded personal case, **every migrated repo function returns a payload deep-equal to what the owner-keyed version returned** for the same fixture, and every migrated route returns a byte-identical JSON body and status. Committed as snapshots in this PR so a later refactor cannot quietly change them.
- [ ] The dashboard, matches, plan, documents, checklist, guide, profile, and assess pages render the same content for an unchanged fixture user (existing component/page tests pass unmodified — a test that had to be *edited* to pass is a behaviour change and must be justified on the card).
- [ ] **The production-shaped before/after regression against the 9 auth users / 7 profiles / ~76 assessments / 74 plan items / 12 program-state rows / 6 documents / 10 predictions / 10 attempts / 19 outcome events is MV-160's**, and the stage exit with it. This card's obligation is that nothing it ships makes that regression fail.

### I. Caching
- [ ] No migrated route or page introduces `unstable_cache`, a fetch cache, `export const revalidate`, or static generation; every one renders dynamically (plan lines 386-390). Any deliberate cache key added anywhere in this PR contains **both** the actor and the case; a key omitting either is a defect.

### J. Non-goals, stated so a cold agent does not absorb a sibling
- [ ] **Deploy coupling: MV-157 and MV-158 ship as ONE release train.** This card may **merge** ahead of MV-158, but it must not reach **production** until MV-158 is merged. The reason is concrete: this card moves the assessment reads onto `case_id` while `lib/assessments/claim.ts` still writes `owner` only, so any assessment claimed in that window is written `owner`-set / `case_id`-null by the untouched claim path — and is **invisible** to the case-scoped dashboard the student lands on immediately afterwards. The PR body states this, and the founder gate on the production deploy names MV-158 as its precondition. **An `owner` fallback on the assessment reads is explicitly REJECTED as the alternative:** it would defeat section E ("no migrated read retains an `owner` predicate — not as a belt, not for the index") and hide exactly the case-scoping bugs Risk 1 exists to surface. The fix for the window is to close it, not to paper over it.
- [ ] Anonymous claim / create-or-resolve-a-case-at-claim (**MV-158**) · `owner` NULLABLE + composite-FK rebase (**MV-156**) · case-aware RLS + grant review on these tables (**MV-159**) · `case_id` NOT NULL, dropping compensating checks and legacy indexes (**MV-160**) · document header/versions + case-aware Storage paths + at-mint audit (**Stage 4**) · any `/workspace/[orgSlug]/…` route or consultancy UI (**Stage 3**). Absorbing any of these breaks the seam.

## Test plan
- **Unit / semantics (`npm test`).** Extend the existing per-repo suites under `tests/lib/`, `tests/plan/`, `tests/outcomes/`, `tests/documents/`, `tests/profiles/`, `tests/matches/` so each migrated function is asserted to (a) query by `case_id`, (b) write `case_id` unconditionally, (c) write `owner` **only** when the fixture case carries a `student_user_id`, and (d) reject a call that supplies `owner` directly. Reuse `tests/helpers/fake-case-db.ts` (MV-151's in-memory fake) for the resolver and permission calls — and note in the file, as `lib/cases/README.md` insists, that these prove *semantics only* and cannot prove the database denies anything.
- **Route-level (`npm test`).** For each of the 15 routes: an authorized happy path; an **unauthorized** path where `requireCasePermission` denies and the route 403s **before any query is issued** (assert the db mock recorded zero calls — "authorized after reading" is the defect this catches); and a byte-identical-response snapshot against the pre-card fixture.
- **Real DB (`npm run test:integration`) — new `tests/integration/case-data-access.itest.ts`,** extending the localhost hard-guard + env-gated `skipIf` idiom from `tests/integration/anon-purge.itest.ts` and the RLS-client factory from `tests/integration/fixtures/tenancy.ts`:
  - **The case_id filter is what selects the rows, proven with RLS out of the way.** Seed two personal cases, then issue each migrated read through the **service-role** client (RLS bypassed) and assert it returns case A's rows and none of case B's. This is the only probe that can see a case-scoping bug while the legacy owner-scoped policies are still in force (Risk 1); a read issued as the authenticated user is silently ANDed with `owner = auth.uid()` and would pass regardless.
  - **Dual-write:** every insert path leaves `case_id` set and `owner` equal to the case's `student_user_id`. A deliberate mismatched write (service-role, bypassing the writer helper) is **DETECTED** by `private.mv155_assert_case_backfill()` — and the test says so in a comment: **nothing rejects it at write time**. There is no constraint to catch this until MV-160; the assert function is a detector the tests call, and the writer helper is the only thing standing between a mismatched write and the database. Every mutating case in this suite ends with that call.
  - **MV-155's six case-scoped unique indexes bite,** each with a paired positive control (the legal second row is accepted), so "nobody can insert" cannot pass as "uniqueness works". This card creates none of them — it asserts them present and proves the application writes conflict against them.
  - **Every `onConflict` target actually matches:** for each upsert path (`upsertProgramState`, `upsertProfile`, `upsertDocument`, `setObtained`), upsert the same logical row twice and assert **one** row exists. A missed conflict target does not error — it inserts a duplicate or raises `42P10` — so this is the only assertion that distinguishes a moved target from a stale one.
  - **Both index generations coexist:** for a personal case, a violation of the legacy owner-scoped index and a violation of the new case-scoped index are the same write — asserted, because that equivalence is the whole reason the legacy indexes are safe to keep.
  - **Personal-case 1:1:** `ensurePersonalCase` called twice for one actor yields one case; MV-155's `cases_personal_student_idx` on `cases(student_user_id) where organization_id is null` is asserted present.
  - **Anonymous rows stay case-less:** creating an anonymous assessment writes `case_id is null`, and the purge predicate still matches it.
- **Not tested here, and why:** cross-tenant denial on these tables (no case-aware policy exists until **MV-159**; the DB still enforces the legacy owner predicate) and the production-shaped before/after regression (**MV-160**). Both are recorded in the suite as deferred so a cold agent does not read silence as coverage.

## Integration gate
`npm run typecheck` · `npm run lint` · `npm test` · **`npm run test:integration`** (mandatory — this card changes which predicate selects a row and which index an upsert conflicts against; unit tests against a fake cannot prove either). **This card ships no migration** — plan step 7's indexes are MV-155 §E's (see section F).

**Repo posture (changed since Stage 1 — this is not advisory any more).** `master` is protected by an active ruleset with **no bypass**: required PR, no direct pushes, no force pushes, and two required status checks — **`validate`** (`npm ci` → typecheck → lint → test → build) and **`integration`** (`npx supabase start` → `npm run test:integration`) — both **strict / up-to-date with master**. `continue-on-error` is gone from the `integration` job, so a red integration lane blocks the merge. Locally: `npx supabase start`, then export `SUPABASE_TEST_URL`, `SUPABASE_TEST_SERVICE_ROLE_KEY`, `SUPABASE_TEST_ANON_KEY` from `npx supabase status -o env`. Run `npm run board` after any `board.json` edit and commit the regenerated views.

## Dependencies / blocked-by
- **MV-155 (hard blocker, read side):** `case_id` must exist and be backfilled on all nine student-owned tables, and every existing owner must already have a personal case. Reading by `case_id` against a partially-backfilled table silently returns nothing.
- **MV-156 (hard blocker, write half only):** the consultancy branch of the dual-write rule (`case_id` set, `owner` NULL) is impossible while `owner` is NOT NULL on the eight tables. The personal branch works without it, so the card can *start* on MV-155 alone — but it cannot be called done. MV-156 must also have replaced the `user_program_state` and `document_status` primary keys (both carried `owner` as a key column, which no `drop not null` can relax); this card verifies the surrogate PKs on arrival rather than raising it as a finding.
- **MV-151 (merged):** `getCaseContext`, `requireCasePermission`, `requireOrgPermission`, the permission matrix, the service-role exception registry + ESLint fence.
- **MV-152 / MV-153 (merged):** the `cases` policies the resolver reads through, and the harness whose fixtures and RLS-client factory this card's itest extends.
- **Blocks:** **MV-159** (its policies replace the owner predicate these repos have just stopped using — land MV-157 first, per sequence steps 10 → 11) and **MV-160** (which cannot prove "no repository assumes actor equals student" until this card has made it true).
- **Blocks deploy of — and is deploy-blocked BY — MV-158.** This card may merge first; it may not reach production first. Until MV-158 lands, `lib/assessments/claim.ts` writes `owner` only while this card's assessment reads filter on `case_id` alone, so a claim in that window yields an assessment the student cannot see. The two are one release train (criterion J) and the production founder gate on MV-157 names MV-158 as its precondition. MV-158's Dependencies section carries the mirror of this rule; the conditional "MV-157 must keep an owner fallback until this card merges" wording it originally carried is **superseded** — section E forbids the fallback, and closing the window is the agreed remedy.
- **Coordinates with MV-158 (ordering is load-bearing):** MV-158 owns the claim path, which writes the same `assessments` rows whose primary-assessment uniqueness MV-155 re-scoped. Neither card may drop the legacy `assessments_primary_idx`; MV-160 does, and only after both have landed. See Risk 5.
- **Not blocked by the Stage 0 legal gate** (decision record D-B). This card ships no migration, so MV-154's rehearsal-on-a-copy obligation does not attach to it — but the founder gate on the production **deploy** stands, and per criterion J it now names MV-158 as a precondition.
- Local Supabase stack (Docker + `supabase` CLI) to apply, regenerate types, and run the itest.

## Risk notes
1. **The reads-move-first blind spot — highest risk of false confidence.** Until MV-159, the database still enforces the legacy owner-scoped policies on these nine tables. A read issued through the **authenticated** client with a `case_id` filter is therefore silently ANDed with `owner = auth.uid()`, so a wrong, missing, or accidentally-still-owner-scoped `case_id` filter produces correct results in Stage 2 and only fails when MV-159 removes the owner predicate — i.e. in the next card, in a different PR, with a different author. Mitigation is structural and non-negotiable: **the case-scoping assertions run through the service-role client with RLS bypassed**, so the `case_id` filter alone is what selects the rows. A test that proves case scoping while RLS is helping proves nothing.
2. **Dual-write divergence.** `owner` and `case_id` pointing at different people is the corruption this stage can produce and the hardest to detect later. There is **one** defence, not two. `owner` is derived inside one writer helper from `cases.student_user_id` and is never a caller parameter — and that is the whole of it. MV-155's `private.mv155_assert_case_backfill()` is a **detector, not a lock**: it is a function the migration and the tests call, and it rejects nothing at write time (MV-155 §F — the invariant is cross-table and no `CHECK` can express it). So the helper is load-bearing and unbacked until MV-160 makes `case_id` NOT NULL. Compensate with frequency rather than a second lock: call the assert function at the end of every mutating integration test, and treat any code path that writes `owner` outside the helper as a defect on sight.
3. **PostgREST `onConflict` is an unchecked string.** `upsertProgramState`'s `{ onConflict: "owner,program_id" }` is text the type system never validates. Changing the uniqueness target without changing the string turns an upsert into a plain insert that 23505s — or, while both index generations exist, resolves against the *legacy* index and writes the wrong row. Same class of hazard in `upsertProfile`, `upsertDocument`, and `setObtained`. Every `onConflict` in the diff is enumerated in the PR body and covered by a real-DB upsert-twice test.
4. **Keeping both index generations is safe only because a personal case is 1:1 with an owner.** If MV-155 ever produces two personal cases for one owner, the legacy and case-scoped indexes stop agreeing and writes begin failing in production for a reason that reads like an application bug. The 1:1 property is asserted directly (the partial unique index on `cases(student_user_id) where organization_id is null`), not assumed from MV-155's prose.
5. **The MV-158 interlock — a real hole if the indexes are ever swapped instead of added (MV-155 §E adds; verify that it did).** `assessments_primary_idx` is partial-unique on `(owner) where is_primary`, and a partial unique index treats NULLs as distinct. If it were *replaced* with `(case_id) where is_primary` before MV-158 teaches the claim path to set `case_id`, a claim would write `case_id = NULL` and **two primary assessments per case would become insertable** — on exactly the table with a past primary-assessment bug (`lib/assessments/claim.ts`'s demote-then-promote comment records it). Keeping both indexes closes it: the owner-scoped index still constrains any row that has an owner. **MV-160 drops the legacy index, and only after MV-158 has landed.**
6. **`user_program_state` and `document_status` arrive with a REPLACED primary key — verify it, do not re-derive it.** Both tables historically carried `owner` inside the PK (`(owner, program_id)` and `(owner, kind)`), which a `drop not null` cannot touch. **MV-156, this card's direct predecessor, already resolves it**: a surrogate `id` PK plus a partial unique on the legacy pair. The risk here is not the finding — it is assuming the legacy PK shape while writing repository code against tables whose key columns moved underneath. Read the two tables' actual PKs at the start of the card. If either is still owner-keyed, MV-156 did not land as specified and this card is blocked.
7. **Flipping a route from service-role to the authenticated client changes what it can see.** Ten `legacy-owner-scoped` paths exist precisely because `authenticated` lacks the grant (assessments SELECT-only; profiles/plan_items/documents have no INSERT). Flipping one without the grant produces a silent empty result or a 42501 in production. Only flips that need **no** grant change happen here (shortlist); the rest wait for MV-159's grant review, and each deferred entry says which grant it is waiting on.
8. **`requireCasePermission` is not a field allowlist** (MV-153 Finding 1; `lib/cases/README.md` footnote 1). On a personal case the actor is the data subject, so the whole profile surface is legitimately theirs — but `app/api/profile/section/route.ts` must keep its own payload validation, and the allowlist question is inherited, not solved, by Stage 3's consultancy mutations. Recorded on the card so Stage 3 does not read this card's silence as a solved problem.
9. **`app/(focused)/assessment/[id]/page.tsx` is a deliberate two-regime page.** Claimed rows are case-authorized; unclaimed anonymous rows keep id-as-credential. Two regimes in one file is how "knowing the id grants access" survives a refactor by accident — the branch is explicit, commented, and covered by a test on each side.
10. **This card ships no migration, and that is a hazard of its own kind.** All the DDL it depends on — the nine `case_id` columns, the six case-scoped unique indexes, the personal-case index, the assert function (MV-155), the owner relax and the PK replacements (MV-156) — arrives from predecessors. The failure mode is therefore a *drift* failure rather than an apply failure: code written against what the sibling dossiers say instead of what the database contains. Read the end state out of `pg_index` / `pg_constraint` before writing repository code, and if something is missing, fix the card that owns it. Adding "just this one index" here re-creates the duplicate-migration problem section F exists to remove.
11. **Scope creep into MV-159 is the likely failure mode.** Having moved every read to `case_id`, the obvious next keystroke is to rewrite the RLS policies to match. Do not. Policies, helper functions, and the grant review are one reviewed unit in MV-159, and splitting them across two PRs is how a policy ships without its grant.

## Agent resume notes (for a cold start)
1. **Confirm the prerequisites are actually merged**, not just carded: `git log --oneline -20` plus `ls supabase/migrations/` should show MV-155's `case_id` migration (and MV-156's owner relax, if the write half is in scope). Then `npx supabase db reset` and check in psql that `case_id` exists and is fully backfilled on all nine tables — `select count(*) from public.profiles where case_id is null` and the same for `assessments` (anonymous rows legitimately NULL), `plan_items`, `user_program_state`, `documents`, `document_status`, `program_predictions`, `application_attempts`, `outcome_events`. **If any non-anonymous row has a NULL `case_id`, stop — reading by `case_id` will silently return nothing.**
2. **Read, in this order:** this card; plan lines 277-300 (domains + uniqueness rules), 302-318 (sequence steps 9-10), 336-345 (enforcement boundary), 362-366 (layer 1), 384-391 (caching), 637 (exit gate); `docs/superpowers/specs/2026-08-02-stage1-canonical-access-matrix.md` end to end; `lib/cases/README.md` (API + the Stage 3 checklist you are about to run); `lib/supabase/service-role-exceptions.ts` (the ten `legacy-owner-scoped` entries are your route worklist).
3. **Map the surface before changing it.** `grep -rn '\.eq("owner"' lib/ app/` and `grep -rn 'from("profiles")\|from("assessments")\|from("plan_items")\|from("user_program_state")\|from("documents")\|from("document_status")\|from("program_predictions")\|from("application_attempts")\|from("outcome_events")' lib/ app/` — the result should match sections B/C/D above. **If it does not, the card is stale: reconcile it before writing code.**
4. **Write `lib/cases/personal-case.ts` first** (`resolvePersonalCaseId` + idempotent `ensurePersonalCase`), with unit tests against `tests/helpers/fake-case-db.ts`. Everything else depends on it.
5. **Then verify the schema you are about to write against — do NOT write a migration.** `npx supabase db reset`, then read the end state from the database: MV-155's six case-scoped unique indexes and `cases_personal_student_idx`, MV-156's surrogate PKs on `user_program_state` and `document_status`, and `private.mv155_assert_case_backfill()`. Anything missing blocks this card and is fixed on the card that owns it. Types should already be current from MV-155/MV-156; if you do regenerate (`npx supabase gen types typescript --local > lib/supabase/types.ts`), **hand-restore the `__InternalSupabase.PostgrestVersion: "14.5"` pin** (MV-150's Done evidence records that the CLI drops it).
6. **Then one repo at a time, TDD:** rename `…ForUser(db, userId)` → `…ForCase(db, caseId)`, let the compiler enumerate the call sites, fix the route/page above it, and run `npm test` before moving to the next. The rename is the tool — a signature that still accepts `userId` will be silently mis-called.
7. **Then the registry:** flip `app/api/shortlist/route.ts` to the authenticated client and delete its entry; rewrite the `requiredCaseCheck` prose of every remaining `legacy-owner-scoped` entry to name the real check and the deferred flip's blocker.
8. **Then the itest:** `tests/integration/case-data-access.itest.ts`, copying the localhost hard-guard from `tests/integration/anon-purge.itest.ts` and the fixture/client factory from `tests/integration/fixtures/tenancy.ts`. Write the **service-role, RLS-bypassed** case-scoping probes first — they are the ones that can fail.
9. **Run the gate:** `npm run typecheck` · `npm run lint` · `npm test` · `npm run test:integration`. Then open a PR (master takes no direct pushes) and confirm both `validate` and `integration` are green.
10. **Do NOT:** touch the claim path (`lib/assessments/claim.ts`, `app/api/assess/claim/route.ts`, `app/api/results/sign-claim/route.ts`, `lib/auth/finish-sign-in.ts`'s claim branch) — MV-158; drop any legacy unique index — MV-160; write or alter an RLS policy, helper, or grant — MV-159; make `case_id` NOT NULL — MV-160; rebase the `unique (id, owner)` composite-FK targets — MV-156; touch the document model beyond `case_id` — Stage 4; apply anything to the hosted project — founder-gated.

## Decision log
- 2026-08-02 — Card carved from Stage 2 of the revised consultancy plan (integrator session).
- 2026-08-02 — **Uniqueness rules land as ADDITIVE indexes; the legacy owner-scoped ones survive to MV-160.** Recorded here as an inherited precondition this card depends on — MV-155 §E ships them — because the reasoning binds this card's reads and upserts: a swap looks tidier and opens a real hole: `assessments_primary_idx` is partial-unique and NULLs are distinct, so re-scoping to `(case_id) where is_primary` before MV-158 sets `case_id` at claim time would make two primary assessments per case insertable — on the exact table with a past primary-assessment bug. Both generations are equivalent for personal data (a personal case is 1:1 with an owner), so keeping both costs write amplification and buys the interlock.
- 2026-08-02 — **Repos are renamed, not just re-parameterised** (`…ForUser(db, userId)` → `…ForCase(db, caseId)`). A repo that kept its name and swapped its argument meaning would compile at every stale call site and read the wrong rows; the rename makes the compiler enumerate the surface.
- 2026-08-02 — **`owner` is derived, never passed.** One writer helper reads `cases.student_user_id`, so a caller cannot supply an `owner` that disagrees with the `case_id` it also supplied. **There is no second lock.** An earlier draft of this card called MV-155's consistency constraint the second lock; MV-155 §F ships an assertion *function*, not a constraint, because the invariant is cross-table and unexpressible as a `CHECK`. The helper is the only write-time guard in Stage 2, and the assert function is a detector the tests run.
- 2026-08-02 — **Seam check: section F stops shipping the six uniqueness indexes — MV-155 §E already does.** Plan step 7 was carded twice. Two migrations creating the same six indexes means one fails on apply or is a silent no-op, and both cards' rehearsal logs and rollbacks describe DDL that only ran once. Section F is now the application half only: moving the `onConflict` targets and proving them with upsert-twice tests. This card therefore ships **no migration** — which also removes it from MV-154's rehearsal-and-rollback obligation.
- 2026-08-02 — **Seam check: the MV-157/MV-158 deploy coupling is resolved by coupling the deploy, not by an owner fallback.** MV-158's Dependencies proposed the alternative — "if the window cannot be closed, MV-157 must keep an owner fallback on the assessment reads until this card merges" — which section E flatly forbids and section B's grep allow-list would fail. The two options were: keep a fallback (defeats section E, hides the case-scoping bugs Risk 1 exists to surface, and creates a second read path nobody removes), or ship the two cards as one release train. The train wins: merging is unblocked, only the production deploy is gated, and the gate is one sentence in the PR body plus one precondition on the founder gate. MV-158's conditional wording is superseded and should be reworded when that card is next edited.
- 2026-08-02 — **Case-scoping is asserted through the SERVICE-ROLE client, with RLS bypassed.** Until MV-159 the legacy owner-scoped policies still filter every authenticated read, so an authenticated probe cannot distinguish "the `case_id` filter is right" from "RLS saved us". This mirrors MV-153's inverse reasoning (it probes TS with the unfiltered client so a correct policy cannot answer for a broken matrix): use the client that removes the layer you are not testing.
- 2026-08-02 — **Go-forward personal-case creation is owned here, not by MV-155.** MV-155 creates cases for existing owners *at migration*; a user who signs up afterwards still needs one. `ensurePersonalCase` is called from the non-claim sign-in branch, idempotent under a partial unique index on `cases(student_user_id) where organization_id is null`. The claim branch stays MV-158's so the hardest dual-write site has one owner.
- 2026-08-02 — **The service-role flip is split from the grant review.** Only paths that need no new grant flip here (`shortlist`, since `user_program_state` already grants `authenticated` full CRUD); the rest keep an entry naming the grant they wait on, because grants and policies are one reviewed unit in MV-159 and shipping a grant ahead of its policy is how a table ends up briefly open.
- 2026-08-02 — **`app/(focused)/assessment/[id]/page.tsx` keeps id-as-credential for unclaimed rows.** Plan line 354 ("knowing a case ID grants no access") governs cases; a pre-claim anonymous assessment has no case, is purged in 3 days under MV-135, and is reachable only by an unguessable id. The two regimes are made explicit in the file rather than resolved by pretending the anonymous one does not exist.
- 2026-08-02 — **Resolved upstream, not flagged from here: `user_program_state` and `document_status` carried `owner` in the PRIMARY KEY.** A `drop not null` cannot apply to a PK column. MV-156 — this card's direct predecessor — already ships the fix (surrogate `id` PK plus a partial unique on the legacy pair), so this card's job is to **verify** the replaced PKs on arrival, not to raise the finding. The earlier "flag to MV-156" wording was written before the DAG order was settled and read as an open question after it.
- 2026-08-02 — **This card owns half the Stage 2 exit gate, and says so.** "Case-scoped repositories no longer depend on actor equals student" is delivered here; "existing students see the same correct data" is kept true here and *proved* by MV-160's production-shaped before/after regression. Splitting the claim keeps this card falsifiable without making it the stage exit.

## Done evidence
(pending)
