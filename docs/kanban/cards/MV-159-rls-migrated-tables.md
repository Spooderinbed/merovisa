# MV-159 — Case-aware RLS for the migrated student-owned tables (Stage 2 step 11)

**Priority:** P1   **Owner:** agent
**Goal:** The database — not just the server layer — enforces case scoping on the nine student-owned tables, so after Stage 2 a bug in a repository, a route, or MV-151's permission layer is still not sufficient to read or change another case's profile, assessment, plan, program state, prediction, attempt, outcome event, or document row.

## Context links
- The step this card is: plan §"Additive migration sequence" step 11 — "Enable and verify case-aware Row Level Security policies", `docs/superpowers/plans/2026-07-23-consultancy-student-case-workspace.md` line 314. Steps 2–7 are MV-155, step 8 is MV-156, steps 9–10 are MV-157/MV-158, steps 12–13 are MV-160.
- Enforcement boundary — why this card exists at all: same plan, lines 336–345. RLS evaluated **as the authenticated user** is the load-bearing tenant-isolation layer; the server data-access layer is defence in depth, and a bug in it must not be sufficient to cross a tenant. Helpers must be a small set of `SECURITY DEFINER`, `STABLE` functions with a pinned `search_path`, and the columns they read must be indexed.
- RLS layer requirements: same plan, §"Enforcement layers" item 2, lines 368–374 (enable **and** force RLS; scope to authenticated; `USING` **and** `WITH CHECK` on updates; explicit grant review; security-invoker views; index every column a policy predicate reads).
- Domains being scoped + the uniqueness rules that move: same plan, §"Moving existing data to cases", lines 277–300.
- Schema obstacles this card must respect but does **not** own: same plan, lines 324–329 — `owner` NOT NULL on eight tables (MV-156), the `program_predictions` immutability trigger (MV-155), the predictions→attempts→outcomes composite-FK rebase onto `unique (id, case_id)` (MV-156), the documents header/versions **replacement** (Stage 4), the `storage.objects` one-folder-per-user policies (Stage 4), the anonymous-claim dual-write (MV-158).
- Stage 2 and its exit gate: same plan, lines 629–637. This card does not carry the exit — MV-160 does.
- **AUTHORITATIVE for every policy, helper and grant on the nine tables — read this BEFORE writing any SQL, and transcribe from it rather than re-deriving:** `docs/superpowers/specs/2026-08-02-stage2-migration-and-access-matrix.md`. It exists because Stage 1's root cause was six slices each deriving a matrix from the plan's prose independently, and it is grounded in a live capture of the hosted project. The sections that bind this card: **§2.6** (the current policy set verbatim, with roles and `USING`/`WITH CHECK`) and **§2.7** (the exact column grants — the baseline this card pins); **§3** the invariant; **§4.1–§4.9**, whose *Policy form* row gives the policy names, the `to authenticated` clause, the transitional disjunct, the parent-case helpers and the two integrity clauses `oe_insert_case` must retain (`source = 'self_reported'`, `verified_by IS NULL`) — **§4 is this card's deliverable in table form; the SQL is transcription of it**; **§6** the deferred verbs; **§7** the grant contradiction resolved in this card's favour; **§9.1** (the `documents` bare-`auth.uid()` defect does **not** exist — do not go hunting for it), **§9.9** (`anon` holds the full grant set on `storage.objects`, unlike the nine — the same reasoning does not transfer), and **§10.1 R2** this card's rollback. Where the spec and the live schema disagree, that is a **finding to report and stop on**, never to resolve silently in a predicate.
- **Authoritative for every access-control cell:** `docs/superpowers/specs/2026-08-02-stage1-canonical-access-matrix.md`. Stage 2 extends the case model to existing student data; where a policy here disagrees with that file, the policy is wrong. The dual-role rule (§"The dual-role rule") is the one this card most easily breaks.
- Controller model — which layer each of these tables belongs to: `docs/legal/2026-07-29-stage0-decision-record.md` §D-A (Option B layered; the data map is live work item 1, and it assigns *every table* to a layer — this card's per-table matrix is the engineering half of it).
- **The pattern to mirror exactly:** `supabase/migrations/20260730180000_case_aware_rls_policies.sql` (MV-152) — the BYPASSRLS precondition `do` block, `private` SECURITY DEFINER STABLE helpers with `set search_path = ''`, `revoke all on function … from public` before granting execute to `authenticated` only, the uncorrelated-array-helper InitPlan shape (`x = any ((select private.actor_*())::uuid[])`) and why `(select f(col))` is the wrong wrapper, one policy per table per command, column-level UPDATE grants as the "cannot re-point" mechanism. Its card `docs/kanban/cards/MV-152-case-aware-rls-policies.md` carries the build-time findings verbatim; read the §"Build-time decisions" and §"Follow-up round" blocks before writing a line of SQL.
- **The legacy policies being replaced** (read all six; they are the current behaviour that must not regress): `20260603011208_init_assessments_and_leads.sql` (assessments — SELECT-only policy, nullable owner, `assessments_primary_idx` partial-unique on `(owner) where is_primary`), `20260603170655_add_profiles_evolve_assessments.sql` (profiles), `20260604024609_add_plan_items.sql` (plan_items + `plan_items_kind_open_idx`), `20260604002139_add_programs_universities_state.sql` (user_program_state, PK `(owner, program_id)`), `20260604060000_add_documents.sql` + `20260605130000_fix_documents_rls.sql` (documents — **the two owner policies carry no `to` clause**, so they apply to `PUBLIC` including `anon`; note they were later rewritten to the `(select auth.uid())` InitPlan form by `20260618120000_harden_advisors.sql` lines 26-32, so read that one too before assuming the bare-`auth.uid()` defect is still there — it is not), `20260626000000_add_document_status.sql` (document_status, PK `(owner, kind)`), `20260620000000_add_outcome_validation.sql` (predictions/attempts/events — **the three INSERT policies contain inline `exists` subqueries against peer tables and re-assert `owner` equality**).
- Harness to extend, not duplicate: `tests/integration/tenant-isolation.itest.ts` (429 assertions, the matrix), `tests/integration/fixtures/tenancy.ts` (two-org factory, recording RLS-client factory, localhost hard-guard, disposable-org seeder, `readGrantedWriteSurface()`), `tests/integration/case-rls.itest.ts` (the catalog/grant/anti-recursion/EXPLAIN smoke, `psql`-in-container idiom for `information_schema`). MV-153's dossier records what each proves and, more usefully, the four ways its earlier rounds proved nothing.
- Sibling cards: **MV-154** (Stage 2 umbrella + reversibility requirement), **MV-155** (personal cases, nullable `case_id`, backfill, case indexes — hard blocker), **MV-156** (owner nullable + composite-FK rebase — hard blocker), **MV-157** (repositories onto case authorization), **MV-158** (anonymous claim under the case model), **MV-160** (tighten + Stage 2 exit — its §D **ships the policy rewrite** that drops the transitional owner disjunct this card creates, as step (d) of its migration, and retires this card's window-scoped test block).

## Acceptance criteria

**Spec fidelity — the stage-level rule, added 2026-08-03 because it has now failed twice running**
- [ ] **If this slice's implementation contradicts the Stage 2 spec in any cell, the spec is amended IN THIS PR — recording it only in a decision log or PR body does not satisfy spec §1 rule 3, because the next slice reads the spec, not your log.** The file is `docs/superpowers/specs/2026-08-02-stage2-migration-and-access-matrix.md`. The amendment is an edit to **every** place that states the wrong thing — the matrix cell in §4, any §9 item that quotes it, and any §10.1 rollback row that would **re-create** it — **plus** a dated entry in §12. Where the same stale claim also sits in a `board.json` card summary, correct that field. **The check is mechanical:** `git diff --stat origin/master -- docs/superpowers/ docs/kanban/board.json`; if this PR reports a spec contradiction anywhere and that diff is empty, this criterion is not met. It bites hardest on this card: §4's per-table **Policy form** and **Grants after** cells are what MV-160 asserts against, so a policy shipped differently from its cell and recorded only here hands MV-160 an assertion that goes red against correct output. **Why it is a criterion and not prose:** MV-155 measured that §4.4/§4.6's `UPDATE` grant cells were unexecutable, and MV-156 measured that the same two cells' replacement uniques were unexecutable. Both shipped the correct SQL, both reported it accurately in their own Decision log and PR body, and **neither amended the spec** — a reviewer had to, afterwards, both times. In MV-156's case the un-amended cell was one §10.1 **R1** would have *executed* during a stage rollback.

**Scope — nine tables, named**
- [ ] Every owner-only policy on `profiles`, `assessments`, `plan_items`, `user_program_state`, `documents`, `document_status`, `program_predictions`, `application_attempts`, `outcome_events` is **dropped and replaced** with a case-aware equivalent in one migration. `enable` + `force` row level security is true on all nine **after** the swap (verified from `pg_class`, not assumed — `documents` only gained `force` in a later fix migration, so it is the one most likely to lose it).
- [ ] One policy per table per command (no `multiple_permissive_policies`), named to the house convention (`<table>_<command>_<scope>`). The two quoted legacy `documents` policies (`"Users read own documents"`, `"Users delete own documents"`) are renamed in the process, and both gain the `to authenticated` clause they never had — today they apply to `PUBLIC`, which includes `anon`, and only the grant keeps anon out. That is the same latent-grant class MV-152 found on helper `EXECUTE`.

**The anti-recursion discipline, extended**
- [ ] Every case/assignment/membership lookup inside a policy runs through a `private` **SECURITY DEFINER STABLE** helper with `set search_path = ''`, EXECUTE revoked from `public` then granted to `authenticated` only. **No policy predicate on any of the nine tables contains an inline subquery against a `public` table** — asserted structurally from `pg_policy`, extending the assertion `case-rls.itest.ts` already runs over the six tenancy tables.
- [ ] The three parent re-assertions on `program_predictions` / `application_attempts` / `outcome_events` INSERT are re-expressed as **case parentage through definer helpers** (e.g. `private.assessment_case_id(uuid)`, `private.prediction_case_id(uuid)`, `private.attempt_case_id(uuid)`, mirroring `private.case_org_id`), not as `owner` equality and not as an inline `exists`. Observable: an authorized actor can insert a prediction whose row carries `case_id` and a null `owner`; nobody can attach a child row to a parent in a case they cannot access; and the check does not silently deny when the parent is visible to the actor only through a different disjunct of the parent's own SELECT policy.
- [ ] A new uncorrelated array helper (`private.actor_case_ids()` or equivalent) returns every case the actor may reach — student link, org owner/admin, and **membership-status-gated** assignment — so list predicates take MV-152's measured InitPlan shape `case_id = any ((select private.actor_case_ids())::uuid[])`. It is defined in terms of the existing `private.can_access_case` semantics so the two cannot drift.

**The transitional window (this card ships it; MV-160 §D removes it, as step (d) of its migration)**
- [ ] While `case_id` is nullable, every policy predicate is the disjunction `owner = (select auth.uid()) OR (case_id is not null AND <case predicate>)`. **The removal is a real, ordered, executable step on MV-160 as of 2026-08-02 — §D's "ships the policy rewrite" criterion and step (d) of its migration order — not a hand-off in prose.** It runs after MV-160's `SET NOT NULL`s, re-creates each policy under the same name with the disjunct dropped and everything else unchanged, and only then asserts that no predicate reads `owner`. Write your policies so that step is a **predicate edit and nothing more**: one policy per table per command, named to the house convention, the disjunct as the outermost `OR` of the predicate rather than woven through the case branch. A predicate MV-160 has to restructure is a predicate MV-160 will get wrong.
- [ ] **Isolate the transitional-window test so MV-160 can retire it cleanly.** The case-less-row test below is a test of a state MV-160 deletes: after its `SET NOT NULL`s the fixture cannot be seeded at all (`23502`), so the test dies whether or not the disjunct is dropped. Put it in **one** clearly-named block — `describe("transitional owner disjunct — retired by MV-160", …)` — containing nothing else. MV-160 §E deletes exactly that block and is forbidden from editing any other expectation; a block that also carries a matrix cell (cross-tenant denial, dual-role, revocation immediacy, anonymous invisibility) makes that deletion lossy and turns a clean retirement into an access-control edit.
- [ ] **`private.actor_case_ids()`'s student-link arm must NOT be gated on membership status, and this is load-bearing twice over.** It is the dual-role rule today (a revoked staff member who is the linked student of their own case keeps that case's rows) — and it is also what makes MV-160's disjunct removal safe: `owner = (select auth.uid())` implies the case branch **only** because a personal case is always reachable by its linked student. Gate that arm and MV-160's drop hides every personal student's own data from them, silently. Assert the property directly here rather than leaving it as an emergent consequence of `can_access_case`. Observable with a deliberately case-less row: a row whose `case_id` is still null stays visible and updatable to its legacy owner **and to nobody else** — an org admin of an unrelated tenant sees nothing, and a case-scoped actor sees nothing. A pure case predicate would make every not-yet-backfilled row vanish from its real owner; that is the Stage 2 exit-gate failure, created here.
- [ ] **Anonymous assessments stay invisible.** A row with `owner is null AND case_id is null` is returned to **no** authenticated client, including the user who is about to claim it, and remains readable and writable by the service-role claim path (MV-158) and the MV-135 purge job. Paired with a service-role existence proof, because an RLS SELECT refusal and an empty table are indistinguishable.

**The matrix holds at the DB layer, evaluated as the authenticated user**
- [ ] Positive: a student reads and updates their own case's rows on all nine tables exactly as they could before Stage 2; an org owner/admin reaches every case in their org; an **assigned** counsellor reaches their assigned case's student data.
- [ ] Negative: an org-A actor (owner, admin, or counsellor) cannot select, update, or delete any of the nine tables' rows belonging to an org-B case; an **unassigned** org-A counsellor cannot reach an assigned-only case's student data; a student cannot reach another student's rows on any of the nine.
- [ ] Revocation is immediate on student data: flip `organization_memberships.status` to `inactive` and the same signed-in client's next select on that case's `plan_items` returns zero rows — no re-login, no token change. The dual-role rule survives: a revoked staff member who is also the linked student of their own case keeps that case's rows.
- [ ] No cell contradicts `docs/superpowers/specs/2026-08-02-stage1-canonical-access-matrix.md`. The migration carries a comment mapping each table to the matrix verb it implements (`case.read` → SELECT, `case.update` → UPDATE). **Any cell the matrix does not determine is a new decision and is written into a spec amendment, not invented in SQL** — that is exactly how MV-151 and MV-152 came to diverge in six places.

**No write hole, no widened surface**
- [ ] Every mutating policy carries **both** `using` and `with check`. `case_id` and `owner` are **not** in any column-level UPDATE grant on any of the nine tables, so re-pointing a row into another case is unexpressible by any client on any path (MV-152's mechanism — a `WITH CHECK` sees only NEW and structurally cannot say "this column may not change"); the `WITH CHECK` still asserts the NEW row lands in a case the actor may reach, as defence in depth if that grant ever widens.
- [ ] **The grant set is unchanged FROM THE POST-MV-155 BASELINE**, read column-granularly out of `information_schema` before and after this migration. **Capture that baseline from the migrated local stack at the start of the card rather than transcribing it here.** The table-level enumeration this criterion used to carry (`profiles` select+update · `assessments` select · `plan_items` select+update · `user_program_state` select+insert+update+delete · `documents` select+delete · `document_status` select+insert+update+delete · `program_predictions`/`application_attempts`/`outcome_events` select+insert+delete) is the **pre-MV-155** set and asserting it would fail: MV-155 §H revokes those flat `UPDATE`/`INSERT` grants on six tables and re-grants explicit column lists, and — per its corrected §H — adds `case_id` to the INSERT lists on the five insert-granted tables so MV-157's dual-write is expressible. This card adds **no verb and no column** to any grant; it changes **who a row is scoped to, not what a client holds**. Consultancy write verbs are Stage 3 and a grant change is a separate reviewed decision. `anon` holds nothing on any of the nine.
- [ ] **The refusal above stands unchanged, and the deferral it creates is now written down where the next reader will find it: `docs/superpowers/specs/2026-08-02-stage2-migration-and-access-matrix.md` §6 enumerates every verb Stage 2 defers and the exact grant + policy Stage 3 must add for each.** Recorded here because this card is where the refusal is enforced and therefore where somebody will come to argue with it. The four that matter: `profiles` INSERT, `assessments` INSERT/UPDATE, `plan_items` INSERT, `documents` INSERT — all blocked by the **absent `authenticated` grant**, not by any predicate on this card, so no policy written here could unblock them and none should try. **This was a live contradiction, not a hypothetical:** MV-160 §D's original consultancy proof required exactly those four writes as its exit evidence, which made that criterion unsatisfiable before a line of Stage 2 SQL existed (an assigned counsellor *is* the Postgres role `authenticated`; RLS narrows a grant and never widens one). The integrator resolved it **in this card's favour** on 2026-08-02 — MV-160's proof was narrowed to read-on-nine + write-on-five, and this card's rule did not move. The risk note below ("It is tempting … Don't.") named the temptation before it arrived; it is now backed by a `42501` negative assertion in MV-160's tighten suite that goes red the moment Stage 3 grants any of the four.
- [ ] `program_predictions` keeps its immutability: still no UPDATE policy, still no UPDATE grant, and `private.reject_prediction_update` still raises — including for `service_role`, and including for whatever narrowed form MV-155 left behind for the backfill. `outcome_events` stays append-only (no UPDATE policy, corrections remain a new row + `supersedes_event_id`).

**Proof it plans and proof it is clean**
- [ ] `EXPLAIN (ANALYZE, BUFFERS)` on the representative authenticated list query for each of the nine tables shows an Index Scan / Bitmap Index Scan on the MV-155 case indexes and the legacy owner indexes, **no Seq Scan**, and the helper as an **InitPlan** (once per statement, not once per row). Plans captured in the PR. Note MV-152's fixture lesson: it is tenant/case *count*, not row count, that makes the array-parameter plan appear — a too-small fixture turns this assertion into a false alarm.
- [ ] Advisors report zero new findings for these objects: no `rls_enabled_no_policy`, no `function_search_path_mutable` on the new helpers, no `multiple_permissive_policies`, no `unindexed_foreign_keys`, and `auth_rls_initplan` is clean. **Corrected 2026-08-02 against the live hosted schema:** this criterion previously said `documents`' two legacy policies "use bare `auth.uid()` and are therefore a per-row evaluation this card fixes on the way past". **They do not.** Both read `(( SELECT auth.uid() AS uid) = owner)` — the InitPlan form — rewritten by `supabase/migrations/20260618120000_harden_advisors.sql` lines 26-32. There is no `auth_rls_initplan` finding on `documents` to fix, and an agent who sets out to verify the stated defect will find it absent and may reasonably conclude the whole criterion is stale. **The other half of the `documents` claim is real and survives:** both policies genuinely carry **no `to` clause**, so they apply to `PUBLIC` including `anon`, and adding `to authenticated` stays an acceptance criterion of this card. (If the Supabase MCP is unauthenticated, run the advisor rules as catalog SQL against the migrated local stack, as MV-152 did, and record that the hosted `get_advisors` re-run is owed at apply time.)
- [ ] `lib/supabase/types.ts` is unchanged — this migration creates no table and alters no column.

**Harness extension (the Stage 1 harness proves nothing about these tables today)**
- [ ] `tests/integration/fixtures/tenancy.ts` seeds **student data** on cases in both orgs: a profile, ≥2 assessments including one primary, ≥1 anonymous case-less assessment, plan items (including one open per kind), program state, a document, a document_status row, and a full `prediction → attempt → outcome_event` chain — plus at least one deliberately **case-less** row on an owner-held table to exercise the transitional disjunct.
- [ ] `tests/integration/tenant-isolation.itest.ts` covers all nine tables in the cross-org denial catalogue and the positive matrix, in both directions, each denial paired with a service-role existence proof.
- [ ] `readGrantedWriteSurface()`'s completeness guard is extended to the nine tables, so a grant added to any of them without a matching probe **fails a test** — the mechanism MV-153 round 3 had to make real after claiming it for two rounds.
- [ ] **Mutation evidence, not just green.** Each new policy is reverted directly on the live local database and the suite re-run; each revert turns a *named* test red and nothing else. A passing RLS suite is worth nothing on its own — an RLS SELECT denial is silent, so `expect(data).toEqual([])` passes just as happily against a deleted policy.
- [ ] CI still proves the suite ran: if this card adds a **new** `*.itest.ts` file rather than extending the two existing ones, `.github/workflows/ci.yml` gains a `✓ <path>` guard for it. Without that, a new suite that skips every assertion greens the lane — the exact failure MV-153's round-2 MAJOR 3 found on the guards that were already there.

**Reversibility (MV-154's requirement, in the form this card takes)**
- [ ] This card mutates **no data**, so it needs no data-migration rehearsal — but it changes what live users can *see*, which is the same blast radius. Rehearsed on a copy of live data: for each of the 7 real profile owners, the set of row ids visible **as that authenticated user** on each of the nine tables is byte-identical before and after the migration. A `0 rows` result for a real student is the failure this rehearsal exists to catch, and it is silent in production.
- [ ] A stated rollback: the previous policy set, re-applied as a single reverse migration held in the PR (policy-only, so revert is exact and instant). Named in the PR body before merge, not written after an incident.

## Test plan
- **Two files, one seam, mirroring Stage 1.** `case-rls.itest.ts` owns the *policy smoke* for the nine tables — catalog shape from `pg_policy` (one policy per table per command; `polqual` **and** `polwithcheck` present on every UPDATE; no predicate naming a `public` table), the `information_schema` grant pin — **column-granular, and pinned against a baseline captured from the migrated stack at the start of this card, never against a list transcribed onto the dossier**; the post-MV-155 set is explicit column lists, not the pre-MV-155 table-level verbs, and a test that hard-codes the old enumeration fails on a correct migration — helper properties (`prosecdef`, `provolatile = 's'`, `proconfig` contains `search_path=`, owner holds `BYPASSRLS`, `anon` holds no EXECUTE), and the EXPLAIN assertions. `tenant-isolation.itest.ts` owns the *matrix* — every actor × table × verb cell. Do not duplicate across the two; MV-153's decision log records that seam and the CI guards assume it.
- **Every tenant query goes through an authenticated client.** The service-role admin is used only to seed, to tear down, and to prove that a denied read was *denied* rather than empty. A green service-role test proves exactly nothing here: `service_role` holds `BYPASSRLS`.
- **Named proofs, each its own test:**
  - *anti-recursion, structurally* — extend the `pg_policy` sweep to the nine tables; a single inline `exists` against `public.assessments` in a predicate fails it.
  - *transitional disjunct* — a case-less row is visible to its owner, invisible to a cross-tenant admin, invisible to a case-scoped actor; then backfill its `case_id` in-test and confirm the case-scoped actor gains it and the owner keeps it.
  - *anonymous assessment* — invisible to every authenticated client in the fixture (paired with a service-role existence read), still readable by the service-role claim path, still matched by the MV-135 purge predicate.
  - *parent-case re-assertion* — an actor may insert an attempt only against a prediction in a case they can reach; a consultancy-shaped row (`owner` null, `case_id` set) inserts successfully; a cross-case parent is rejected.
  - *no re-point* — `update plan_items set case_id = <other case>` and `set owner = <someone else>` are rejected `42501` (column grant), the row is verified unchanged by a service-role read, and a legitimate in-case `status` update succeeds.
  - *predictions immutability survives Stage 2* — an authenticated UPDATE is refused, and a `service_role` UPDATE still raises from the trigger.
  - *revocation immediacy on student data*, and the *dual-role* case (revoked staff who is the linked student of their own case keeps it).
  - *EXPLAIN* per table, at a fixture large enough for the planner to have a real choice.
- **Regression, not just isolation.** A test that reproduces the Stage 2 exit-gate question one card early: seed a personal case exactly as MV-155's backfill produces it, and assert the personal student's visible row set on all nine tables is identical to the pre-migration owner-scoped set. MV-160 owns the exit gate against production-shaped data; this is the cheap local version that stops MV-159 shipping the regression in the first place.
- `npm test` stays green — watch for unit suites that assert legacy policy text or owner-scoped repository behaviour; if MV-157 already moved them, this card should need no unit change at all, and needing one is a signal that a repository still assumes actor-equals-student.

## Integration gate
- `npm run typecheck` · `npm run lint` · `npm test` · **`npm run test:integration`**
- `test:integration` is **mandatory and gating** here: this card produces real-DB authorization behaviour, and a mocked client structurally cannot exercise RLS, definer helpers, column grants, or `WITH CHECK` rejection.
- **Repo posture:** `master` is protected by an active ruleset with **no bypass** — required checks `integration` **and** `validate` (strict / up-to-date), PR required, no direct pushes, no force pushes. The `integration` job is **gating** (`continue-on-error` is gone, PR #111). So: branch, PR, both checks green on a branch that is up to date with `master`, and the merge is the integrator's. A red integration lane blocks this card; there is no local-green override.
- Locally: `npx supabase start`, then export `SUPABASE_TEST_URL`, `SUPABASE_TEST_SERVICE_ROLE_KEY`, `SUPABASE_TEST_ANON_KEY` from `npx supabase status -o env`.

## Dependencies / blocked-by
- **Hard blocker — MV-155.** `case_id` must exist on all nine tables, be backfilled from the current owner relationship, and be indexed. Without the backfill every case predicate matches nothing and the transitional owner disjunct is carrying 100% of the load — the suite would be green and the card would have proven nothing.
- **Hard blocker — MV-155's §H grant rewrite, specifically for the "grant set is unchanged" assertion.** That criterion is only meaningful relative to a baseline, and the baseline is whatever §H leaves behind: flat `UPDATE`/`INSERT` on six tables replaced by explicit column lists, with `case_id` present in the INSERT lists and absent from every UPDATE list. Write this card's grant pin **after** MV-155 is merged and applied locally, reading the real set out of `information_schema`; a pin drafted from the pre-MV-155 table-level verbs will fail, and "fixing" it by re-granting the flat verbs would silently undo §H and hand `authenticated` `UPDATE(case_id)` — the exact hole §H exists to close. If MV-155 resolved its UPSERT seam by the definer-trigger route, this card must also confirm the trigger is `security definer` with `set search_path = ''` and does not become a second, unreviewed write path.
- **Hard blocker — MV-156.** Two of its outputs are load-bearing here: (a) `owner` nullable, or a consultancy-shaped row (`owner` null, `case_id` set) cannot exist and the positive half of the parent-case re-assertion criterion is untestable; (b) the predictions→attempts→outcome_events chain re-based onto `unique (id, case_id)`, because these policies re-assert **case** parentage and the composite FK must agree with them or the two disagree row by row. Note the plan's warning (line 326): while `case_id` is nullable a composite key including it is unenforced for null rows under MATCH SIMPLE, so MV-156's compensating CHECK is what these policies lean on until MV-160.
- **Watch item MV-156 must have resolved, or this card cannot be written as specified:** `user_program_state`'s primary key is `(owner, program_id)`, `document_status`'s is `(owner, kind)`, `documents` has `unique (owner, kind)` and `profiles` has `owner uuid not null unique`. A nullable column cannot sit in a primary key. If MV-156 left those keys owner-based, say so and stop — do not invent a key swap here.
- **Coordinates with MV-157.** MV-157 moves repositories onto case authorization and dual-writes `owner` + `case_id`; these policies must admit exactly what those repositories legitimately do and nothing more. Divergence is a bug in whichever lands second — cross-check against MV-151's permission matrix and the canonical access matrix, never against the other implementation.
- **Coordinates with MV-158.** The claim path runs as `service_role` and therefore bypasses these policies, but everything the user sees *immediately after* a claim is read through the authenticated client and is governed by them. The claim itest must stay green.
- **Consumed by MV-160**, which makes `case_id` NOT NULL where the domain requires it and **then re-creates every policy this card ships with the transitional owner disjunct removed** (MV-160 §D; step (d) of its migration order, after the `SET NOT NULL`s), before asserting that no predicate reads `owner` — and proves the stage exit. That is a real DDL step on that card as of 2026-08-02, not a note: leave the disjunct as the outermost `OR` and isolate its test in one named block so the removal is a predicate edit and a block deletion, nothing more.
- **Builds on MV-152's shipped helpers** (`private.can_access_case`, `can_staff_case`, `org_role`, `is_org_admin`, `case_org_id`, `actor_assigned_case_ids`, …) in `supabase/migrations/20260730180000_case_aware_rls_policies.sql`. Extend that set; do not fork it.
- A local Supabase stack (Docker) plus **a copy of live data** for the visibility rehearsal — MV-154 holds the reversibility requirement and the procedure for obtaining that copy.

## Risk notes
- **Denial is silent, and that is the defining hazard of this card.** An RLS SELECT refusal returns zero rows and no error — indistinguishable from an empty table, a fixture that never seeded, or a policy that was never created. Every "sees nothing" assertion must be paired with a service-role read proving the rows exist, and preferably with the actor who legitimately does see them. This is also the production failure mode: a wrong predicate here does not throw, it just makes a real student's assessments disappear.
- **The nullable-`case_id` window is where existing students break.** Between MV-155's backfill and MV-160's tighten, some rows will legitimately carry a null `case_id` (anonymous assessments always; anything created by a path MV-157 has not yet migrated, transiently). A pure case predicate hides them from their own owner. The owner disjunct is the mitigation, and it is only *safe* because MV-155's backfill guarantees a row's `case_id` is its owner's personal case — if that invariant is ever broken, the disjunct becomes a stale-access hole. Test the invariant, do not assume it.
- **The recursion landmine is now reachable on these tables, and it was not before.** `pp_insert_own`, `aa_insert_own` and `oe_insert_own` today contain inline `exists` subqueries against `assessments`, `program_predictions` and `application_attempts` respectively. Those peer tables are about to acquire their own policies. Two distinct failures follow: a predicate that reads a table whose policy reads back is `42P17`; and — the quieter one — **a table referenced inside a policy expression has its own RLS applied**, so a legitimate insert can be silently denied because the parent row is not visible to the actor through the disjunct the planner happened to use. Both are avoided the same way: definer helpers, never inline subqueries. Plan line 345 mandates it and MV-152 proved it structurally.
- **SECURITY DEFINER is a loaded gun, and there are now more of them.** Each new helper must pin `set search_path = ''` and fully qualify every object; derive the actor from `auth.uid()` **internally** and never trust a caller-passed user id as the authority; be STABLE and side-effect free; and have `EXECUTE` revoked from `public` *before* being granted to `authenticated` — a new function's EXECUTE defaults to PUBLIC and `anon` is in PUBLIC. MV-152 shipped that latent grant on its first green and only caught it with an assertion.
- **Widening a grant is the easiest accidental tenant hole in this card.** It is tempting, while "making these tables case-aware", to hand `authenticated` the INSERT on `assessments` or `documents` that consultancy staff will eventually need. Don't. Those verbs are Stage 3, they arrive with their own review, and the byte-identical grant assertion exists to make the temptation fail CI. **The temptation arrived on schedule and was refused:** MV-160's first draft required those exact writes as its exit proof. Resolved 2026-08-02 by narrowing MV-160's proof rather than widening this card's grants — reasoning in `2026-08-02-stage2-migration-and-access-matrix.md` §7.3, deferred grant list in §6.
- **`program_predictions` immutability must survive being touched twice.** MV-155 adjusts the trigger to permit a `case_id`-only backfill; MV-159 then rewrites its policies. Either step can leave a general UPDATE path open. Assert the trigger still raises for `service_role` — a "no UPDATE policy" is not immutability, which is the whole reason the trigger exists.
- **Two authorization conventions will coexist in the documents bucket, deliberately.** `storage.objects` still carries the one-folder-per-user policies (plan line 328); this card scopes the `documents` *rows* to cases and leaves the objects alone, because retiring those policies is Stage 4's job alongside the header/versions replacement. Record the window rather than closing it quietly — an object path that says "user" beside a row that says "case" is exactly the kind of drift that later reads as a bug in whichever layer is examined second.
- **Performance: `actor_case_ids()` grows with a counsellor's caseload.** For the personal-student path it is a one-element array and the plan is trivial. For a busy consultancy admin it is every case in the org. Measure at a realistic case count, and remember MV-152's finding that Postgres cannot see inside `= ANY ($initplan)` and assumes ~10 elements — the plan flips on estimated selectivity, so a fixture that is too small measures the wrong plan and a fixture that is too large hides a genuine regression.
- **Scope creep with a security shape.** The student-visible-vs-consultancy-only note classification is Stage 5, not table RLS; the document header/versions model is Stage 4; audit events on sensitive reads are Stage 6. This card gates access to *rows* on nine existing tables. Anything else that arrives dressed as "while we're in here" belongs on another card.

## Agent resume notes (for a cold start)
1. **First concrete action:** confirm the blockers are merged and inspect what they actually left behind. `git log --oneline -20`, then `ls supabase/migrations/` and read the newest MV-155 and MV-156 migrations end to end. You need, per table: the exact `case_id` column, its nullability, its index name, the compensating CHECK MV-156 added for the composite-FK nullable window, and whether the primary keys on `user_program_state` / `document_status` (and the uniques on `documents` / `profiles`) were re-keyed. If `case_id` is absent or unbackfilled anywhere, **stop** — MV-155 is the blocker and this card cannot start.
2. Read, in this order: **`docs/superpowers/specs/2026-08-02-stage2-migration-and-access-matrix.md` FIRST — §3 (the invariant), §4 (the per-table matrix: current shape, grants before/after, role × verb, policy form, rollback, final state, for each of the nine), §6 (the verbs deferred to Stage 3), §7 (why this card's grant refusal was upheld) and §9 (where the dossiers contradict reality)**; then plan lines 277–345 and 604–637 (`docs/superpowers/plans/2026-07-23-consultancy-student-case-workspace.md`); the canonical access matrix (`docs/superpowers/specs/2026-08-02-stage1-canonical-access-matrix.md`) in full; then `supabase/migrations/20260730180000_case_aware_rls_policies.sql` in full — its header comments are the design rationale you are extending, not boilerplate.
3. Read the nine legacy policy definitions (the six migrations listed in Context links) — **then TRANSCRIBE the per-table matrix from spec §4; do not re-derive it.** §4.1–§4.9 already contain, per table, exactly what this step used to ask you to reconstruct: current policies and shape, current grants column-granularly, current owner-derived uniques and keys, the policy form to write, the matrix verb, and the final Stage 2 state. Re-deriving it in scratch notes is the **MV-151/MV-152 failure the spec was written to end** — six slices each independently deriving a matrix from prose is how they diverged in six cells, four of them SQL-more-permissive. Copy §4 into your notes if a working copy helps; the SQL is transcription of a committed artifact, not of your own reconstruction. **Where the spec and the live schema disagree, that is a FINDING: stop, record it, and raise it — do not silently resolve it in either direction.** The spec's §2 inventory was captured from the hosted project on 2026-08-02 and independently re-verified, so a disagreement means either the schema moved under it or a predecessor slice did not land as specified, and both are somebody's card. Reconciling it inside this card's SQL buries the one signal that would have caught it.
4. Write the failing tests first. Extend `tests/integration/fixtures/tenancy.ts` with a `seedStudentData(caseId)` helper, then add the nine-table cells to `tests/integration/tenant-isolation.itest.ts` and the catalog/EXPLAIN smoke to `tests/integration/case-rls.itest.ts`. Copy the localhost hard-guard, the recording client factory, and the `psql`-in-container `information_schema` idiom rather than reinventing them.
5. Write ONE migration `supabase/migrations/<timestamp>_case_aware_student_data_rls.sql`, timestamped after every MV-155/MV-156 migration. Structure it like MV-152's: (0) the BYPASSRLS precondition `do` block; (1) new `private` helpers — `actor_case_ids()`, `assessment_case_id()`, `prediction_case_id()`, `attempt_case_id()` — each `security definer stable set search_path = ''`, EXECUTE revoked from `public` then granted to `authenticated`; (2) drop the legacy policies per table; (3) create the replacements with `USING` + `WITH CHECK`, the transitional owner disjunct, and the array-helper InitPlan shape; (4) re-review grants and assert they are unchanged; (5) a closing comment block enumerating every deliberate omission. Ship the reverse migration in the PR as the stated rollback.
6. Commands (PowerShell): `npx supabase start` · `npx supabase db reset` (applies every migration locally) · `npx supabase status -o env` → set `$env:SUPABASE_TEST_URL`, `$env:SUPABASE_TEST_SERVICE_ROLE_KEY`, `$env:SUPABASE_TEST_ANON_KEY` · `npm run test:integration` · `EXPLAIN (ANALYZE, BUFFERS)` via `docker exec -i <db container> psql -U postgres -d postgres` or Studio · then `npm run typecheck; npm run lint; npm test` · `npx supabase gen types typescript --local > lib/supabase/types.ts` and confirm no diff.
7. Mutation-test before claiming done: revert each new policy on the live local database (`alter policy` / `drop policy`), re-run the suite, confirm a *named* test goes red and nothing else, then restore. Record the table in Done evidence. This is the repo's bar — MV-153 was sent back twice for assertions that were green and inert.
8. Then the visibility rehearsal on a copy of live data (MV-154 holds the procedure): for each of the 7 real owners, capture the visible row-id set per table as that authenticated user, apply, re-capture, diff. Any non-empty diff is a blocker, not a note.
9. Branch, PR, both required checks green and the branch up to date with `master`. Never point the harness at a non-local database — the hard guard refuses non-localhost URLs, and keeping that guard is not optional.

## Decision log
- 2026-08-02 — Card carved from Stage 2 of the revised consultancy plan (integrator session).
- 2026-08-02 — Scope fixed at **nine tables** — `profiles`, `assessments`, `plan_items`, `user_program_state`, `documents`, `document_status`, `program_predictions`, `application_attempts`, `outcome_events` — matching the plan's "Moving existing data to cases" domain list as it exists in the real schema. `leads` is deliberately excluded: it is RLS-on/no-policy/no-grant by design (the `harden_advisors` precedent) and carries no case.
- 2026-08-02 — **The transitional owner disjunct is this card's, and removing it is MV-160's.** Shipping a pure case predicate while `case_id` is nullable would hide not-yet-backfilled rows from their own owners — the precise regression Stage 2's exit gate forbids. The disjunct is a stated, dated, single-card-lifetime widening, not a permanent shape. **Amended 2026-08-02 (integrator): "removing it is MV-160's" was true as an intention and false as a work assignment — MV-160 contained no policy DDL, only an assertion that no predicate reads `owner`, which would have gone red against this card's own correct output. MV-160 §D now ships the rewrite as step (d) of its migration, after its `SET NOT NULL`s. Nothing on this card changed except that the hand-off now points at something executable, and two obligations were added: keep the disjunct as the outermost `OR` so the removal is a predicate edit, and isolate the transitional-window test in one named block so MV-160 can retire it without touching a matrix cell.**
- 2026-08-02 — **The three parent re-assertions become case parentage through definer helpers, not inline `exists`.** The legacy `pp/aa/oe` INSERT policies re-assert `owner` equality via subqueries against peer tables; under the case model those subqueries are both an anti-recursion violation and a silent-denial hazard (a referenced table's own RLS applies inside a policy expression), and the owner equality is the actor-equals-student assumption Stage 2 exists to delete.
- 2026-08-02 — **The grant set does not change.** This card re-scopes rows; it does not hand `authenticated` a verb it did not already hold. Consultancy write surfaces are Stage 3 and arrive with their own review. Asserted column-granularly from `information_schema` so the rule cannot erode.
- 2026-08-02 — **Integrator, post-Codex-review: the MV-159 / MV-160 grant contradiction is resolved IN THIS CARD'S FAVOUR, and this card does not change.** MV-160 §D required an assigned counsellor to drive every migrated **write** path — profile upsert, assessment, plan items, document row — as its operational exit proof, while this card forbids adding the grants those writes need. Against the real grant table, this card was right on the facts: an assigned counsellor is the Postgres role `authenticated`, which holds **no INSERT** on `profiles`, `assessments`, `plan_items` or `documents`, so four of MV-160's eight write paths fail with `42501` regardless of what policy anyone writes — the criterion was unsatisfiable before Stage 2 began. The alternative resolution (grant the four verbs here) was rejected on five grounds recorded in matrix spec §7.3: the plan's actual exit gate (line 637) is about *repositories* not depending on actor-equals-student and survives the narrowing; Stage 0 D-B gates consultancy-**entered** student data to Stage 3 and those four INSERTs are precisely that mechanism, so granting them here ships the gated surface ahead of its consent attestation and APP 5 notice; **no Stage 2 caller needs them** — all four paths are already registered service-role exceptions and stay so, and the exception list still shrinks by one via MV-157 §G's shortlist flip; the enforcement boundary is not weakened by the deferral; and this card's refusal is internally correct, so the cheaper correction is the right one. MV-160 §D is amended instead, and the deferral is pinned by a `42501` negative so Stage 3 cannot land the grants unnoticed.
- 2026-08-02 — **Seam check: "unchanged" means unchanged from the POST-MV-155 baseline, and the baseline is measured, not transcribed.** The first draft spelled the expected grants out on the card as table-level verbs. That is the pre-MV-155 set: MV-155 §H narrows six of them to explicit column lists and puts `case_id` into the INSERT lists so MV-157's dual-write is expressible at all. A dossier-transcribed pin would have gone red on a correct migration and invited the worst available fix — re-granting the flat verbs. The pin is therefore captured from the migrated stack at the start of the card and diffed across this migration only.
- 2026-08-02 — **Re-pointing is prevented by column-level grants, with `WITH CHECK` as defence in depth**, adopting MV-152's build-time finding verbatim: a `WITH CHECK` sees only the NEW row and structurally cannot express "this column may not change", so `case_id` and `owner` stay out of the UPDATE grant entirely.
- 2026-08-02 — **`documents`' two legacy policies are fixed on the way past — but only the defect that exists.** They carry no `to` clause, so they apply to `PUBLIC` including `anon`, and only the absent grant keeps `anon` out; the replacements add `to authenticated`. **The second half of this entry was wrong and is withdrawn:** it claimed the two policies use bare `auth.uid()` and are a per-row `auth_rls_initplan` evaluation. Reading the hosted catalog on 2026-08-02 shows both already in the `(select auth.uid())` InitPlan form — `20260618120000_harden_advisors.sql` rewrote them. Withdrawn rather than quietly deleted because an unverifiable defect on a card is worse than no note: the agent who cannot find it has to decide whether the criterion is stale or they are wrong, and the cheap resolution is to skip it.
- 2026-08-02 — **The harness must be extended, not assumed.** MV-153's 429 assertions cover the six tenancy tables and say nothing about `profiles`, `assessments` or `plan_items`. Extending the existing two suites (matrix in `tenant-isolation.itest.ts`, smoke in `case-rls.itest.ts`) preserves the Stage 1 seam and the CI `✓` guards; a new file would need its own guard or it can skip silently and green the lane.
- 2026-08-02 — **Reversibility takes a different form here than on MV-155/MV-156.** No data is mutated, so there is no backfill to rehearse — but visibility changes for 7 real owners, and a wrong predicate fails silently. The rehearsal is therefore a before/after visible-row-id diff per owner per table on a copy of live data, and the rollback is the previous policy set held as a reverse migration in the PR.
- 2026-08-02 — **The canonical access matrix governs; cells it does not determine are escalated, not invented.** MV-151 and MV-152 diverged in six places precisely because each derived a matrix from the plan's prose independently. Any Stage 2 cell the matrix does not cover gets a spec amendment before it gets SQL.
- 2026-08-04 — **§4 was transcribed, not re-derived, and it held.** Every one of the 24 policies is its *Policy form* cell in SQL; the grant baseline was captured column-granularly from the migrated local stack (not from this card) before a line was written; the live catalogue was diffed against §2.6 and matched it row for row. **Three things moved, all amended IN THE SPEC in this PR** (§4.4, §4.6, §4.7–§4.9, §5, §10.1 R2, §12) rather than only here — the stage rule, honoured: (a) §4.4/§4.6's `owner → NULL is 42501` describes the LEGACY policy and stops holding; (b) §4.7–§4.9's plain-`=` parentage retires MV-156's "an authenticated client can still write an owner-only chain row"; (c) §5's open question about a client-created personal case is answered **refused**.
- 2026-08-04 — **`owner → NULL` on one's own row is now ADMITTED, and it ships unpatched.** Found by measurement: the shipped assertion in `case-backfill.itest.ts` went red against a correct migration. The case branch of the new `WITH CHECK` is TRUE for a row that stays in the actor's own case. Not patched because (1) the row provably cannot leave the case (`case_id` is in no UPDATE grant, so it is unchanged, and the row stays invisible to everyone else — both asserted); (2) the dangerous direction, `owner → another user`, is still `42501`, because MV-155 §H's derive trigger re-derives `case_id` onto the NEW owner's personal case, which the actor cannot reach; (3) it is the END STATE, not a transitional wrinkle — MV-160's pure case predicate admits it equally, since `owner` stops being the authorization axis. Closing it would require a predicate that reads `owner` outside the one line MV-160 deletes, which MV-160 §D forbids. Residual loss: provenance on that row (`owner … on delete cascade` no longer reaches it) — Stage 6's column removal settles it. Unreachable from any MeroVisa-authored caller.
- 2026-08-04 — **The parent-case clause is plain `=`, and that is a security decision, not a transcription detail.** Under `is not distinct from`, a child with `case_id IS NULL` compares TRUE against an unclaimed ANONYMOUS assessment (`owner NULL, case_id NULL`, id shareable in a URL) — so any signed-in client could hang a prediction-of-record off a stranger's assessment, which the legacy `a.owner = uid` refused. `=` yields NULL there and a `WITH CHECK` admits only TRUE. The cost is that an authenticated client can no longer create an owner-only chain row; nothing live does (MV-157 routed every chain insert through `lib/cases/dual-write.ts`, which writes `case_id` unconditionally with no owner-only fallback) and the SCHEMA still permits the shape, so the service-role and backfill paths are untouched. MV-156's assertion is inverted with the reasoning on it, not deleted.
- 2026-08-04 — **`cases_insert_admin` NOT widened.** Spec §5 named this card as the place the question is answered. Refused, for three reasons now recorded in the migration's omissions block and in §5: `ensurePersonalCase` is handed a service-role client at two already-registered exceptions so nothing needs it; a client-side personal-case INSERT is a new write verb on a Stage 1 tenancy table, which is exactly the grant change this card refuses; and `cases_personal_student_idx` is partial, so a client upsert would raise `42P10` whatever the policy says.
- 2026-08-04 — **The harness went into a SIBLING file, deviating from this card's "extend the two existing suites".** `tests/integration/student-data-rls.itest.ts`, anchored in `ci.yml` by name with a floor of 110, matching the six suites already anchored there. Reason: MV-160 §E must delete exactly one `describe` block, and a file-scoped deletion is checkable by eye where a block buried among Stage 1's 425 cells is not; and `tenant-isolation.itest.ts`'s completeness guard is keyed on `TENANCY_TABLES` with a `-ge 400` floor, so mixing the nine in would have meant restructuring the suite MV-160 is forbidden from touching. The Stage 1 seam is preserved rather than broken: the new file owns the nine tables' smoke AND matrix and duplicates neither existing suite.
- 2026-08-04 — **MV-153's `readGrantedWriteSurface()` had a blind spot that made its completeness guard meaningless for these tables, and it is fixed rather than worked around.** INSERT was read from `role_table_grants`; MV-155 §H's **column-level** INSERT grants write no row there, so five of the nine reported "no INSERT grant" while holding one — the guard demanded no probe for a verb they hold. It now reads INSERT from `column_privileges` (`distinct`, table granularity). The six Stage 1 tables are byte-identical under both forms, so `tenant-isolation.itest.ts` is unaffected.

## Done evidence
**Migration:** `supabase/migrations/20260803180000_case_aware_student_data_rls.sql` — 4 helpers, 24 policies, 0 table or column grants touched. Policy-only; `lib/supabase/types.ts` unchanged (verified: `npx supabase gen types typescript --local` produces no diff).

**Helpers MV-160 §D must keep, then drop at R2:** `private.actor_case_ids()`, `private.assessment_case_id(uuid)`, `private.prediction_case_id(uuid)`, `private.attempt_case_id(uuid)`. All `security definer stable set search_path = ''`, `EXECUTE` revoked from `public` then granted to `authenticated` only, owned by a role holding `BYPASSRLS` — each asserted from `pg_proc` / `has_function_privilege`, not assumed.

**The 24 policies MV-160 §D re-creates with the disjunct dropped:**
`profiles_select_case` · `profiles_update_case` · `assessments_select_case` · `plan_items_select_case` · `plan_items_update_case` · `ups_select_case` · `ups_insert_case` · `ups_update_case` · `ups_delete_case` · `documents_select_case` · `documents_delete_case` · `ds_select_case` · `ds_insert_case` · `ds_update_case` · `ds_delete_case` · `pp_select_case` · `pp_insert_case` · `pp_delete_case` · `aa_select_case` · `aa_insert_case` · `aa_delete_case` · `oe_select_case` · `oe_insert_case` · `oe_delete_case`.
Every one carries `owner = (select auth.uid())` as the **outermost `OR`**, on its own line, marked `-- MV-160 §D: delete this line`. A suite test parses all 28 rendered predicates and asserts the disjunct appears exactly once and that nothing but opening parentheses precedes it — so "MV-160's edit is a predicate edit" is a test, not a promise. `documents`' `"Service inserts documents"` (service_role INSERT) is untouched.

**MV-155's UPSERT-seam trigger, confirmed rather than re-asserted** (this card's dependency ask): `private.mv155_derive_case_id_from_owner()` is `security definer` with `proconfig` pinning `search_path`, and its two triggers are enabled and qualified `when (new.owner is not null)` — already pinned by `tests/integration/case-backfill.itest.ts` (`prosecdef` / `proconfig` / `tgenabled`, and both branches of the `WHEN`), which is CI-anchored. Not duplicated here, per the two-suite seam. It is **not** a second write path: it derives `case_id` from `owner` and writes nothing else, and the `owner IS NULL` branch it deliberately does not fire on is bounded by this card's `WITH CHECK`.

**Gate:** `npm run typecheck` clean · `npm run lint` clean · `npm test` **2598 passed / 328 files** · `npm run test:integration` **785 passed / 9 files** against a local stack at `20260803180000`, of which `student-data-rls.itest.ts` is **132**.

**How denial was proved rather than assumed.** Every "sees nothing" is paired with a service-role existence read that THROWS if the row is absent (`proveExists`), so a silent RLS refusal is distinguishable from a fixture that never seeded. Hard denials assert the code: INSERT across a case boundary is `42501`; UPDATE/DELETE across it are silent (zero rows affected, no error) and are therefore proved by a service-role read-back showing the row **unchanged**, not by an error. Every tenant query runs through a JWT-bearing client; the service role appears only to seed, prove and tear down.

**Mutation evidence** (`drop policy` on the LIVE local database, re-run the suite, restore): all 24 reverted individually; **every one turned at least 2 named tests red and none was inert**. Counts ranged 2–12; the catalogue test fails for any drop, and behavioural tests fail alongside it in every case. Restored count re-verified at 25 policies (24 + the untouched service_role INSERT).

**Plans** (`EXPLAIN (ANALYZE, BUFFERS)` as an org admin, 400 orgs / 10,000 cases / one row per case on each of the nine): **zero `Seq Scan` on any of the nine**. Each is a `Bitmap Heap Scan` over a `BitmapOr` of both disjuncts — the pre-Stage-2 `owner` index and the MV-155 `case_id` index — with **two InitPlans** (`auth.uid()` and `actor_case_ids()`), i.e. once per statement, not once per row. Asserted in the suite, not only captured.

**Reversibility.** Rollback: `supabase/rehearsal/MV-159-rollback.sql` (spec §10.1 **R2**) — one transaction, three guards, no `-v` flag, **no point of no return** (it mutates no data and re-runs). Predecessor: **MV-159 applied, MV-160 NOT applied**; Guard 2 refuses after MV-160 because the restored `(select auth.uid()) = owner` policies would hide `owner IS NULL` consultancy rows from the counsellor who owns the case, silently. Rehearsal: `supabase/rehearsal/MV-159-visibility.sql` — captures the visible row-id set **as each authenticated owner** on each of the nine, applies the migration inline, re-captures, and raises on any diff; procedure in `supabase/rehearsal/README.md` §"Running the MV-159 rehearsal". The founder-gated dump replay is still owed at production apply time, and the corpus's limits are stated there.
